# Implementation

Implementation docs are grouped by status.

- `active/` - work that may guide current implementation.
- `proposed/` - future or unapproved work. Review before coding.
- `completed/` - completed guides that still explain current behavior.

Use `docs/roadmap.md` as the planning index.

