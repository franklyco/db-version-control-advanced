# Archive: Root Handoffs

These docs are historical and should not guide current implementation work.

Current replacement docs:

- `docs/README.md`
- `docs/agent-entrypoints.md`
- module-local `AGENTS.md` files

Notes:

- The old root Visual Editor-heavy `AGENTS.md` is preserved as `AGENTS.visual-editor-snapshot.md`.
- The media/diff/official promotion handoff is preserved for context only.

