# Evidence Log

Record concrete repository evidence. Do not record assumptions as facts.

| ID | Date | Repository | Path/symbol/commit | Evidence | Implication | Release |
|---|---|---|---|---|---|---|
| E-001 | 2026-08-14 | DBVC | historical pre-commit branch `codex/visual-editor-linked-posts-plan`; HEAD `78a06ad`; `git status`/diff stats | Initial R0 began with protected dirty Visual Editor and unrelated work; those changes were later committed by the user | Historical dirty-boundary evidence; superseded for current authority by E-028 | R0/All |
| E-002 | 2026-08-14 | DBVC | `addons/visual-editor/src/Bootstrap/Addon.php`; `Addon::register()` | One composition root wires runtime guard, contexts, descriptor registry, resolvers, mutation, journal, cache, assets, hooks, and routes | Add R1 services through current composition root | R1 |
| E-003 | 2026-08-14 | DBVC | `addons/visual-editor/src/Registry/EditableRegistry.php` | Opaque public IDs map to private compressed descriptors in user-bound transient sessions with 8-hour default TTL and keepalive | Reuse conventions, not the store; scan snapshot requires separate blog/user/generation/paging lifecycle | R1/R2 |
| E-004 | 2026-08-14 | DBVC | `PageContextResolver`; `CapabilityManager`; `FrontendRuntimeGuard` | Current Visual Editor knows singular/archive/term contexts, owner-specific capabilities, and builder/frontend exclusions | Reuse context/permission/guard policy | R1 |
| E-005 | 2026-08-14 | DBVC | `addons/visual-editor/src/Rest/Controllers/ObjectSearchController.php` | Navigation search uses public/show-UI post types/taxonomies, configured exclusions, per-object capability, and a capped result query | Reuse policy conventions; do not repurpose capped navigation endpoint as scanner | R1 |
| E-006 | 2026-08-14 | DBVC | `addons/visual-editor/src/Resolvers/PostFeaturedImageResolver.php` | Canonical read is thumbnail ID; writes use set/delete post thumbnail; local image attachment validation exists | R2 can reuse resolver after fresh descriptor; R1 reuses read semantics | R1/R2 |
| E-007 | 2026-08-14 | DBVC | `AcfImageResolver.php`; `AcfGalleryResolver.php`; `AbstractAcfResolver.php` | Raw ACF reads, local image normalization, ordered gallery IDs, `update_field()`, post/term/user/option owners, and render-proven group/repeater/flexible existing rows exist | Media mutation primitives exist; generic off-page path discovery does not | R1/R2 |
| E-008 | 2026-08-14 | DBVC | `addons/visual-editor/src/Save/MutationService.php`; `CompositeSaveController.php` | Single mutation rereads old value but has no expected-old request; batch path has expected-value preflight/compensation for composite contracts | Add narrow expected-empty gate in R2; defer Save Row | R2 |
| E-009 | 2026-08-14 | DBVC | `ChangeJournalRecorder`; `ChangeJournalStore`; `CacheInvalidator`; DBVC activity/sync logger calls | Existing mutation flow journals before/after, audits, and invalidates post/term/user/option caches | No parallel mutation/audit/cache system | R2 |
| E-010 | 2026-08-14 | DBVC | `addons/visual-editor/src/Rest/Routes.php` | Routes exist for session/touch, descriptors, search, Shared Globals, seed, composite save, and save; no scan/progress/results/cancel endpoint | New cohesive Media Manager controller/routes required | R1 |
| E-011 | 2026-08-14 | DBVC | `addons/visual-editor/src/Assets/AssetLoader.php`; `AssetLoader::enqueue()` | Active Visual Editor eagerly calls both `wp_enqueue_editor()` and `wp_enqueue_media()` | R1 adds no new enqueue; package scan-only asset assumption corrected | R1 |
| E-012 | 2026-08-14 | DBVC | `assets/js/overlay-app.js`; `assets/css/overlay.css` | Toolbar/popovers, field-index search/scroll preservation, panel/focus/layering/status, and Media Library frame patterns exist; no large data-grid/virtualization component exists | Extend current vanilla frontend shell with scoped Media Manager table | R1/R2 |
| E-013 | 2026-08-14 | DBVC | `SharedGlobalFieldsController`; `DBVC_Visual_Editor_Addon::get_shared_global_field_names()` | Field-name allowlist defaults to `settings_globals_default_posts`; controller accepts only option-owned relationship/post_object and manually attaches descriptors with owner `option`/`acf_options` | Off-render precedent is narrow and relationship-specific, not a general registry/factory | R2/R3/R5 |
| E-014 | 2026-08-14 | DBVC | `addons/content-migration/mapping-catalog/dbvc-cc-target-field-catalog-service.php` | Recursive domain artifact/catalog includes broad meta/media heuristics and does not provide exact object-specific nested-media support | Do not use as R1 runtime catalog; reuse traversal lessons only | R1 |
| E-015 | 2026-08-14 | DBVC | `includes/Dbvc/AiPackage/AcfDiscoveryService.php` | Normalizes runtime/local JSON groups, nested fields, clones, layouts, return formats, and basic locations for AI packages | Possible later pure-normalizer extraction; not exact per-entity applicability | R1 |
| E-016 | 2026-08-14 | DBVC | `tests/phpunit/VisualEditorElementInstrumentationTest.php`; `scripts/check-ve-flex-image.php`; `scripts/check-ve-gallery-save.php`; VE QA docs | Focused PHP coverage is small/untracked; media scripts/manual probes exist; no dedicated JS/browser harness | Add focused policy/catalog/snapshot/REST tests; do not run mutating probes in R0/R1 baseline | R0/R1/R2 |
| E-017 | 2026-08-14 | Vertical | `admin/class-vf-tools-media-alt-ai.php`; `vf_media_health_state` | Scans attachments for size, alt, orphan parent, and backup usage; persists option state | Not a missing owner-field scanner | R1 |
| E-018 | 2026-08-14 | Vertical | `admin/media-unused-cleanup/class-vf-media-unused-cleanup-scanner.php`; repository | Attachment-first bounded dry run scans references in posts/postmeta/termmeta/options and rechecks selections before destructive work | Reuse stale reinspection/bounded-job ideas only; no code dependency | R1/R2 |
| E-019 | 2026-08-14 | Vertical | `admin/media-inventory/class-vf-media-inventory-scanner.php`; repository/installer | Bounded filesystem directory/file steps and blog-bound custom-table jobs | Useful progress precedent, wrong data/source contract | R1 |
| E-020 | 2026-08-14 | Vertical | `acf-json/field-groups/*.json` | 48 source JSON groups; explicit traversal found 113 image/16 gallery fields: 5 top-level, 105 group-only, 19 repeater; 5 conditional | Strong fixture/catalog evidence, not runtime authority; clone/runtime expansion changes counts | R0/R1 |
| E-021 | 2026-08-14 | Vertical | `functions/plugins/acf/field-context/class-vf-field-context-resolver.php`; catalog; `vf_field_context_get_entry_for_runtime_field()` | Semantic catalog exposes canonical key/name paths, clone/ancestor context, status, and caches; group query criteria do not replace exact location visibility | Reuse as fixture terminology/evidence only; DBVC must remain independent | R1/R5 |
| E-022 | 2026-08-14 | Vertical | `config/context-broker/acf-field-group-manifest-registry.json` | Schema `vf-acf-field-group-agent-context-registry.v1`, source `frameworkflo-live.local`, 23 curated groups | Curated manifest is not exhaustive runtime inventory | R0/R1 |
| E-023 | 2026-08-14 | Vertical | `functions/plugins/acf/acf.php`; `acf_custom_options_setup()` | Registers Site Settings plus capability-varied subpages; nine ACF JSON files with media definitions target option pages | Later Brand Control Center evidence; excluded from Media Manager R1 | R3-R5 |
| E-024 | 2026-08-14 | Vertical | exact search for `dbvc_visual_editor`, `DBVC_Visual_Editor`, `dbvc-ve` outside docs/artifacts | Only a backup export contains DBVC settings; no executable current integration hook found | DBVC ships independently; backup is not authority | R1 |
| E-025 | 2026-08-14 | LocalWP runtime | read-only `/private/tmp/dbvc_media_manager_r0_probe.php` against site ID `4gScrLykQ` | Exact runtime: 220 eligible published posts/CPTs, 234 public terms, 101 empty featured assignments, 48 active groups, 149 image/22 gallery runtime definitions, 2,119 applicable pairs, 612.62 ms enumeration/visibility baseline, no writes | Sizes bounded scan/read model; does not prove raw empty-scan/REST/UI performance | R0/R1 |
| E-026 | 2026-08-14 | ACF runtime source | `advanced-custom-fields`/bundled ACF `acf_get_field_group_visibility()` and `acf_match_location_rule()` | Visibility evaluates active groups and every registered location rule against a supplied screen; custom theme rules participate through filters | Use as exact applicability primitive with runtime definitions and explicit screen args | R1 |
| E-027 | 2026-08-14 | DBVC/package validation | focused PHPUnit; `PACKAGE-CONTENTS.sha256`; manifest validator | Visual Editor instrumentation: 7 tests/15 assertions passed; every checksum passed; 45 manifest entries had zero byte/hash mismatches | R0 documentation is internally consistent; this is not Media Manager feature coverage | R0 |
| E-028 | 2026-08-14 | DBVC | branch `codex/visual-editor-linked-posts-plan`; commit `5db4b4094c0d834b3cf482adb095273387b59dc8`; Git status/divergence | Checkout was clean; HEAD and origin matched with zero divergence when repository reconciliation began | Current package/canonical planning checkpoint; recheck for newer work before implementation | R0/All |
| E-029 | 2026-08-14 | DBVC validation baseline | user-supplied full-suite/lint result; focused R0 PHPUnit result | Full PHP suite has 6 deterministic failures out of 684 tests; full JavaScript lint did not complete; focused Visual Editor instrumentation passed 7 tests/15 assertions | Do not claim a green repository baseline; future slices must use focused checks and distinguish regressions | R0/All |
| E-030 | 2026-08-15 | DBVC | `addons/visual-editor/src/MediaManager/`; `bootstrap.php`; `CapabilityManager`; `tests/phpunit/VisualEditorMediaManagerR1ATest.php` | R1-A adds a default-off setting, published/public post and public-term owner policy with per-object capability/exclusion checks, exact ACF group visibility, request-local definition caching, unconditional direct/group-only media cataloging, unsupported-path counts, and invalid-nonempty value classification. Focused suite passed 5 tests/65 assertions; combined Visual Editor focus passed 12 tests/80 assertions; touched PHP syntax passed. | Policy/catalog gate is implemented without scanner, REST, UI, descriptor, or mutation authority; R1-B remains separately gated | R1 |
| E-031 | 2026-08-15 | DBVC | `vendor/bin/phpunit` | Full comparison ran 689 tests/7,186 assertions with six failures: `BricksAddonPhase11Test::test_i18n_strings_are_rendered_for_phase11_additions`, `BricksAddonPhase7Test::test_disabled_mode_regression_suppresses_submenu_routes_and_jobs`, `CapabilityLandscapeTest::test_prepared_records_identify_cli_and_parity_opportunities`, `ContentCollectorV2Phase29Test::test_phase_twenty_nine_resolved_conflicts_no_longer_block_package_preflight`, `ContentCollectorV2Phase32Test::test_phase_thirty_two_url_qa_reports_field_context_provider_risk_and_reviewed_ambiguity`, and `ContentMigrationPhase37W0SettingsTest::test_phase37_feature_flags_are_registered_with_safe_defaults`. These match the inherited six identities. | R1-A introduced no new PHP failure; the suite is still not green | R1/All |
| E-032 | 2026-08-15 | DBVC | `ScanCandidateProvider`; `MediaScanService`; `ScanSnapshotStore`; `MediaScanCoordinator`; `VisualEditorMediaManagerR1BTest` | R1-B implements bounded post/term source pages, supported raw-value reads, deterministic opaque findings and empty fingerprints, compressed user/blog-bound transients, latest generation, revision/idempotency, atomic short locks, expiry/cancel/retry/config invalidation, safe payload ceilings, summary counts, and performance metrics. Focused R1-B passed 5 tests/106 assertions; R1-A/R1-B passed 10/171; combined Visual Editor focus passed 17/186. | Scanner/snapshot gate is implemented without REST, public read model, row hydration, descriptor, UI, or mutation authority; R1-C remains separately gated | R1 |
| E-033 | 2026-08-15 | DBVC isolated PHPUnit performance fixture | temporary 20-entity R1-B probe against the configured test database | One 20-entity chunk produced 60 findings in 4.661 ms with 24 WordPress queries, zero additional allocated/peak memory pages at PHP's reported granularity, and a 4,983-byte compressed 20-group snapshot | Bounded chunk instrumentation and compact storage are functioning at the default-size cohort; 100/500/2,000-object, REST payload, and frontend render measurements remain R1-E gates | R1 |
| E-034 | 2026-08-15 | DBVC | `vendor/bin/phpunit --do-not-cache-result`; isolated and class reruns; `composer agent-docs:check` | The first full run reported the six inherited failures plus `ContentMigrationPhase4ImportExecutorTest::test_import_executor_execute_is_idempotent_across_reruns`. That extra test passed alone (1/34) and with its complete class (32/568); a clean full rerun produced 694 tests/7,302 assertions with exactly the inherited six failures. Agent docs passed with 54 records/408 surfaces/0 unmapped. | No reproducible new PHP failure was introduced by R1-B; the repository suite remains non-green and the transient extra result is recorded rather than hidden | R1/All |
| E-035 | 2026-08-15 | DBVC | `MediaScanReadModel`; `MediaManagerController`; `Routes`; `Addon`; `VisualEditorMediaManagerR1CTest` | R1-C adds seven feature/base-capability/active-mode-protected REST routes, bounded safe list projection, current list-time permission/status checks, user/site/generation/revision isolation, and one-owner row rescanning. Deep payload assertions exclude internal owner/field/path/fingerprint data; focused R1-C passed 6 tests/417 assertions and combined current Visual Editor focus passed 23/603. | The read/list/row gate is implemented without frontend UI, descriptors, Media Library actions, journal/cache work, or content mutation; R1-D remains separately gated | R1 |
| E-036 | 2026-08-15 | DBVC | `vendor/bin/phpunit --do-not-cache-result` | The R1-C full comparison ran 700 tests/7,723 assertions with exactly the six inherited failure identities: Bricks Phase 11 i18n, Bricks Phase 7 disabled mode, Capability Landscape prepared records, Content Collector Phase 29 conflicts, Content Collector Phase 32 URL QA, and Content Migration Phase 37 settings defaults. | R1-C introduced no new PHP failure; the repository suite remains non-green and the incomplete full-JavaScript-lint baseline remains unchanged | R1/All |
| E-037 | 2026-08-15 | DBVC | `composer agent-docs:refresh`; `composer agent-docs:check` | Generated references include the seven R1-C REST registrations; strict check passed with 54 curated records, 415 discovered surfaces, and zero unmapped | The public REST/add-on/safety reference is synchronized with R1-C | R1/All |
| E-038 | 2026-08-15 | DBVC/package | `MEDIA-MANAGER-CLAUDE-MOCKUP-HANDOFF.md`; minimal Claude prompt; `media-manager-r1c-view-model.json` | R1-D authorization is translated into a progressive context contract: one launcher, one capsule, one current read-only fixture, targeted CSS/image evidence, and separate D1-D4 review stops. The older future-R2 fixture is no longer the default R1 source. | Claude can begin D1 with substantially less duplicated prompt context; no static or production UI is claimed | R1 |
| E-039 | 2026-08-15 | DBVC/mockup | `docs/ui-mockups/dbvc-visual-editor/r1-media-manager/` | Claude delivered 15 read-only D1-D4 files: default and 11-state HTML views, scoped CSS, local-only interaction JS, five screenshots, component notes, wiring schematic, decision record, and handoff aids. Claude reported 30/30 structural checks, passing `node --check`, and no horizontal overflow at 1440/900/390 widths. | Static visual direction exists and preserves the R1 read-only boundary; it is not production integration, accessibility proof, cross-browser evidence, or real-device QA | R1 |
| E-040 | 2026-08-15 | DBVC source/browser review | `MediaScanReadModel`; `MediaManagerController`; `MediaScanCoordinator`; R1-C fixture; mockup `styles.css`/`mockup.js`/wiring docs | Source reconciliation confirmed alphabetical `entity_asc`, false `hasMore` when six rows fit under limit 20, valid `missing_desc`/`scanned_desc`, fingerprint-based expansion freshness, and safe field projections for changed/resolved/provider-unavailable rows. It also identified backend state names and distinct group-404 semantics. Browser interaction checks passed search, expansion, close/reopen, and desktop geometry, but 390x844 measured drawer client height 738 versus scroll height 1264 with `overflow-y: hidden`; sort, legend, results, and footer were clipped. The local script also announced final revalidation before a pending placeholder and used plural copy for one row. | Approve the visual direction with required D4A fixture/state/responsive/interaction corrections before production translation; accessibility, AT, cross-browser, real-device, and frontend integration gates remain open | R1 |
| E-041 | 2026-08-16 | DBVC/mockup D4A handback | Corrected R1-C fixture; `docs/ui-mockups/dbvc-visual-editor/r1-media-manager/`; Claude D4A-1/D4A-2/D4A-3 reports; Codex reconciliation | D4A produced a contract-valid fixture and four expansion projections, explicit backend/view-state and request-error mapping, a single reachable short/mobile drawer scroll body, corrected pending/final announcements and count grammar, 44px narrow/short targets, 13 state cases, and seven refreshed true-viewport screenshots. Structural checks passed 30/30, `node --check` and package 46/46 integrity passed. axe-core 4.11.0 on `index.html` returned 33 passing rule groups and one serious `color-contrast` violation affecting 12 confirmed nodes, all traced to the shared production subtle-text token; additional contrast nodes remained incomplete | D4A is accepted as qualified static direction. Production integration, assistive technology, Safari/Firefox, real-device behavior, `states.html` axe coverage, and the shared-token correction remain separate evidence gates | R1 |
| E-042 | 2026-08-16 | DBVC R1-D production shell and contrast checkpoint | `AssetLoader`; `overlay-app.js`; `media-manager-app.js`; `media-manager.css`; `overlay.css`; `VisualEditorMediaManagerR1DTest`; refreshed mockup | The default-off feature now conditionally enqueues a separate Media Manager shell module inside active Visual Editor mode. Its toolbar entry replaces the disabled overflow placeholder at the gate, and the trigger exposes dialog relationships; open/close, Escape, `aria-expanded`, and trigger-focus restoration are module-owned. Focused PHP coverage passed 4 tests/32 assertions including disabled and Builder asset gates. A same-checkout isolated Chromium harness passed toolbar/shell geometry and focus at 1440x900, 1440x600, 390x844, and 320x568 with no horizontal overflow and zero axe violations in the shell. The full PHP comparison ran 704 tests/7,755 assertions with exactly the inherited six failures; repository `npm run lint` completed with stale browser-data warnings, and direct Node syntax checks passed for the raw Visual Editor assets outside that lint target. Agent docs passed 54 records/415 discovered surfaces/0 unmapped. The shared subtle token was moved to the 60% mix; post-correction axe returned 33 passes/0 violations/38 incomplete for `index.html` and 22 passes/0 violations/0 incomplete for `states.html`; seven screenshots were regenerated | This slice makes no scan/list request, table projection, descriptor, Media Library call, content mutation, journal write, or cache invalidation. The browser harness is not authenticated WordPress runtime, AT, cross-browser, or real-device evidence | R1 |
| E-043 | 2026-08-16 | DBVC R1-D production API/state checkpoint | `api-client.js`; `media-manager-app.js`; `media-manager.css`; `AssetLoader`; `VisualEditorMediaManagerR1DTest`; `tests/visual-editor-media-manager-state.test.cjs`; `composer agent-docs:check` | The API client wraps latest/start/list/next/retry/cancel with nonce and generation/revision identity. The separate state controller maps all five backend states plus no-scan/request-error/stale outcomes, stores copied safe list/query/pagination state, suppresses out-of-order responses, drives action visibility from authoritative state and `canRetry`/`canCancel`, and renders no result rows. Five jsdom state/route tests pass. Isolated Chromium at 1440x900 and 1280x720 passed latest 404, start, next, list identity/query, no-row DOM, geometry, Escape/focus, and shell axe with 13 passes/0 violations at each viewport. The full PHP comparison ran 704 tests/7,755 assertions with exactly the same six inherited failures. Repository lint including all touched Visual Editor JavaScript and the focused state test completed with stale browser-data warnings only. Agent docs passed 54 records/415 discovered surfaces/0 unmapped | Browser evidence uses mocked protected responses and is not authenticated WordPress runtime. Result-table/row expansion, descriptors, Media Library actions, content mutation, journal/cache effects, and additional responsive/mobile work remain absent | R1 |
| E-044 | 2026-08-16 | DBVC R1-D production table checkpoint | `media-manager-app.js`; `media-manager.css`; `AssetLoader`; `VisualEditorMediaManagerR1DTest`; `tests/visual-editor-media-manager-state.test.cjs`; `tests/playwright/visual-editor-media-manager-table.spec.js` | The frontend renders only normalized safe R1-C rows in a semantic internally scrollable table. Search/entity/field/sort remain server-driven; first-page replacement and opaque-cursor append preserve query state, de-duplicate group references, and preserve append scroll. Eight jsdom tests cover safe collapsed DOM and unknown-key stripping, query replacement, all sorts, cursor append, empty/error states, route identity, stale suppression, and backend-state mapping. Four PHPUnit tests/37 assertions keep asset/Builder/read-only/static-contract gates. Two isolated Chromium tests pass at 1440x900 and 1280x720 with 15-to-25 row append, scroll retention, server replacement/ARIA sort, nonce/identity/query/cursor proof, no expansion control, bounded geometry, focus restoration, and axe WCAG A/AA zero violations. The full PHP comparison ran 704 tests/7,760 assertions with exactly the inherited six failures. Targeted `wp-scripts` lint passes for the new module/tests with stale browser-data warnings only; the aggregate repository command did not complete in a bounded rerun. | Mocked browser responses are not authenticated WordPress runtime. Lazy group expansion, descriptors, Media Library actions, mutation/journal/cache effects, large-scale profiling, AT/cross-browser evidence, and additional responsive/mobile work remain outside this slice | R1 |
| E-045 | 2026-08-16 | DBVC R1-D production row-expansion checkpoint | `api-client.js`; `media-manager-app.js`; `media-manager.css`; `AssetLoader`; `VisualEditorMediaManagerR1DTest`; `tests/visual-editor-media-manager-state.test.cjs`; `tests/playwright/visual-editor-media-manager-table.spec.js` | The frontend group client sends the current opaque scan/generation/revision identity and an allowlisted group reference. One-row lazy expansion has independent loading/error state, strips unknown keys, forces descriptor/media actions false, distinguishes group-request failure from successful provider-unavailable projections, and suppresses late responses after collapse or a newer row. Ten jsdom tests pass; focused R1-D PHP passes 4 tests/41 assertions; targeted and aggregate lint plus Node syntax pass with stale browser-data warnings only. Two isolated Chromium tests at 1440x900 and 1280x720 verify group identity, field statuses, ARIA/region/collapse behavior, no assignment action, retained paging/scroll/geometry/focus behavior, and axe WCAG A/AA zero violations in expanded and collapsed states. The full PHP comparison ran 704 tests/7,764 assertions with exactly the inherited six failure identities. | Browser responses are mocked. Authenticated WordPress runtime, assistive technology, cross-browser, large-site performance, and all descriptor/Media Library/mutation effects remain separate gates | R1 |
| E-046 | 2026-08-16 | DBVC initial R1-E keyboard/reduced-motion hardening | `media-manager-app.js`; `media-manager.css`; `VisualEditorMediaManagerR1DTest`; jsdom state test; permanent Playwright table spec | The table renderer now captures the active row disclosure through `body.ownerDocument.activeElement` and restores focus to the replacement safe group-ref button after loading/success/collapse rerenders with `preventScroll`. The disclosure indicator disables its transition under reduced-motion preference. Jsdom focus assertions pass; focused PHP passes 4 tests/43 assertions; targeted and aggregate lint pass with stale dependency-data warnings. Chromium at both supported viewports activates expansion with Enter, collapses with Space, retains row-button focus, verifies `transitionDuration=0s`, and retains expanded/collapsed axe zero violations. | Mocked responses are not authenticated WordPress runtime or screen-reader evidence. Safari/Firefox, assistive technology, large-site profiling, and current active-site integration remain open R1-E gates | R1/R1-E |
| E-047 | 2026-08-16 | DBVC R1-E synthetic scale/no-auto-scan hardening | `VisualEditorMediaManagerR1ETest`; focused and combined R1-A-R1-E PHPUnit | Ordinary frontend Media Manager asset enqueue leaves the current user without a latest scan. Synthetic 100/500/2,000-group compressed snapshots remain below the 5 MiB ceiling and return only a 50-row page. Focused R1-E passes 2 tests/50 assertions; combined R1-A-R1-E passes 22/681. In the combined 2,000-group run: 25.425 ms, 0 additional WordPress queries, 6,291,456 allocated-memory bytes, 120,475 stored bytes, and 24,833 response bytes | Synthetic groups share one eligible owner to isolate snapshot/list/payload cost. Complete candidate traversal/raw ACF reads, authenticated REST/browser transport, AT, and cross-browser proof remain open | R1/R1-E |
| E-048 | 2026-08-16 | DBVC full PHP comparison after R1-E scale coverage | `vendor/bin/phpunit -c phpunit.xml.dist --do-not-cache-result` | Full suite ran 706 tests/7,816 assertions with six failures. The identities remain Bricks Phase 11 i18n, Bricks Phase 7 disabled mode, Capability Landscape prepared records, Content Collector Phase 29 conflict state, Content Collector Phase 32 URL QA state, and Content Migration Phase 37 settings defaults | No new PHP failure was introduced; the repository suite remains non-green | R1/All |
| E-049 | 2026-08-16 | DBVC R1-E automated semantic/fallback hardening | `media-manager-app.js`; `AssetLoader`; R1-D PHP/jsdom/Playwright coverage; full PHP comparison | The dialog now references its visible description; results and the focusable table scroller have stable headings/names; list/group loading exposes status/live/atomic and busy state; every expansion presentation retains an entity-specific heading; and the stable polite live region announces field-check start/completion/failure. Eleven jsdom tests, 4 R1-D PHP tests/47 assertions, targeted lint, and 6 Chromium/Firefox/WebKit viewport cases pass while expanded/collapsed axe stays at zero confirmed violations. Combined R1-A through R1-E passes 22/685; full PHP runs 706/7,820 with the same six failures. The latest aggregate lint rerun did not complete | Automated semantics/browser engines do not prove real screen-reader or real Safari behavior. Default-off/Builder/no-auto-scan/no-migration fallback is retained; authenticated runtime, AT, real Safari, complete traversal/raw reads, and aggregate lint completion remain open | R1/R1-E |
| E-050 | 2026-08-16 | LocalWP runtime / DBVC | read-only `wp_options` SELECT on site `4gScrLykQ` (`dbvc-codexchanges.local`); unauthenticated `curl` of `dbvc/v1` REST index and Media Manager routes | Runtime provenance rechecked read-only: active site `dbvc-codexchanges.local`, `template=bricks`/`stylesheet=vertical`, DBVC plugin active, `siteurl=https://dbvc-codexchanges.local`, MySQL running. `dbvc_visual_editor_media_manager_enabled=1`. The persistent `dbvc_addon_visual_editor_enabled` is now `1` (ON) — a drift from the previously recorded OFF state; no option/login/content/LocalWP state was changed. All seven live Media Manager REST routes are registered; unauthenticated GET `.../scans/latest`, a tampered `.../scans/{ref}` list, a tampered group route, and POST `.../scans` each return HTTP 401 `rest_forbidden` with no data and no created snapshot | The live REST surface and its permission gate are confirmed against real WordPress and enforce authentication before resource resolution. Authenticated active-site REST/table data behavior remains unproven because no already-authorized session is available. The VE-option drift is recorded, not reverted | R1/R1-E |
| E-051 | 2026-08-16 | DBVC isolated PHPUnit fixture | `tests/phpunit/VisualEditorMediaManagerR1ETest.php::test_complete_candidate_traversal_and_raw_reads_scale_without_field_definition_n_plus_one` | A new deterministic, non-mutating test drives the real `ScanCandidateProvider`->`MediaScanService`->`ScanSnapshotStore` pipeline to completion across 100 and 300 live owners at chunk size 50. Every owner is enumerated exactly once and raw-read into three empty findings (featured + ACF image + ACF gallery); per-owner raw ACF reads are constant at 2 and applicability is evaluated exactly once per candidate. Per-candidate DB cost falls from ~1.25 to ~0.83 queries as owners triple (total ~125->~249), max candidates/chunk = 50, max source queries/chunk = 1, wall 41-219 ms, snapshot 15.3-41.0 KB. Focused R1-A-R1-E now passes 23 tests/1,127 assertions | Complete multi-owner candidate traversal and raw ACF read scaling is now measured against live fixtures with no field-definition/capability/permalink N+1 (per-candidate cost decreases with scale). This complements the synthetic 100/500/2,000-group snapshot/read/payload evidence. Authenticated REST/browser transport, real AT, and real Safari remain open | R1/R1-E |
| E-052 | 2026-08-16 | DBVC | `vendor/bin/phpunit -c phpunit.xml.dist --do-not-cache-result`; `npm run lint` | Full suite ran 707 tests/8,262 assertions with exactly the six inherited failure identities (Bricks Phase 11 i18n, Bricks Phase 7 disabled mode, Capability Landscape prepared records, Content Collector Phase 29 conflicts, Content Collector Phase 32 URL QA, Content Migration Phase 37 settings defaults). The +1 test/+442 assertions over the prior 706/7,820 are the new candidate-traversal test. One bounded aggregate `npm run lint` attempt ran ~11 minutes without completing and was stopped; targeted Media Manager lint passes with stale Baseline/Browserslist warnings only | The new coverage introduces no PHP regression; the repository suite remains non-green on the same six inherited identities. Aggregate repository JavaScript lint still has no completed result and remains a recorded caveat, not a pass | R1/All |
| E-053 | 2026-08-16 | DBVC | `addons/visual-editor/src/MediaManager/MediaFindingDescriptorBridge.php`; `EditableRegistry::persistDetachedDescriptor()`; `MediaManagerController` finding-descriptor route; `Routes`/`Addon` wiring; `VisualEditorMediaManagerR2ATest` | R2-A adds a server-authoritative bridge that exchanges one opaque finding for one fresh standard `EditableDescriptor`. It loads the user/site-bound snapshot, validates generation/revision, resolves the owner/field only from the snapshot group/finding, rechecks owner eligibility/status/capability, rescans the single owner to reconfirm applicability and the current empty value by fingerprint, then mints a descriptor for the correct family (`post_featured_image`/`acf_image`/`acf_gallery`) routing to exactly one existing resolver, persisted via the narrow `persistDetachedDescriptor()`. The response exposes only opaque token/session ids plus safe labels/status; no owner id, field key/name/selector, ACF object id, path, or fingerprint leaks. It opens no media frame, hydrates no value, and mutates nothing. Focused R2-A passes 11 tests/200 assertions; combined R1-A-R1-E plus R2-A passes 34/1,327 | The finding->fresh-descriptor bridge is implemented with no client-supplied target authority and fails closed on tamper/expiry/stale/format/changed/permission cases; Media Library selection, upload, and save remain R2-B/R2-C | R2/R2-A |
| E-054 | 2026-08-16 | DBVC | `vendor/bin/phpunit -c phpunit.xml.dist --do-not-cache-result`; `composer agent-docs:refresh`/`check` | Full suite ran 718 tests/8,462 assertions with exactly the same six inherited failure identities; the +11 tests/+200 assertions over 707/8,262 are R2-A. Agent docs were refreshed for the new `.../findings/{finding_ref}/descriptor` REST registration and pass 54 curated records, 416 discovered surfaces (+1), and 0 unmapped; the shifted `dbvc_visual_editor_session_descriptor_compression_level` hook and the eight Media Manager routes were remapped in `manifest.json` | R2-A introduces no PHP regression; the new public REST route is registered and covered in the agent reference. The repository suite remains non-green on the same six inherited identities | R2/All |

| E-055 | 2026-08-16 | DBVC | `media-manager-app.js` (assign controls + wp.media staging); `api-client.js` `mediaManager.descriptor`; `AssetLoader.php` R2-B strings; `media-manager.css`; `visual-editor-media-manager-state.test.cjs`; `VisualEditorMediaManagerR1DTest` | R2-B renders a capability-gated `assign-media` control for each field still `missing`, calls the R2-A bridge on activation, and on a `writable` descriptor opens the native `wp.media` frame (single image / multiple gallery) reusing the same standard config as `overlay-app.js`. Selections stage client-side with an `Unsaved selection` badge, thumbnail preview, and `Replace`/`Clear`; non-writable descriptors surface a notice and never open the frame; clearing and collapse discard the selection. No save, mutation, journal, or cache path is touched, and the descriptor token/session id and all raw targets stay out of the DOM. jsdom passes 16 tests (11 prior + 5 R2-B); targeted `lint:visual-editor-media-manager` is clean; R1-D read-only + R2-A focused pass 15 tests/252 assertions. `overlay-app.js` is unchanged | The loaded table can now choose an existing image or upload a new one for a missing field, staged unsaved, with no content write; field save and no-reload reconciliation remain R2-C | R2/R2-B |
| E-056 | 2026-08-16 | DBVC | `vendor/bin/phpunit -c phpunit.xml.dist --do-not-cache-result`; `composer agent-docs:refresh`/`check` | Full suite ran 718 tests/8,467 assertions with exactly the same six inherited failure identities; the +5 assertions over 8,462 are the R1-D read-only invariant updated to allow staged, unsaved `wp.media` (no new test, no new regression). Agent docs were refreshed for the AssetLoader localized-string change (discovery fingerprint only) and pass 54 curated records, 416 discovered surfaces, and 0 unmapped; R2-B adds no new REST/hook/setting surface | R2-B introduces no PHP regression and no new agent-reference surface; the repository suite remains non-green on the same six inherited identities | R2/All |

| E-057 | 2026-08-16 | DBVC | `MediaAssignmentService`; `MediaFindingDescriptorBridge::resolveFinding`; `MediaManagerController` assignment route; `media-manager-app.js` save/reconcile; `api-client.js` `mediaManager.assign`; `AssetLoader.php` R2-C strings; `media-manager.css`; `VisualEditorMediaManagerR2CTest`; `visual-editor-media-manager-state.test.cjs` | R2-C saves the staged selection through `POST .../findings/{finding}/assignment`. The service re-runs the R2-A revalidation as the expected-empty precondition, mutates through the shared `MutationService` (resolver save, journal/audit, cache invalidation), and rereads via `expandGroup` to reconcile. `VisualEditorMediaManagerR2CTest` (7 tests/81 assertions) proves featured-image/ACF-image/ACF-gallery saves persist and reconcile, a field populated after scan is blocked with `409 media_assignment_stale` and not overwritten, non-image and empty selections are rejected without a write, and a stale generation is blocked. jsdom adds 3 R2-C cases (19 total): save reconciliation with no list/scan reload, a conflict that keeps the staged selection and shows a notice, and the in-progress saving state. R1-D read-only invariant updated (gated save via the dedicated endpoint only; still no direct fetch/generic/composite save); combined Media Manager PHP 41 tests/1,413 assertions; targeted lint clean | Field-level assignment is implemented with the expected-empty gate enforced and proven, reusing the single audited mutation pipeline and reconciling without a table reload; only real-browser save QA remains the residual gate | R2/R2-C |
| E-058 | 2026-08-16 | DBVC | `vendor/bin/phpunit -c phpunit.xml.dist --do-not-cache-result`; `composer agent-docs:refresh`/`check` | Full suite ran 725 tests/8,550 assertions with exactly the same six inherited failure identities; the +7 tests/+83 assertions over 718/8,467 are R2-C plus the R1-D invariant update. Agent docs were refreshed for the new `.../assignment` REST registration and the AssetLoader string change and pass 54 curated records, 417 discovered surfaces (+1), and 0 unmapped; the nine Media Manager routes were remapped in `manifest.json` | R2-C introduces no PHP regression; the new content-mutating route is registered and covered in the agent reference | R2/All |

| E-059 | 2026-08-16 | DBVC | `MediaFindingDescriptorBridge`; `EditableRegistry`; `MediaAssignmentService`; `media-manager-app.js`; `VisualEditorMediaManagerR2ATest`/`R2CTest`; `visual-editor-media-manager-state.test.cjs` | Applied three R2 self-review fixes. CR-1: removed the unused descriptor persistence and `{token, sessionId}` round-trip — the bridge returns only `{input, family, expectedState}`, `EditableRegistry::persistDetachedDescriptor` was deleted, and the client staged selection keeps no token/session; the R2-A test now inspects the minted descriptor via `resolveFinding` and asserts the response has no token, and cross-user binding is proven at the snapshot layer. CR-4: capped gallery assignment cardinality (200) server-side with a rejection test. CR-5: `reconcileAfterSave` now patches only the affected collapsed row, expanded panel, and summary instead of rebuilding the whole table. R2-A 11 tests/204 assertions; R2-C 8 tests/85 assertions; jsdom 19; combined Media Manager PHP 42/1,429; targeted lint clean; agent docs 54/417/0 | The R2 implementation is simpler (no dead persistence/token state), more robust (bounded gallery input), and cheaper to reconcile (targeted DOM patch), with coverage preserved | R2/Review |

| E-060 | 2026-08-16 | DBVC | `media-manager-app.js`; `AssetLoader.php` (`canUpload` + R2-D strings); `media-manager.css`; `visual-editor-media-manager-state.test.cjs`; mockup `COMPONENT-NOTES.md` §11 | R2-D adds verified UX states over R2-A/B/C with no new REST/mutation surface: `Opening Media Library…` while the descriptor request is in flight; unsaved image/gallery selection; an upload-unavailable hint driven by the new `canUpload` bootstrap flag; save-in-progress; a verified `Saved` field chip plus a `Resolved` row badge marked in place; a polite refresh notice for changed-since-scan; and an assertive `role="alert"` validation-error notice that retains the staged selection. jsdom passes 23 tests (4 new R2-D); the R1-D read-only invariant is intact; targeted lint clean | The nine R2-D states are implemented and jsdom-verified with correct ARIA (polite status vs assertive alert), distinct saved/resolved presentation, and honest upload gating; the mockup notes catalog them | R2/R2-D |
| E-061 | 2026-08-16 | DBVC | `vendor/bin/phpunit -c phpunit.xml.dist --do-not-cache-result`; `composer agent-docs:refresh`/`check` | Full suite ran 726 tests/8,564 assertions with exactly the same six inherited failure identities (R2-D added only jsdom coverage, no PHP tests). Agent docs were refreshed for the AssetLoader string/flag change and pass 54 curated records, 417 discovered surfaces, and 0 unmapped; R2-D adds no new REST/hook/setting surface | R2-D introduces no PHP regression and no new agent-reference surface | R2/All |

| E-062 | 2026-08-16 | DBVC | `VisualEditorMediaManagerR2ETest`; `dbvc_visual_editor_audit_event`/`dbvc_visual_editor_invalidate_cache` hooks; `{prefix}dbvc_ve_change_items` | R2-E1 journal/cache verification. A successful media assignment records a `completed` change-journal item (correct `resolver_name` and the new attachment value), fires the audit event exactly once with the empty old value, the new attachment, and the acting user, and fires cache invalidation exactly once for the correct post entity. A failed (non-image) save records a `failed` journal item, fires neither the audit nor the cache-invalidation hook, and leaves the featured image unset. An ACF image save records `resolver_name = acf_image` and invalidates the post cache. 3 tests/39 assertions; combined Media Manager PHP 45 tests/1,468 assertions | The R2-C save path's journaling, auditing, and cache invalidation are proven through the existing services, and a failed save records a failure rather than a phantom success or a stray invalidation | R2/R2-E1 |

| E-063 | 2026-08-16 | DBVC | `MediaScanService` (`$include_assigned`/`buildPreview`); `MediaScanReadModel` (`projectAssignedField`/dedup/`populated` count); `media-manager-app.js` (`normalizeFieldPreview`, `assigned` status); `AssetLoader.php`/`media-manager.css`; `VisualEditorMediaManagerR2FTest` | R2-F Slice 1 (populated-field inventory + bounded preview). Expanding an entity now lists populated fields (`status: assigned`) beside empty findings, each with a sanitized `preview` (thumbnail URL/alt/count); a field empty at scan and now populated is deduped to one entry (resolved, with the merged preview); the snapshot and top-level list/counts stay empty-focused; the preview leaks no field key/selector/owner id/ACF object id/path/raw value. Slice 1 is read-only (`replace: false`, no assign control on populated fields). `VisualEditorMediaManagerR2FTest` 3 tests/29 assertions; jsdom +1 (24 total); combined Media Manager PHP 48 tests/1,504 assertions; R1-D read-only invariant intact; full suite 732/8,639 with the same six inherited failures; agent docs 54/417/0 | The entity media inventory is reported safely (preview-only) without disturbing the top-level missing-media report; thumbnail presentation (Slice 2) and replace (Slice 3) remain | R2-F/Slice 1 |

| E-064 | 2026-08-17 | DBVC | `media-manager-app.js` (`createFieldThumbnail`, `createFieldProjection`); `media-manager.css` (`field-item`/`field-content`/`field-thumb`); `AssetLoader.php` (`mediaManagerThumbCount`); `visual-editor-media-manager-state.test.cjs` | R2-F Slice 2 (thumbnail presentation). Each field renders as `[thumbnail | content]`; `createFieldThumbnail` prefers a staged selection thumbnail, then the populated `preview.url`, then an accent-color placeholder wrap for empty fields, and adds a `+{count}` badge for galleries. Images set `loading="lazy"`/`decoding="async"` via attributes and render only inside the already-lazy detail panel (nothing fetched for collapsed rows). CSS gives the thumb a square `aspect-ratio: 1/1`, rounded, full field-item height, and a `color-mix` accent placeholder. jsdom `R2-F Slice 2` case asserts the thumb/content cells, lazy attributes, the placeholder for the empty field, and the gallery `+2` badge; media-manager lint clean; R1-D read-only invariant intact | The entity inventory now carries a left-aligned lazy thumbnail per field with a single accent placeholder swap point for a future default image, without eager loading or a mobile-specific change | R2-F/Slice 2 |

| E-082 | 2026-08-19 | DBVC | `MediaIndexJsonExporter` (new; `exportAll`/`importIfEmpty`); `MediaIndexBuilder` (`dbvc_visual_editor_media_index_build_completed` action on final chunk); `MediaIndexReconciler` (`dbvc_visual_editor_media_index_reconciled` action when a sweep touched rows); `Addon::register` wiring (subscribes exporter to both completion hooks + guarded import on bootstrap); `VisualEditorMediaIndexJsonExportTest` | R2-H Phase 1 Slice 5 — derived JSON mirror in the DBVC sync folder (backup portability). `MediaIndexJsonExporter::exportAll()` writes an envelope `{schema:1, exported_at, source, generation, count, entities}` to `{sync}/visual-editor/media-index.json` — only the SERVING generation is exported (rebuilds-in-flight are internal state), each entity carries identity + cached label/URL + counts + family breakdown + content_hash + indexed_at + is_dirty, and the envelope-level `generation` is authoritative (per-row `index_generation` column is omitted from exported entities). Writes honor `dbvc_is_safe_file_path` and `wp_mkdir_p` and no-op silently on any failure. `MediaIndexBuilder` fires `dbvc_visual_editor_media_index_build_completed($generation, $rebuild_active)` once per build (initial or rebuild swap); `MediaIndexReconciler` fires `dbvc_visual_editor_media_index_reconciled($processed)` when a sweep actually touched rows; `Addon::register` subscribes the exporter to both so the mirror refreshes at completion boundaries without per-invalidator churn. `importIfEmpty()` is the guarded restore path — when the table is empty AND the mirror file exists AND validates (schema match, well-formed `vmig_` generation, non-empty entities), it sets `OPTION_GENERATION` to the file's generation, upserts every row, and marks the builder state complete for that generation (so the scheduler drain does not re-fire immediately after a restore). A no-op when the table has any rows. Two new `do_action`s let integrators observe: `dbvc_visual_editor_media_index_exported($file_path, $count)` and `dbvc_visual_editor_media_index_imported($file_path, $imported)`. Verified (5 tests/41 assertions): export writes a well-formed envelope with only serving-gen rows (a stray non-serving row is filtered) and no per-row `index_generation`; import populates an empty table + marks the build state complete for the imported generation; import is a no-op when the table has rows; a round-trip (export → deleteAll → import) preserves identity + `entity_ref` (refs are HMAC-derived from identity + generation, which is preserved); export does not throw when the safety check filters an unsafe base. Combined Media Manager PHP 115 tests; full suite 799 with the same six inherited failures; jsdom 38; agent docs 54/431/0 (four new `do_action` extension points + two now-discovered option keys mapped). **R2-H Phase 1 complete** | The durable index is now backup-portable — the DBVC sync folder carries a self-describing JSON mirror that hydrates a restored site whose table is empty, closing the last Phase 1 gap in D-053 (custom table is source of truth; JSON is a derived, importable mirror) | R2-H/Slice 5 |

| E-081 | 2026-08-19 | DBVC | `MediaIndexStore` (schema v2, `hasActiveRebuild`/`buildingGeneration`/`activeBuildGeneration`/`beginRebuild`/`completeRebuild`/`abortRebuild`, gen-scoped `entityRowId`/`upsertEntity`, serving-preferred `getEntity`/`getByEntityRef`); `MediaIndexBuilder` (target `activeBuildGeneration`, call `completeRebuild` on drain finish); `MediaIndexInvalidator` (dual-write to serving + building; `onAttachmentDeleted` also marks building dirty); `MediaIndexRebuildController` (new, MM-gated); `Addon::register` wiring; `VisualEditorMediaIndexRebuildTest` | R2-H Phase 1 Slice 4b-2 — topology/exclusion rebuild triggers + atomic swap. `MediaIndexStore` splits "current generation" in two: `currentGeneration()` is the SERVING generation (unchanged for reads); the new `OPTION_BUILDING_GENERATION` holds the BUILDING generation while a rebuild is in flight. Schema v2 extends the unique key to `(entity_type, entity_id, entity_subtype, index_generation)` so a per-entity building row coexists with its serving counterpart mid-rebuild. `beginRebuild()` mints a fresh building generation (dropping any orphaned rows from a prior in-flight rebuild); `completeRebuild()` atomically swaps the serving pointer and prunes every other generation. `MediaIndexBuilder` targets `activeBuildGeneration()` so a rebuild writes into building without touching serving; when its final chunk completes AND a rebuild was in flight, it calls `completeRebuild()` — the swap and prune happen in one step. `MediaIndexInvalidator::reindexEntity` dual-writes into serving AND building during a rebuild so a mid-rebuild save survives the swap; `onAttachmentDeleted` flags both generations dirty for the post-swap reconcile. The new `MediaIndexRebuildController` (MM-gated in `Addon::register`) wires the trigger surface: `acf/update_field_group`, `acf/delete_field_group`, `acf/trash_field_group`, `acf/untrash_field_group`; `update_option_dbvc_visual_editor_excluded_post_types` / `_excluded_taxonomies` (plus add/delete siblings); and a `wp_loaded` (priority 20) topology fingerprint over the sorted set of public+show-UI post types and taxonomies plus current exclusion lists — a drift triggers a rebuild, catching post-type/taxonomy (de)registration without firing on every request. Triggers are non-clobbering: a call while a rebuild is already running is a no-op, and `dbvc_visual_editor_media_index_rebuild_skipped` fires so integrators can observe missed events; `dbvc_visual_editor_media_index_rebuild_started` fires when a fresh rebuild begins. Verified (`VisualEditorMediaIndexRebuildTest`, 4 tests/40 assertions): a rebuild keeps serving the OLD generation until completion (mid-drain reads still return the complete list, and building rows coexist without clobbering serving); each trigger surface begins a rebuild while a concurrent trigger is a no-op; the topology fingerprint primes silently on first call, is stable when nothing changes, drifts on post-type registration → rebuild, and returns to idle after the drain; and the invalidator dual-writes into both generations so a mid-rebuild save survives the swap. Combined Media Manager PHP 110 tests; full suite 794 with the same six inherited failures (a seventh, `ContentCollectorV2Phase8Test`, is order-dependent flaky in mixed runs and passes in isolation — NOT a regression); jsdom 38; agent docs 54/425/0 (two new `do_action` extension points mapped) | Any topology/exclusion change now atomically rebuilds the durable index without ever letting reads observe a half-built copy — reads keep serving the complete OLD generation until the fresh one is fully built, then the pointer swaps and the old is pruned; in-flight edits during the rebuild are captured by the dual-write invalidator so they survive the swap | R2-H/Slice 4b-2 |

| E-080 | 2026-08-19 | DBVC | `EligibilityPolicy` (`$structural` mode); `MediaIndexBuilder` (new); `MediaIndexScheduler` (`BUILD_HOOK` drain); `MediaIndexProjector::onScanRefreshed`/`refreshFromSnapshot`; `Addon` (structural pipeline + hook rewire); `VisualEditorMediaIndexBuilderTest` | R2-H Phase 1 Slice 4b-1 (structural cross-user first-run build). Fixes a correctness gap: the Slice 2 completion hook only populated the index from ONE user's on-demand scan, so the shared, cross-user index held only that user's editable set. `EligibilityPolicy` gains a `$structural` mode that skips **only** the per-object capability check (status/public/show-UI/exclusions preserved); the read path keeps the capability-enforcing policy, so the D-053 read-time-authority contract is intact. `MediaIndexBuilder` runs its own structural pipeline to enumerate every structurally-eligible entity via `ScanCandidateProvider` in bounded chunks — scanning each with an ad-hoc `vmsg_` scan generation and upserting rows under the `vmig_` index generation — persisting a `{generation, cursor, status}` build-state option across runs and marking complete when the cursor is exhausted. `MediaIndexScheduler` drains the build as a self-continuing chain (Action Scheduler async action when available, else WP-Cron single-event re-armed each incomplete chunk via `BUILD_HOOK`), stopping on completion. Because the builder is now authoritative, the manual-scan completion hook is rewired `onScanCompleted` (rotate fresh capability-limited generation + prune) → `onScanRefreshed`/`refreshFromSnapshot` (upsert scanned entities into the current generation, no rotate/prune) so an on-demand scan refreshes-in-place without clobbering the cross-user index. First build fills the current empty generation directly; atomic build-into-fresh-generation-then-swap for topology/exclusion rebuilds is Slice 4b-2. Verified (5 tests/26 assertions): a structural build indexes posts the acting subscriber cannot edit while a capability-scoped build indexes none (proving cross-user structural enumeration); the build advances across multiple small chunks with the cursor persisted and completes (all entities indexed); the `needsBuild`/`reset` lifecycle; a manual-scan refresh leaves the generation and an untouched entity's row intact (no clobber); the scheduler arms the WP-Cron drain, drives it to completion, and then stops. Combined Media Manager PHP 106 tests; full suite 790 with the same six inherited failures; agent docs 54/423/0 (the build hook is a cron listener, not a discovered do_action surface). Remaining: Slice 4b-2 (rebuild triggers), Slice 5 (JSON export) | The durable index now builds itself automatically on first run and is complete for ALL users (not just the first scanner), while a manual scan refreshes it without clobbering — closing the cross-user population gap | R2-H/Slice 4b-1 |

| E-079 | 2026-08-19 | DBVC | `media-manager-app.js` (index source: `source`/`expansion.itemKey`/`expansion.scan`, `loadIndex`, `commitIndexPayload`, index `expandGroup`, `openInitialSource`); `api-client.js` (`index`/`indexExpand`); `AssetLoader` (`indexList` flag + strings); `VisualEditorMediaManagerR1DTest`; `visual-editor-media-manager-state.test.cjs` (+4 index cases) | R2-H Phase 1 Slice 2c (frontend flip). Gated behind a filterable bootstrap flag `mediaManager.indexList` (`dbvc_visual_editor_media_index_list_enabled`, default = Media-Manager-enabled; **absent in existing fixtures so the scan path is unchanged**). When on, the Manager **opens from `GET .../index`** (instant, read-time per-user filtered) rendering `vemx_`-keyed rows, and **expands via `POST .../index/expand`**: the response's detached snapshot becomes the per-expansion working identity (`expansion.scan` + working `vemg_` group) while the list row stays keyed by the opaque `vemx_` ref, so the existing descriptor/assign/replace flow drives mutation **unchanged** and a save reconciles the row in place with no list reload. **Automatic fallback to the ephemeral scan** when the index request fails or returns no rows (index not yet built), and "Start new scan" returns to the scan source. Search/entity/field/sort + offset paging route through the index in index mode. Verified (jsdom): index open sets `source='index'` and never calls `/scans`; index-expand adopts the detached snapshot (itemKey `vemx_`, working group `vemg_`) and a subsequent save **posts to the detached scan ref** and reconciles the `vemx_` row in place without an index reload; index error → scan fallback (`/scans/latest`); empty index → scan fallback. jsdom 38/38 (34 scan-mode cases unchanged); media-manager lint clean; R1-D read-only invariant updated for the `expandGroup` rename and passing; combined Media Manager PHP 101 tests; full suite 785 with the same six inherited failures; agent docs 54/423/0 (new filter hook mapped). Remaining: Slice 4b (first-run build), Slice 5 (JSON export) | The Media Manager opens instantly from the durable, per-user-filtered index and remediates in place through a detached snapshot, with a safe automatic fallback to the scan — the user-visible payoff of the R2-H plumbing | R2-H/Slice 2c |

| E-078 | 2026-08-19 | DBVC | `MediaIndexStore` (`buildFilter` search/entity-family, `orderByClause`); `MediaIndexReadModel::getList` (query normalize + `fieldFamily` + echo); `MediaIndexController::handleIndexList`; `VisualEditorMediaIndexQueryTest` | R2-H Phase 1 Slice 2c (server — index list query parity, behavior-neutral). The durable index list now honors the **same query surface as the ephemeral scan list** so the Slice 2c flip loses no filter control: `search` (label `LIKE`, `esc_like`-escaped) and `entityFamily` (`entity_type` = post/term) filter in SQL; `sort` maps to a whitelisted `ORDER BY` (entity/missing/scanned asc·desc); `fieldFamily` is applied after hydration against the per-family counts; and `getList` echoes the normalized query for control sync. Read-time per-user eligibility filtering is unchanged and still authoritative. No UI wiring yet (the flip is E-079). Verified: search matches only the label substring; entityFamily separates posts and terms; fieldFamily returns only the matching-family rows; entity/missing sorts order correctly; and a bogus sort/family/whitespaced search is normalized in the echo. 5 tests/14 assertions; full suite 785 with the same six inherited failures; agent docs 54/422/0 | The durable index is filterable/sortable/searchable exactly like the scan list, so flipping the Manager's source will not regress any filter control | R2-H/Slice 2c |

| E-077 | 2026-08-18 | DBVC | `MediaIndexStore` (`entity_ref`/`getByEntityRef`); `MediaScanCoordinator::snapshotEntity`; `ScanSnapshotStore::createDetached`; `Rest/Controllers/MediaIndexController` (+ `Routes` wiring); `VisualEditorMediaIndexControllerTest` | R2-H Phase 1 Slice 2b — index-backed read plumbing (server only). `MediaIndexStore` now derives a stable opaque `entity_ref` (`vemx_[a-f0-9]{24}` = HMAC of the entity identity under the current index generation) at upsert time and exposes `getByEntityRef` (a forged/stale ref resolves to nothing). `MediaScanCoordinator::snapshotEntity` builds a **complete, detached** single-entity snapshot via `ScanSnapshotStore::createDetached` — user/blog-bound and loadable by its `scan_ref`, but it never updates the user's "latest scan" pointer, so it cannot disturb a real full scan. The new MM-gated `MediaIndexController` adds `GET .../media-manager/index` (read-time-filtered list) and `POST .../media-manager/index/expand`, which resolves an opaque ref, **re-checks `EligibilityPolicy` for the requesting user**, builds the detached snapshot, runs the existing `expandGroup`, and returns the live detail plus the snapshot's `vems_` scan/group refs so the existing assign/replace routes drive mutation unchanged. No UI behavior changes — the frontend still uses the ephemeral snapshot; the flip is Slice 2c. Verified: the ref is opaque and store-only-resolvable (forged ref → null); index-expand returns live `fields` with a usable `vems_` scan ref **without clobbering `loadLatest`**; a forged ref → 404 `media_index_ref_unavailable`; an entity the user can no longer view → 403 `media_index_forbidden`; the list returns the filtered index. 5 tests/40 assertions; full suite 780 with the same six inherited failures; agent docs 54/422/0 (two new index routes mapped) | The durable index is safely readable end-to-end through REST — an opaque, per-user-eligibility-checked ref expands to live detail via a detached snapshot that never disturbs the user's full scan — with the user-visible flip deferred to Slice 2c | R2-H/Slice 2b |

| E-076 | 2026-08-18 | DBVC | `MediaIndexReconciler`; `MediaIndexScheduler`; `MediaIndexInvalidator::onAttachmentDeleted`; `Addon::register` wiring (MM-gated); `VisualEditorMediaIndexReconcileTest` | R2-H Phase 1 Slice 4 — background reconcile + scheduler. `MediaIndexReconciler::run()` re-indexes a bounded chunk of dirty entities via the invalidator's single-entity rescan (each upserted afresh, clearing dirty, or removed if no longer indexable). `onAttachmentDeleted` (hooked `delete_attachment`) flags the whole current generation dirty (an attachment deletion can empty any referencing field; a targeted reverse lookup is deferred), so the reconcile recomputes affected rows over time. `MediaIndexScheduler` registers the reconcile callback and schedules the recurring job via **Action Scheduler when available, else stock WP-Cron** — proven by the test using the WP-Cron fallback (AS absent in the env). Verified: reconcile re-indexes dirty + clears the flag; reconcile removes a dirty entity that lost eligibility; attachment deletion flags the generation dirty; the scheduler registers and clears the recurring job. 4 tests/14 assertions; full suite 775/8,935 with the same six inherited failures; agent docs 54/420/0. Deferred to Slice 4b: automatic first-run chunked full build + ACF-topology/exclusion rebuild triggers | The durable index self-heals in the background — dirty entities are reconciled on a schedule and attachment deletions are swept — on any site via Action Scheduler or WP-Cron | R2-H/Slice 4 |

| E-075 | 2026-08-18 | DBVC | `MediaIndexInvalidator`; `MediaIndexProjector::indexGroup` (extracted); `MediaIndexStore::deleteEntityById`; `Addon::register` wiring (MM-gated); `VisualEditorMediaIndexInvalidatorTest` | R2-H Phase 1 Slice 3 — incremental invalidation. `MediaIndexInvalidator` subscribes to `save_post`/`trashed_post`/`deleted_post`/`edited_term`/`delete_term` and keeps just the affected entity current between full scans: on a save/term edit it runs a bounded single-entity rescan and upserts the row into the current index generation (via the extracted `MediaIndexProjector::indexGroup`); an entity no longer indexable (unpublished, excluded, deleted, or without supported media fields) has its row removed (`deleteEntityById` handles the hard-delete path where the subtype is gone). Autosaves/revisions skipped. Index membership uses the same eligibility as the building scan; the per-user read-time filter still applies on read. Verified: re-index lowers the missing count when a field is filled (2→1) and changes the content hash; `save_post` re-indexes; an unpublished entity is removed; a deleted post's row is removed; a term edit re-indexes. 5 tests/13 assertions; full suite 771/8,921 with the same six inherited failures; agent docs 54/420/0. Deferred to Slice 4 (documented): attachment-deletion reverse lookup, ACF field-group topology, exclusion-option changes, and direct-metadata writes not firing `save_post` | The durable index self-maintains per entity on the common admin edit/delete paths without a full rebuild; broader invalidation pairs with the background reconcile | R2-H/Slice 3 |

| E-074 | 2026-08-18 | DBVC | `MediaScanCoordinator` (`dbvc_visual_editor_media_scan_completed` action); `MediaIndexProjector`; `MediaIndexReadModel`; `Addon::register` wiring (MM-gated); `VisualEditorMediaIndexPhase2Test` | R2-H Phase 1 Slice 2 — index population + read-time filtered read model. The coordinator fires a `media_scan_completed` action on completion; `MediaIndexProjector` rebuilds the index from the completed snapshot under a fresh generation (per-entity identity, cached label/url, missing count, per-family breakdown, content hash; prior generation pruned) — write-only, storing no field keys/selectors/paths. `MediaIndexReadModel::getList` lists missing-media-first and enforces the shared-index security contract: for every row it re-resolves the entity and re-runs `EligibilityPolicy` for the **current user** and drops any row that user may not see (stored row is never authority). Verified: population from a real completed scan + the completion-hook path; eligible listing with no raw-target leak; an entity unpublished after indexing filtered out at read time while its shared row is retained; a subscriber sees zero rows. 5 tests/44 assertions; full suite 766/8,908 with the same six inherited failures; agent docs 54/420/0 (new completion-action hook mapped). The live REST/frontend list flip is deferred to Slice 2b (ref/generation unification) — the index is built and safely readable but the interactive path still uses the ephemeral snapshot | The durable index is populated from completed scans and can be listed safely with per-user read-time filtering on a shared store; the interactive read source is not yet flipped | R2-H/Slice 2 |

| E-073 | 2026-08-18 | DBVC | `MediaIndexStore` (`{prefix}dbvc_ve_media_field_index` table); `VisualEditorMediaIndexStoreTest`; `releases/R2H-PERSISTENT-MEDIA-INDEX-PHASE-1.md`; D-053 | R2-H Phase 1 Slice 1 — durable Media Index storage layer. New custom table (schema-versioned via `maybeUpgrade`/`dbDelta`) + `MediaIndexStore` repository: `upsertEntity` (insert or in-place update keyed by entity identity), `getEntity`, filtered `listEntities`/`countEntities` (generation/onlyMissing/onlyDirty + bounded pagination), `markDirty`/`markGenerationDirty`, `deleteEntity`/`pruneOtherGenerations`/`deleteAll`, and opaque `vmig_…` generation rotation; inputs sanitized, reads prepared. Named `media_field_index` to avoid confusion with the pre-existing core DBVC `dbvc_media_index` attachment-file table (different purpose: this indexes media-*field* completeness of entities). Storage-only and behavior-neutral: `register()` is not wired into the bootstrap and no Media Manager path reads/writes the index yet (Slice 2 wires population + read-time listing with per-user eligibility filtering). 6 tests/32 assertions; full suite 761/8,864 with the same six inherited failures; agent docs 54/419/0 (new table mapped). Backup portability (JSON export), incremental invalidation, and background build/reconcile are later slices | The persistent index has a durable, tested per-entity store; it changes no current behavior until Slice 2 flips the read source | R2-H/Slice 1 |

| E-072 | 2026-08-18 | DBVC | `VisualEditorMediaManagerR2E4Test`; `DBVC_Visual_Editor_Addon::is_media_manager_enabled`; `MediaManagerController::canAccess`; `releases/MEDIA-MANAGER-RELEASE-NOTES-AND-ROLLBACK.md` | R2-E4 feature isolation + ship-readiness. Proves the Media Manager kill switch gates the whole remediation surface, not just assets: `is_media_manager_enabled()` requires BOTH the Visual Editor master flag and the feature flag; the REST permission gate `canAccess` (the permission_callback for every route) is closed when the MM flag is off, the master flag is off, the user lacks the base capability, or the request is logged out — and open only when all hold. 4 tests/10 assertions; combined Media Manager PHP 71 tests. Added a consolidated release-notes/rollback runbook (feature summary R1→R2, gates/isolation, side-effect boundaries, non-destructive rollback: disable the flag → surface off with existing assignments intact; no MM-specific schema migration; per-change revert via the journal). Full suite 755/8,832 with the same six inherited failures; agent docs 54/418/0. R2-E (E1–E4) is complete; real-browser/AT QA remains the standing residual gate (D-049) | The Media Manager kill switch is proven end-to-end (routes closed when disabled/uncapable/logged-out), and the ship-readiness release-notes/rollback runbook is documented | R2/R2-E4 |

| E-071 | 2026-08-18 | DBVC | `MediaScanReadModel::expandGroup` (now-populated → `projectAssignedField`); `VisualEditorMediaManagerR2CTest`; `VisualEditorMediaManagerR1CTest`; `visual-editor-media-manager-state.test.cjs` (`reconciledRow` + R2-C save case) | R2-F Slice 4 / D-052 fix for the field report on the "Agencies" CPT: after assigning a previously-missing field, the reconcile projected it as a terminal `resolved_or_changed` ("refresh the scan") with a preview but no `valueRef`, so no replace control rendered and no assign control either — a dead end affecting every just-assigned family. Fix: `expandGroup` projects an original finding that is now populated (absent from the current missing findings and present in `assigned_by_ref`) as an **`assigned`** field via `projectAssignedField` (valueRef + `replace: true`), incrementing `populated` not `resolvedOrChanged`; the genuinely gone/ineligible case keeps `resolved_or_changed`. Read-model projection only — no new REST/mutation/capability surface; the just-assigned field's `valueRef` comes from the same live rescan as any assigned field, so the Slice 3 expected-current-value precondition still gates replace. Verified: R2-C PHP asserts a just-assigned featured/ACF-image field reconciles as `assigned` with a `vemv_` valueRef + `replace: true`; R1-C asserts a now-populated field counts as `populated` (not `resolvedOrChanged`); the R2-C jsdom save case asserts the field is `assigned` and immediately offers a `replace-media` control with the row still marked resolved (missing 0). Full suite 751/8,822 with the same six inherited failures (one order-dependent flaky ContentMigration test unrelated to this change passes in isolation); jsdom 34/34; media-manager lint clean; agent docs 54/418/0 | A field you just assigned is immediately replaceable (valueRef + replace control) with no "refresh the scan" dead end and no rescan; genuinely gone fields still prompt a refresh | R2-F/Slice 4 |

| E-070 | 2026-08-18 | DBVC | `media-manager-app.js` (`disposeActiveFrame`, single-active-frame lifecycle in `openAssignFrame`/`resetExpansion`/`expandGroup`/`close`); `visual-editor-media-manager-state.test.cjs` (4 R2-E3 jsdom cases + frame-disposal mock) | R2-E3 repeated-remediation frame lifecycle + surgical DOM patch (presentation only, no REST/mutation change). Root issue (RK-011): `openAssignFrame` created a new `wp.media` frame on every open and never disposed it, accumulating frames/listeners over repeated remediation. Fixed to a single-active-frame lifecycle — `disposeActiveFrame()` tears down the prior frame (guarded `detach()`/`remove()`/`dispose()`) before each open and on collapse, group-switch, list reload, and close. jsdom proves: 3 repeated opens keep at most one live frame (2 disposed, 1 live); collapsing the row and closing the manager each dispose the frame; and saving one field patches only its row — the untouched sibling row is the SAME DOM node (not rebuilt) and no list/scan request accompanies the reconcile (current-page DOM patch is truthful, no reload). 34 jsdom cases total; R1-D read-only invariant intact; media-manager lint clean; full PHP suite unchanged at 751/8,815 with the same six inherited failures; agent docs 54/418/0. Residual: real-browser memory/listener profiling over long sessions (D-049) | Repeated media remediation no longer accumulates frames/listeners, and a save updates only the affected row without reloading the table; real-browser profiling deferred | R2/R2-E3 |

| E-069 | 2026-08-18 | DBVC | `VisualEditorMediaManagerR2E2Test` (assign + replace); `EligibilityPolicy::assessPost`; `CapabilityManager::canEditDescriptor`/`canEditPostId` | R2-E2 security/stale/permission hardening for the end-to-end mutation paths (complements the R2-C/R2-F edge coverage). Proves both `assign()` and `replace()` fail closed AND leave content untouched when: a different user presents the user/blog-bound snapshot (`media_scan_expired_or_invalid`), the scan revision changed (`media_scan_revision_changed`), the owner is unpublished after the scan/read (`media_assignment_stale` / `media_replace_unavailable` — eligibility re-checked before write), and the object edit capability is revoked mid-flow via a `map_meta_cap` deny filter (surfaces as an eligibility failure before the write). Also confirms a non-existent attachment id is rejected without writing (`media_assignment_save_failed`) — no silent bad-id write. 9 tests/67 assertions; combined Media Manager PHP 67 tests/1,680 assertions; full suite 751/8,815 with the same six inherited failures; agent docs 54/418/0 | The assign/replace write paths fail closed for cross-user, stale-revision, lost-eligibility, revoked-capability, and invalid-attachment cases with the content provably unchanged | R2/R2-E2 |

| E-068 | 2026-08-18 | DBVC | ACF source `class-acf-field-group.php` (`prepare_field_for_db`/`load_value`/`update_value`); `AbstractAcfResolver` (`writeGroupedFieldValue`/`readGroupedFieldValue`/`getGroupWritePath`); `MediaFindingDescriptorBridge::buildDescriptor` (`group_write_path`, root selector); `VisualEditorMediaManagerGroupedFieldTest`; updated `VisualEditorMediaManagerR2ATest` | Corrected the group-nested ACF save. Field report: after the first fix the nested gallery/image saved with no error but did not persist (gone on refresh); R2-G's optimistic thumbnail masked it. Root cause proven from ACF source: a Group subfield is stored at `{group}_{subfield}` and only written correctly through the root group's `update_value` (`prepare_field_for_db` prefixes the name; `load_value` returns the group key-indexed). The first fix (leaf-key selector) wrote via `update_field(leaf_key)` to the bare, unprefixed meta ACF never reads. Corrected: the descriptor targets the ROOT group selector and carries `group_write_path` (the {key,name} chain to the leaf); `writeGroupedFieldValue` writes `update_field(root, [nested→leaf])` (ACF preserves siblings via `continue`) and `readGroupedFieldValue` reads back through the group, returning ok:false on mismatch so a non-persisting write errors honestly (which also removes the masking). Scoped to descriptors carrying `group_write_path` (Media Manager findings) — the main editor resolver path is unchanged. Verified: `VisualEditorMediaManagerGroupedFieldTest` (4 tests) proves the root-selector + `group_write_path` contract and that a grouped write never lands in a bare leaf meta; the R2-A grouped test now asserts the root-selector/write-path contract; combined Media Manager PHP 58 tests/1,613 assertions; full suite 742/8,748 with the same six inherited failures; agent docs 54/418/0. Residual: no real nested ACF in the test env, so ACF-happy-path persistence is proven from ACF source + the contract; live nested-group save re-test on the reporting site pending (RK-013) | Nested-group media fields now write/read through the parent group so values actually persist; a non-persisting write fails honestly instead of silently vanishing; live re-test pending | R2-F/fix |

| E-067 | 2026-08-18 | DBVC | `media-manager-app.js` (`savedSelectionPreview`/`applySavedPreviewFallback` in `reconcileAfterSave`; `createStatusPanel`/`createHeader`/`createBody` refactor); `media-manager.css` (`__header` column, `__header-top`, compact `__status-panel`); `releases/R2G-...`; `visual-editor-media-manager-state.test.cjs` | R2-G Media Manager UX polish (presentation only, no new REST/mutation/PHP surface). Slice 1: on a successful save the picked selection's thumbnail (already held client-side from `wp.media`) fills the reconciled field's `preview` when the server reread preview has no URL, so the left thumbnail reflects the saved media immediately without a reload — the server preview stays the source of truth. Slice 2: the status panel (state title/description/progress + scan action buttons) moved from the results body into the header as a compact `[content | actions]` grid with smaller buttons/labels and a narrow-width stack, reducing popup crowding. Verified: 2 new jsdom cases (30 total) — Slice 1 shows `pic-21-150x150` in the thumbnail with no list/scan call; Slice 2 asserts the panel is in `__header` (not `__body`) with the `data-*` state hooks and action button intact — plus a faithful static layout preview; media-manager lint clean; R1-D read-only invariant intact; full PHP suite unchanged at 741/8,737 with the same six inherited failures; agent docs 54/418/0 | The picked media populates the thumbnail without a reload, and the status panel/controls sit compactly in the header to declutter the popup, with the state machine, action visibility, and ARIA unchanged | R2-G |

| E-066 | 2026-08-18 | DBVC | `MediaFindingDescriptorBridge::buildDescriptor` (leaf-key write selector for grouped fields); `VisualEditorMediaManagerGroupedFieldTest`; updated `VisualEditorMediaManagerR2ATest` grouped-selector assertion | Field-report fix: a group-nested ACF gallery/image ("Assortment-Objects / Item/Object / Media") returned `400 "ACF field update did not succeed."` on assign and silently failed on retry. Root cause: the scan catalog's `selector` for a nested field is the ROOT group key (`AcfMediaFieldCatalog:319`) — correct for reading, wrong for writing — and `buildDescriptor` copied it into the descriptor's write identifier, so `AbstractAcfResolver::writeAcfValue` (which prefers the identifier for grouped sources) called `update_field(<root group key>, <gallery ids>)`. The fix targets the leaf `field_key` for grouped fields so `update_field()`/`get_field()` resolve the nested subfield, matching the main editor's `resolveAcfFieldSelector`; top-level fields (selector already === leaf key) are unchanged, and the single change fixes both assign (R2-C) and replace (R2-F Slice 3). `VisualEditorMediaManagerGroupedFieldTest` 3 tests/30 assertions prove the grouped gallery/image descriptor now targets `field_leaf_gallery`/`field_leaf_image` not `field_container`; the R2-A grouped-selector test was corrected from the old (buggy) root-key expectation. Full suite 741/8,737 with the same six inherited failures; agent docs 54/418/0. Note: the disposable PHPUnit env has no real nested ACF, so end-to-end grouped **save** is verified at the descriptor-targeting level; live nested-group confirmation on the reporting site is the residual gate | Group-nested ACF media fields now write to the correct subfield instead of the group root; the descriptor-targeting fix is proven and the live save is pending user re-test | R2-F/fix |

| E-065 | 2026-08-17 | DBVC | `MediaScanService` (`value_fingerprint`); `MediaScanReadModel` (`projectAssignedField` → `valueRef`/`replace`); `MediaFindingDescriptorBridge::resolveReplaceable`; `MediaAssignmentService::replace`/`applyMutation`; `MediaManagerController` (`/replacement` route + `handleReplaceFinding`); `api-client.js` (`replace()`); `media-manager-app.js` (`createFieldReplaceControls`/`beginReplaceMedia`/`saveReplacement`); `VisualEditorMediaManagerR2FReplaceTest` | R2-F Slice 3 (gated replace mutation). Populated image/gallery fields now carry an opaque `value_fingerprint` (`vemv_[a-f0-9]{24}`) surfaced as `valueRef`; the replace path re-resolves the owner from the snapshot, rescans in inventory mode, confirms the field is still populated, and enforces the expected-current-value precondition by `hash_equals` before writing — every non-writable outcome is a hard `WP_Error` (`media_replace_stale`, `media_replace_not_populated`, `media_replace_value_ref_invalid`). The write shares the R2-C `applyMutation` pipeline (validate → normalize → mutate → journal/audit/cache → `expandGroup` reconcile), overwrites the field reference only, and never deletes attachments. `VisualEditorMediaManagerR2FReplaceTest` 6 tests/62 assertions (success + fresh fingerprint + prior attachment retained + cache event; stale-ref block; emptied-after-read block; malformed-ref reject; non-image reject; gallery overwrite) plus 3 jsdom cases (control gating on `valueRef`; expected-current-value POST + reconcile; stale 409 retains the staged selection); full suite 738/8,705 with the same six inherited failures; media-manager lint clean; agent docs 54/418/0 (new `/replacement` route mapped); R1-D read-only invariant extended to the distinct gated endpoint and still forbids `fetch(`/`.save(`/composite-save | A populated media field can be deliberately overwritten only when its current value matches what the user read, the overwrite is journaled/audited/cache-invalidated, no attachment is deleted, and the write target is always the freshly server-resolved descriptor | R2-F/Slice 3 |

## Required Media Manager evidence

- Existing scanner/job/session implementation
- Existing ACF field catalog/location matcher
- Existing public object enumerator/capability filter
- Existing featured-image descriptor/editor/mutation
- Existing ACF image descriptor/editor/mutation
- Existing ACF gallery descriptor/editor/mutation
- Existing nested path support
- Existing Media Library frame lifecycle
- Existing composite/multi-field save pattern
- Existing journal/cache/revalidation hooks
- Existing large-table/pagination/virtualization components
- VerticalFramework Media Health/missing-file scanner evidence
- VerticalFramework ACF image/gallery catalogs and exact JSON paths

## Required Brand/Workspace evidence

- Existing Shared Globals configuration and flow
- Registry-like structures
- Option owner canonical representation
- Current option-family support
- Toolbar/overflow/state conventions
- Go To Object/object navigation APIs
- CSS/z-index/focus conventions
- Tests/fixtures/QA notes

## Evidence quality

Strong evidence includes:

- exact path and symbol;
- relevant line/function/class;
- commit or working-tree status;
- test proving behavior;
- captured command output;
- representative fixture/data count.

Avoid vague entries such as “image support exists” without the actual resolver/control/write path.
