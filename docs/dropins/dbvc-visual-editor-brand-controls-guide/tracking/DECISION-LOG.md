# Decision Log

Record actual decisions and their evidence. The defaults below are package directions that R0 must validate against current code.

| ID | Status | Decision | Rationale | Evidence | Revisit trigger |
|---|---|---|---|---|---|
| D-064 | Accepted (2026-08-30) | R4-A value summaries ship as a **batch endpoint** (`POST .../control-center/value-summaries` body `{publicIds: […]}`, batch cap 50) rather than as an inline extra field on every `GET .../controls` list row. Per record, capability is rechecked against the resolved descriptor before the provider's summary factory runs, so a summary can never leak an owned value the caller could not edit. Registry sort switches to `sortKey ASC → label ASC (case-insensitive) → publicId ASC` globally (previously per-provider `id` ASC); providers that leave `sortKey` empty fall through to the label/publicId chain. A provider that throws in `getControls()` is captured in `getProviderErrors()` and dropped from the current pass rather than propagating — one buggy provider must not shield the rest of the map | Batch: keeps the list route cheap on load (400+ Vertical rows never call the summary factory) and lets the drawer paginate summaries lazily against a fixed 50-record cap as rows scroll into view; matches the R4 mockup DESIGN-DECISIONS §5 shape. Global sort: a provider-defined `sortKey` should out-order per-provider defaults so Vertical can pin `must` records ahead of `should`/`nice` inside the same category — R3-A's per-provider id sort could not express that. Fail-soft on `getControls()`: matches the R3-D operator model — an operator can turn the feature off, but a single provider bug must not force that ejection; the drawer shows a subtle banner and keeps serving the healthy providers' records (mockup DESIGN-DECISIONS §6) | `addons/visual-editor/src/Rest/Controllers/ControlCenterValueSummariesController.php` (new); `addons/visual-editor/src/Registry/ControlRegistry.php` (`buildValueSummaryForRecord`, `getProviderErrors`, new sort); `addons/visual-editor/src/Registry/Providers/SharedGlobalsControlProvider.php` (real `buildValueSummary` with option-owned capability recheck); `tests/phpunit/VisualEditorControlCenterR4ATest.php` (19 tests covering the three shape changes); E-100 | Evidence that a page-load summary is cheaper than the batch (unlikely at 400+ records), OR a provider proves it needs to opt out of sort-by-label tiebreak (custom comparator), OR fail-soft masks a class of provider bugs the drawer never surfaces (elevate `providerErrors` banner from subtle to blocking) |
| D-063 | Accepted (2026-08-28) | The R3 Brand Control Center registry-backed surface ships behind a **two-part kill switch** — the master Visual Editor switch AND a new `OPTION_CONTROL_CENTER_ENABLED` (`dbvc_visual_editor_control_center_enabled`, default `'0'`) — and the existing `SharedGlobalFieldsController` popover **stays intact** as a parallel path. R3-B registers `SharedGlobalsControlProvider` on the runtime `ControlRegistry` under that gate; R3-C will add the drawer + open route under the same gate. R3-B (and by extension R3-C when it lands) creates no new write authority — the registry is a discovery-only read model, and existing capability checks continue to gate every mutation through the shared `MutationService` pipeline | Mirrors the well-tested `is_media_manager_enabled()` pattern (requires master switch plus feature-specific switch, both default off) captured in `EXISTING-SUPPORT-INVENTORY.md` §2.5. Keeping the existing Shared Globals popover route intact and parallel avoids any regression risk on the proven surface while the registry-backed path is stabilizing across R3-B/C/D — and matches the R3 plan's explicit "no behavior regression when registry-backed discovery is disabled or unavailable" gate. Two-part gating also gives operators a way to disable just the Control Center without disabling the entire Visual Editor, matching the operator model already established for Media Manager and R3-BX Curation | `addons/visual-editor/bootstrap.php` (`OPTION_CONTROL_CENTER_ENABLED`, `is_control_center_enabled()`); `addons/visual-editor/src/Bootstrap/Addon.php` (kill-switch-gated `SharedGlobalsControlProvider` registration + `getControlRegistry()`); `addons/visual-editor/src/Registry/Providers/SharedGlobalsControlProvider.php`; `SharedGlobalFieldsController` unchanged; `EXISTING-SUPPORT-INVENTORY.md` §2.5 (kill-switch pattern) + §3 (R3-B row); E-094 | The Control Center switch becomes the sole entry point and the Shared Globals popover is decommissioned (would require an explicit deprecation slice with a migration plan for users who have the popover in their muscle memory), OR the registry-backed surface proves stable enough to graduate out of a kill switch entirely |
| D-001 | Accepted | Media Manager ships before Brand Control Center UI | Current production priority; no architecture conflict requires reversing it | User direction; R0 architecture map | Priority changes |
| D-002 | Accepted | R1 is scan/report; R2 adds mutation | Scanner/read-model contracts are missing while mutation primitives already exist and need a safe bridge | `01-DISCOVERY-AND-CURRENT-STATE-REVIEW.md` | Explicit scope change |
| D-003 | Accepted with clarification | Scan published public/show-UI posts/CPTs and valid terms in public/show-UI taxonomies, subject to exclusions and object capability | Terms have no publish status; capability and route validity must be checked | R0 runtime probe; `ObjectSearchController` policy | New owner/status requirement |
| D-004 | Accepted with narrowed first slice | Scan native featured image and unconditional ACF image/gallery fields; initial nested support is top-level and deterministic group-only | Existing media resolvers support more render-proven paths than an off-page scanner can currently prove | Resolver and ACF runtime inspection | Dedicated nested path slice passes fixtures/tests |
| D-005 | Accepted | No arbitrary meta scanning | Preserves declared field/source authority | Existing resolver invariants; Vertical unused-media heuristics are intentionally not reused | New explicit architecture approval |
| D-006 | Rejected as written | No suitable existing scanner/catalog/session can be reused wholesale; build a narrow Media Manager coordinator/catalog/snapshot using existing conventions | DBVC/Vertical scanners solve attachment/filesystem/migration problems; descriptor sessions have a different lifecycle | R0 DBVC/Vertical evidence | A later shared core service proves exact contract parity |
| D-007 | Accepted | User-triggered bounded requests; never scan on page load | 454 current candidate entities and 2,119 applicable field pairs require predictable cancellation/progress | R0 runtime probe | Proven safe background job architecture is intentionally adopted |
| D-008 | Accepted for R2 | Native WordPress Media Library handles selection/upload | Existing `wp.media` lifecycle and WordPress permissions are authoritative | `AssetLoader`; `overlay-app.js` | Core path cannot meet an approved requirement |
| D-009 | Accepted | No cross-entity bulk save in R1/R2 | No approved generalized transactional batch exists | `MutationService`; composite controller | Later change-set program |
| D-010 | Deferred | Same-entity Save Row is not in initial R2 | Current composite expected-value/compensation contract is collection-specific; single save lacks expected-old input | `MutationService::mutateBatch()` and single-save trace | A general same-owner media preflight/outcome contract is approved and tested |
| D-011 | Accepted | Scan findings are non-authoritative; R2 creates a fresh descriptor after owner/field/value/capability revalidation | Preserves descriptor/mutation authority | `EditableRegistry`; `SharedGlobalFieldsController` precedent | Never |
| D-012 | Accepted | Targeted reread is required before a finding is marked resolved | Endpoint success is not source verification | Existing mutation/cache behavior | Never |
| D-013 | Accepted | Static PNG/HTML mockups are visual references only and follow the accepted read model | Prevents mockup-driven data contracts | Handoff model | Never |
| D-014 | Accepted | DBVC works without VerticalFramework | Plugin portability and no live integration hook | Vertical R0 inspection | Explicit product/platform change |
| D-015 | Accepted | Use a separate user/blog-bound scan snapshot rather than `EditableRegistry` as the result store | Findings need paging, progress, cancel, generation, expiry, and non-authoritative lifecycle semantics | `EditableRegistry` inspection | Descriptor session is generalized with equivalent isolation without weakening authority |
| D-016 | Accepted | Exact ACF field-group applicability uses active runtime groups plus `acf_get_field_group_visibility()` | Existing catalogs summarize definitions but do not match all registered location rules per entity | ACF 6.6.2 source/runtime probe | ACF supplies a stronger public object-applicability API |
| D-017 | Accepted | Conditional media fields with inactive/unknown server state are excluded and counted in initial R1 | Reporting optional hidden fields as missing is noisy and condition evaluation is not currently proven | 10 conditional runtime media definitions | A server-side conditional evaluator is proven with fixtures |
| D-018 | Accepted | Featured-image findings are labeled empty assignments, not required-media defects | All 26 current post types advertise thumbnail support and 101 assignments are empty; support does not imply requirement | R0 runtime probe | A provider explicitly declares requiredness |
| D-019 | Accepted | R1 adds no new editor/media enqueue; current eager enqueue is documented as baseline | Visual Editor already enqueues editor/media assets in active mode | `AssetLoader::enqueue()` | Separate performance release changes asset policy |
| D-020 | Accepted | Initial R2 uses single-field media saves with an expected-empty precondition | Prevents overwriting a value populated after scan without inventing a batch transaction | Current single/composite mutation trace | General batch contract approved |
| D-021 | Accepted | R1 revalidates a row on expansion before exposing current field findings | Scan snapshots age; capability/status/location/value may change | R0 stale-value analysis | Never |
| D-022 | Accepted; R1-A authorized and implemented | R1 ships as five slices: policy/catalog, bounded scanner/snapshot, read model/revalidation, frontend table/mockup translation, hardening | Keeps contracts testable and preserves scan/report independence | Revised R1 plan; canonical phase reconciliation at `5db4b40`; user `Proceed` crossing line for R1-A | Explicit product scope change before later R1 slices |
| D-023 | Accepted | Initial R2 excludes same-entity `Save Row` | Current composite preflight/compensation is collection-specific and single save lacks expected-old input; per-field expected-empty is the smallest safe bridge | `MutationService`; `CompositeSaveController`; R0 save trace | A separately approved general media batch contract passes preflight/outcome/compensation tests |
| D-024 | Accepted | Inherited validation limitations are recorded, not normalized as success | R1-A full comparison retained the same six failure identities across 689 tests versus the 684-test checkpoint; full JavaScript lint still has no completed result | User checkpoint; focused R0/R1-A results; full R1-A PHP comparison | A green full suite and completed full lint supersede the limitation |
| D-025 | Accepted | R1-A uses a standalone scan-specific owner policy and ACF catalog; it does not wire into the render-time descriptor registry or add routes | Off-page discovery needs published/public object rules and exact ACF location visibility, while `EditableRegistry` remains request-render and mutation authority | `MediaManager\\EligibilityPolicy`; `AcfMediaFieldCatalog`; focused 5-test/65-assertion contract suite | R1-B integration reveals a missing policy input or exact catalog mismatch |
| D-026 | Accepted; R1-B authorized and implemented | R1-B uses bounded source pages plus a separately namespaced compressed transient snapshot with one latest generation per user/blog, monotonic revision checks, and a short atomic update lock | No existing DBVC/Vertical scanner has the required owner-field domain; descriptor sessions lack paging/cancel/config lifecycle; revision plus lock prevents duplicate/concurrent chunk corruption | `ScanCandidateProvider`; `ScanSnapshotStore`; `MediaScanCoordinator`; user authorization to proceed; focused 5-test/106-assertion R1-B suite | Representative production scale exceeds the measured transient/payload ceiling or a proven shared queue offers equivalent isolation |
| D-027 | Accepted | R1-B stores server-internal owner/field path data but exposes only opaque HMAC-derived group/finding references in the future read model | R1-C/R2 need server-owned revalidation inputs, while browser authority must remain limited to non-guessable references | `MediaScanService`; `SCAN-AND-FINDING-CONTRACT.md`; deterministic identity tests | R1-C proves a smaller internal record without losing revalidation safety |
| D-028 | Accepted; R1-C authorized and implemented | R1-C uses one protected Media Manager controller over the existing coordinator/read model; explicit list/row calls require generation and revision, latest is a resume exception, cursor paging omits an exact filtered total, and row expansion rescans one server-owned entity without issuing descriptors | This keeps active-mode, capability, stale-request, and snapshot isolation consistent while bounding list-time object checks and preserving R2 as the only future descriptor/mutation bridge | `MediaManagerController`; `MediaScanReadModel`; `VisualEditorMediaManagerR1CTest` 6/417; user authorization to proceed | R1-D needs a different safe display field/action or R1-E profiling proves the cursor/read bounds insufficient |
| D-029 | Accepted; R1-D authorized | Claude starts from one self-contained R1-D context capsule plus an R1-C-safe fixture and loads broader docs/source only on a demonstrated discrepancy; D1-D4 remain separately reviewed | Repeating repository architecture in a large startup prompt wastes context and risks drift. A compact launcher, current fixture, and progressive reads preserve authority while reducing initial token spend | User R1-D authorization and context-usage request; `MEDIA-MANAGER-CLAUDE-MOCKUP-HANDOFF.md`; `media-manager-r1c-view-model.json` | The capsule becomes stale, Claude cannot resolve a required design decision without repeated broad reads, or production integration begins |
| D-030 | Accepted with required adaptations | The Claude D1-D4 mockup is approved as visual direction, not as production markup, CSS, JavaScript, fixture authority, or runtime evidence | Its hierarchy, read-only boundary, naming/tokens, server-driven queries, opaque rows, cursor pattern, expansion, and single responsive markup align with current architecture, while contract and short-height defects make verbatim translation unsafe | `docs/ui-mockups/dbvc-visual-editor/r1-media-manager/`; Codex source/browser review; E-039/E-040 | D4A corrections are reviewed or a product decision changes the visual direction |
| D-031 | Accepted | Production uses an explicit backend-to-view state adapter and keeps request errors separate from row revalidation statuses | Backend states are `scanning`, `failed`, `canceled`, `complete`, and `invalidated`; latest/group 404s and stale/superseded requests are not equivalent to `current`/`changed`/`resolved_or_changed`/provider-`unavailable` row projections | `MediaScanCoordinator`; `MediaScanReadModel`; `MediaManagerController`; E-040 | The server publishes a new normalized UI-state contract |
| D-032 | Accepted | D4A must correct the package fixture and short-height scroll model before production frontend translation | The fixture conflicts with authoritative sort/pagination and omits production expansion fields; at 390x844 the drawer's hidden overflow and results-only scroll make lower controls/results unreachable | `media-manager-r1c-view-model.json`; `styles.css`; 390x844 browser geometry; E-040 | Corrected fixture and 390x844/375x667/320x568 acceptance evidence land |
| D-033 | Accepted; D4A complete | The corrected D4A package is approved as qualified static visual direction and production translation proceeds one bounded slice at a time | The fixture, state adapter, request/row error separation, responsive scroll model, announcements, pluralization, touch targets, screenshots, and active documentation now agree with the R1-C read contract. Static HTML/ARIA and axe evidence do not replace production integration, assistive-technology, browser, or device proof | Corrected R1-C fixture; `docs/ui-mockups/dbvc-visual-editor/r1-media-manager/`; E-041 | Production evidence disproves the interaction direction or the R1-C contract changes |
| D-034 | Accepted | The shared subtle-text contrast correction is a production token change, not a mockup-only override | The D4A axe run traced all 12 confirmed failures to the authoritative `--dbvc-ve-color-text-subtle` token in `overlay.css`; changing only the mockup would create a false pass | `addons/visual-editor/assets/css/overlay.css`; mockup `DESIGN-DECISIONS.md` section 6.8; E-041; RK-036 | A broader palette review proves a different token/value is required |
| D-035 | Accepted; production slice 1 implemented | The Media Manager shell is a separately enqueued module and the existing overlay owns only its conditional toolbar event seam | Keeping shell lifecycle/state outside the already-large overlay avoids creating a second framework while preserving current toolbar, feature, active-mode, and Builder guards. The first slice remains presentation-only | `AssetLoader`; `overlay-app.js`; `media-manager-app.js`; `media-manager.css`; `VisualEditorMediaManagerR1DTest`; E-042 | The next API/state slice proves the module boundary cannot own scan/query state cleanly |
| D-036 | Closed 2026-08-23; **superseded by [[D-058]]** | Original 2026-08-16 wording: "responsive expansion tabled program-wide, mobile work paused until a later explicit decision." **D-058 (2026-08-23) closes this door permanently**: desktop-only is now a **permanent non-goal** for mobile support, not a deferral. Existing narrow-width protections stay as a regression floor to keep desktop at unusual DPI/zoom from breaking; no new mobile-shaped work builds on them | Superseded per D-058: the maintainer's product boundary has always been staff operating dental/medical/contractor client sites on laptops or desktops, and keeping mobile "deferred" invited scope drift back into every phase's residual-gates list. Making it an explicit non-goal is honest about intent | See [[D-058]] for the current authoritative statement; the original references (`media-manager.css`, RK-034/RK-035) still apply for their regression-floor role only | Not applicable — D-058 supersedes this row; a fresh scope decision would open a new row, not resume D-036 |
| D-037 | Accepted; production slice 2 implemented | Media Manager lifecycle/query state stays in `media-manager-app.js`, while nonce-authenticated route construction stays in `DBVCVisualEditorApi.mediaManager`; the state module accepts only copied R1-C projections and renders no result row yet | This preserves the existing API owner, avoids putting request state into the large overlay, and lets the next table slice consume one explicit state/event contract. Monotonic request IDs plus same-generation revision checks prevent slower responses from replacing newer accepted state | `api-client.js`; `media-manager-app.js`; `tests/visual-editor-media-manager-state.test.cjs`; desktop/laptop browser harness; E-043 | The table slice proves replacement/append state requires a narrower store contract or authenticated runtime disproves the route adapter |
| D-038 | Accepted; production slice 3 implemented | The R1 list UI is a semantic server-driven laptop/desktop table over the safe R1-C projection; query changes replace page one, opaque-cursor requests append by unique group reference while preserving scroll, and the browser does not calculate authoritative filtered totals or expose row targets | Server-owned search/filter/sort/paging preserves permission and visibility rechecks, while a bounded DOM and opaque rows avoid duplicating backend authority. Keeping expansion out of this slice makes the next group-hydration crossing line independently reviewable | `media-manager-app.js`; `media-manager.css`; `tests/visual-editor-media-manager-state.test.cjs`; `tests/playwright/visual-editor-media-manager-table.spec.js`; E-044 | Authenticated runtime reveals a payload/geometry defect, R1-E scale evidence requires a different bounded rendering strategy, or the read model changes |
| D-039 | Accepted; production slice 4 implemented | R1 expands at most one entity row through the protected server-owned group route. Expansion uses current scan/generation/revision identity, independent request sequencing and error state, strict safe-field normalization, and accessible button/region semantics; it never issues a descriptor or exposes a media/mutation action | A separate expansion lifecycle keeps a deleted/out-of-scope/request-failed entity from erasing the authoritative list and prevents slower responses from reopening a collapsed or superseded row. Reusing the R1-C group projection keeps owner/field/path targets server-side and leaves the R2 authority crossing independently reviewable | `api-client.js`; `media-manager-app.js`; `media-manager.css`; focused jsdom/PHP/Chromium coverage; E-045 | Authenticated runtime disproves group-state mapping, R1-E accessibility/scale evidence requires a different interaction, or the R2 descriptor bridge needs a narrower compatible expansion contract |
| D-040 | Accepted; initial R1-E keyboard hardening implemented | Expansion rerenders restore focus to the replacement row button using its allowlisted opaque group reference and `ownerDocument.activeElement`; Enter and Space remain native button activation, and the disclosure indicator has no transition under reduced-motion preference | Replacing the table body is needed for the current simple bounded renderer but otherwise detaches the active button. Explicit focus continuity preserves keyboard position without inventing custom key handlers or moving table scroll, while a reduced-motion rule avoids unnecessary disclosure animation | `media-manager-app.js`; `media-manager.css`; jsdom focus assertions; laptop/desktop Playwright keyboard/reduced-motion checks; E-046 | R1-E authenticated runtime/AT evidence requires a different focus lifecycle or table rendering strategy |
| D-041 | Accepted; R1-E automated semantic hardening implemented | Keep one stable polite live region and give the existing dialog, results scroller, loading states, and expanded entity region explicit names/status/busy semantics. Expansion uses one entity-specific heading across loading, success, and error, and announces start/completion/failure without making the region itself a broad live container | Stable named structures reduce repeated or ambiguous announcements while preserving the simple bounded renderer and keyboard focus contract. Reusing the existing footer status avoids nested competing live regions and does not introduce layout or R2 authority | `media-manager-app.js`; `AssetLoader`; 11 jsdom tests; focused R1-D PHP; 6 Chromium/Firefox/WebKit laptop/desktop Playwright cases with axe; E-049 | Real AT, real Safari, or authenticated runtime evidence shows duplicate/missing announcements or requires a different region/focus lifecycle |
| D-042 | Accepted; R1-E closeout evidence added | Prove the live REST permission gate with unauthenticated read-only probes and measure complete multi-owner candidate traversal/raw reads with a deterministic non-mutating PHPUnit fixture, instead of logging in, toggling options, or mutating runtime state | No already-authorized authenticated session is available and the task forbids changing login/option/content/LocalWP state to satisfy QA. Unauthenticated 401 enforcement plus a transactional multi-owner traversal test close the highest-value REST-surface and performance gates without crossing the R1 read-only boundary or fabricating authenticated evidence | E-050; E-051; E-052; `tests/phpunit/VisualEditorMediaManagerR1ETest.php` | An already-authorized authenticated session, real Safari, or real assistive technology becomes available, or R2-A is authorized |
| D-043 | Recorded, not actioned | The persistent Visual Editor option `dbvc_addon_visual_editor_enabled` is now ON in the active LocalWP site, a drift from the previously recorded OFF provenance; it is documented and left unchanged | The task forbids toggling persistent options merely to satisfy QA, and the option may have been changed by the user. Recording the drift preserves honest provenance while respecting the read-only/no-state-change boundary | E-050; read-only `wp_options` read on site `4gScrLykQ` | The user asks to normalize the option or authenticated runtime QA requires a specific option state |
| D-044 | Accepted; R1 signed off | R1 is signed off with its residual R1-E gates (authenticated table-data runtime, real assistive technology, real Safari, large-list responsiveness, and a completing aggregate JavaScript lint run) explicitly accepted as documented risk | The security-critical read-only contract is proven by focused, live-REST-auth, and complete-traversal evidence; the residual gates require environments not available in this session and R1 introduces no write path, so accepting them as documented risk is proportionate | User sign-off on 2026-08-16; R1-E closeout evidence E-046-E-052 | New evidence disproves a read-only contract, or a residual environment becomes available and reopens a gate |
| D-045 | Accepted; R2-A implemented | R2-A adds a server-authoritative `MediaFindingDescriptorBridge` that resolves the target only from the current user/site-bound snapshot and opaque references, revalidates snapshot identity/owner status/capability/field applicability/field family/empty value, then mints one fresh standard `EditableDescriptor` reusing the existing registry/resolver families. It reports writable/changed/resolved/unavailable and stops before Media Library selection and mutation | No client-supplied owner id, field key/name, ACF object id, selector, or path may become authority; reusing the existing descriptor/registry/resolver systems (rather than a Media Manager-specific writer) preserves the single mutation authority for R2-C. A narrow `persistDetachedDescriptor` on the registry lets a non-rendered finding produce a retrievable user-bound descriptor without instrumenting a page render | `MediaFindingDescriptorBridge`; `EditableRegistry::persistDetachedDescriptor`; `MediaManagerController` finding-descriptor route; `VisualEditorMediaManagerR2ATest`; E-053 | R2-B media-frame integration needs a different descriptor shape, or authenticated runtime disproves the resolver routing |

| D-046 | Accepted; R2-B implemented | R2-B opens the native WordPress `wp.media` frame from within `media-manager-app.js` using the same standard frame configuration (`{title, button, library:{type:'image'}, multiple}`) as `overlay-app.js`'s image/gallery builders, capability-gated by the existing `supportsWpMedia` bootstrap flag. It stages the selection client-side only (unsaved) and calls no save/mutation path. `overlay-app.js` was left untouched | The overlay is a 330KB critical file; extracting a shared frame factory from its render-coupled closures now would risk regressing proven editing behavior, which the task forbids. Using the identical standard core primitive is faithful reuse of WordPress's media frame (not a fork of the descriptor hydration/save pipeline, which R2-C still routes through the existing MutationService). Consolidating both call sites onto one shared factory is recorded as deferred planned work | `media-manager-app.js` `openAssignFrame`; `api-client.js` `mediaManager.descriptor`; R1-D read-only invariant updated to allow staged, unsaved `wp.media`; E-055 | R2-C or a later cleanup consolidates the overlay and Media Manager onto a shared media-frame module without regression |

| D-047 | Accepted; R2-C implemented | R2-C saves through a dedicated `MediaAssignmentService` + `POST .../findings/{finding_ref}/assignment` route rather than the generic session save. The service re-runs the R2-A `resolveFinding` revalidation immediately before writing (the expected-empty precondition), mutates through the existing `MutationService` (validation, resolver save, journal/audit, cache invalidation), and rereads via `expandGroup` to reconcile the row. The write target is always the freshly server-resolved descriptor; the client selection/token is never the write authority | The gate "a field populated after scan cannot be overwritten" requires re-resolving and reconfirming empty at write time, plus a Media-Manager-specific finding/row reconciliation the generic save does not provide. Re-minting the descriptor from the snapshot at save time guarantees no client-supplied target can be written, and reusing MutationService preserves the single audited mutation authority | `MediaAssignmentService`; `MediaFindingDescriptorBridge::resolveFinding`; `MediaManagerController` assignment route; `media-manager-app.js` save/reconcile; `VisualEditorMediaManagerR2CTest`; E-057 | R2-D/R2-E hardening changes the reconciliation contract, or a shared media-frame consolidation lands |

| D-048 | Accepted; R2-D implemented | R2-D presents the assignment/save UX states as refinements over R2-A/B/C with no new REST or mutation surface: assign notices carry a kind (`refresh` -> polite `role="status"`, `error` -> assertive `role="alert"`) so a validation error is distinct from a stale-refresh prompt; a just-saved field reads as a verified `Saved` success and its resolved row is **marked in place** (`Resolved` badge) rather than removed; upload availability is surfaced with a new `canUpload` bootstrap flag (`upload_files`) driving a "choose existing media" hint; a `media modal open` state shows an `Opening Media Library…` affordance while the descriptor request is in flight | Marking resolved rows in place avoids focus/scroll disruption (the release doc permits "remove or mark"); distinguishing alert vs status notices is an accessibility improvement; surfacing `canUpload` reflects the native wp.media upload gating honestly. All changes are presentation/state only | `media-manager-app.js`; `AssetLoader.php` (`canUpload` + strings); `media-manager.css`; `visual-editor-media-manager-state.test.cjs`; mockup `COMPONENT-NOTES.md` §11; E-060 | R2-E hardening revisits any of these states or a shared media-frame consolidation lands |

| D-049 | Accepted; in progress | R2-E was authorized but sequenced: the laptop/desktop Media Library browser + keyboard QA is **deferred** to a later session, and the journal/cache verification slice (R2-E1) is done first. R2-E is split into R2-E1 (journal/cache verification), R2-E2 (security/stale/attachment/permission tests), R2-E3 (repeated-expansion performance and current-page DOM/reload QA), and R2-E4 (feature isolation, release notes, rollback) | Maintainer direction on 2026-08-16. Journal/cache verification is test-only and unblocks confidence in the audited write path; the browser QA needs an authenticated runtime and is the standing residual gate | Release doc R2-E sub-slices; `VisualEditorMediaManagerR2ETest`; E-062 | The deferred browser/keyboard QA is scheduled, or the R2-E sub-slice ordering changes |

| D-050 | Accepted; gate disabled | The R2 "no editing non-empty fields" exclusion is **disabled going forward** for the Media Manager. The new phase R2-F (Entity Media Inventory & Replace) may present populated media fields and **replace** an existing value (single image / ordered gallery). Replacement is a controlled overwrite governed by an expected-current-value precondition (not R2-C's expected-empty gate), never deletes attachments, and routes through the shared audited mutation pipeline. Populated fields are reported only in the detail panel (the top-level table/counts stay empty-focused), and only a sanitized preview (thumbnail URL/alt/count) is exposed | Maintainer authorization on 2026-08-16. The Media Manager's product direction extends from "fill missing media" to "manage entity media"; the expected-current-value model preserves safety (no blind overwrite of a concurrently-changed field) while enabling intentional replacement | `releases/R2F-MEDIA-MANAGER-ENTITY-MEDIA-INVENTORY-AND-REPLACE.md`; supersedes the R2 out-of-scope exclusion; RK-008; [[RK-012]] | The maintainer re-scopes R2-F or reinstates the non-empty-edit restriction |
| D-062 | Deferred; direction locked (2026-08-27) | The R3-BX curation admin surface is designated to be **promoted to a permanent admin governance tool** — proposed name **"Brand Control Center · admin options"** — in a future deferred phase, rather than being decommissioned once the seed is exported. The current temporary kill-switch policy (D-059) stays authoritative for the R3-BX slice; the promotion is a separate scheduled phase whose scope, timing, and gating are captured in the dedicated deferred-phase doc | The maintainer walked through the curation table on 2026-08-27, bulk-applied the recommender's `include` set (397 new includes; store went from 767 defer / 2 include / 0 ignore → 370 defer / 400 include / 0 ignore; see E-093), and identified that the same admin surface is a strong long-term fit for **per-role / per-site / audit-annotated governance** of which Visual Editor fields are exposed to which frontend users. This is a permanent product opportunity, not a scope creep of R3-BX. Locking the direction now (deferred) means future planners inherit the intent — they will not have to re-derive whether the tool is throwaway or permanent, and R3-C / R5.x are unblocked because they do not depend on this promotion landing | `docs/dropins/dbvc-visual-editor-brand-controls-guide/deferred/BRAND-CONTROL-CENTER-ADMIN-OPTIONS-PHASE.md` (new); supersedes [[D-059]] in spirit when the promotion phase lands (D-059 stays authoritative until then); E-093 (bulk-apply evidence) | The promotion phase gets scheduled (turns from deferred → planned), or the maintainer explicitly reverses direction and reinstates the "temporary throwaway" plan |
| D-061 | Accepted (2026-08-26); supersedes [[D-060]] | The R3 Brand Control Center is a **new left-anchored tabbed scrollable drawer** opened from **a new dedicated icon on the Visual Editor floating toolbar**. The existing Layers / Shared Globals toolbar entry and its popover stay **completely intact** — no evolution, no content addition, no visual change. The drawer is a new component shell (`.dbvc-ve-control-center`), fixed width ~480px, full-viewport-height minus admin bar and toolbar strip, backdrop-less so the site stays visible on the right, with a tab strip (one tab per curation category plus an `All` tab), a persistent filter strip, a sticky-header scrollable table body, and a footer showing row counts. Coexists with the main editor panel — opening a row does not close the drawer. Outside-click does NOT close (workspace, not popover); Escape does. New z-index token `--dbvc-ve-z-drawer` = 120015 sits between panel (120010) and toolbar (120020) | Maintainer directive on 2026-08-26 reversing D-060. Two product reasons: (a) the Shared Globals popover is proven and any evolution risks regressing a working surface, and (b) a popover cannot deliver the tabular, filterable, dense UX the R3 center needs at curation-realistic scale (30–60+ approved controls across 7 categories). A drawer form suits dense curated data with tabs + filters far better than a popover, matches the "design-tool sidebar" mental model dense-work users expect, and leaves the toolbar strip clean while enabling drawer/panel coexistence (the main editor panel opens over/beside the drawer without hiding it). The one new toolbar icon is the smallest addition that satisfies the "avoid a NEW permanent slot unless necessary" spirit while acknowledging that a drawer without its own entry point is not discoverable | `docs/dropins/dbvc-visual-editor-brand-controls-guide/ui-ux/R3-BRAND-CONTROL-CENTER-WIRING-SCHEMATIC.md` §6.1 + §5.1 (drawer contract) + §7 (reuse boundary) + §8 (deliverables); component map §1 (z-index tokens) + §2 (toolbar shell + `createToolbarButtonMarkup`); R3 release doc §"Existing surfaces extended" | Real user testing shows the drawer form is too heavy for the actual control count, or the drawer + main-editor-panel coexistence causes focus / layering trouble that cannot be resolved by tuning z-index or the outside-click policy |
| D-060 | Closed 2026-08-26; **superseded by [[D-061]]** | Original 2026-08-24 wording: "The R3 Brand Control Center opens **as the Shared Globals (Layers) popover**, not as a new toolbar entry and not from the Globe slot. The existing Layers toolbar button becomes the single permanent entry point; the R3 center content renders inside the existing `.dbvc-ve-statusbar-popover` shell; the existing Shared Globals rows appear at the top of the new list alongside any additional registered controls." **D-061 (2026-08-26) reverses this direction**: the Shared Globals popover stays intact and unchanged, and the R3 center becomes a new left-anchored drawer opened from a new toolbar icon. Historical D-060 rationale retained for lineage; do not act on it | Superseded per D-061: the popover form cannot deliver the dense tabular UX the R3 center needs at curation-realistic scale (30-60+ controls across 7 categories), and reversing the "evolve Shared Globals" choice preserves that proven surface untouched | See [[D-061]] | Not applicable — D-061 supersedes this row; a fresh product decision would open a new row, not resume D-060 |
| D-059 | Accepted; implemented (R3-BX) | Curated approval of Brand Control Center controls is done through a **temporary, kill-switch-gated admin admin surface** whose scope is **options-page ACF fields only**, whose output is a **committable JSON + Markdown artifact** written into `addons/visual-editor/curation/`, and whose per-field state persists in a **dedicated option** (`dbvc_visual_editor_curation_decisions`) — never in any Visual Editor runtime setting. The curation tool is admin-only, requires `manage_options` + nonce on every write, and never mutates content (reads ACF metadata + live option values only). Turning off the kill switch (`dbvc_visual_editor_curation_tool_enabled`) hides the admin submenu on the next request; decisions and the exported artifacts survive so the tool can be re-enabled later without data loss | Approval is a **product judgment**, not something the code should infer from ACF structure — enumerating what exists is easy, deciding what is appropriate for a client to touch through the frontend is not. Committing the JSON to the repo means the eventual `VerticalControlProvider` can consume it directly and PR review can see the approved set without opening the tool. Scoping to **options-page fields only** matches the "Global / Site-wide controls" boundary — post-owned and term-owned scopes explode candidate count and are a different curation problem (post-owned per-service style overrides belong in a later phase). Storing state in a dedicated option keeps the surface **completely isolated from any Visual Editor runtime setting** so flipping the kill switch or removing the addon later has zero blast radius | `addons/visual-editor/src/Curation/*`; `addons/visual-editor/src/Admin/CurationPage.php`; `addons/visual-editor/bootstrap.php` (`OPTION_CURATION_TOOL_ENABLED`, `is_curation_tool_enabled()`); `tests/phpunit/VisualEditorCuration*Test.php` (21/76); E-089 | The maintainer decides to persist curation in a different store (e.g. YAML files or a custom table), broaden scope beyond options pages, or replace the manual curation with an automated allowlist |
| D-058 | Accepted; permanent scope narrowing (2026-08-23) | The Visual Editor add-on and the DBVC plugin are **desktop-only** software. Desktop-sized viewports are the sole supported operating range for both the frontend Visual Editor / Media Manager and every DBVC admin surface. Mobile/tablet layouts, responsive cards, slide-overs, touch refinements, real-handset optimization, and any mobile-specific mockup or QA are **permanent non-goals** — not deferred, not tabled, not "later". Real assistive-technology (VoiceOver, JAWS, NVDA) sit-with-a-screen-reader QA is also removed as a required gate. Existing narrow-width protections (`media-manager.css` responsive floor, overlay panel viewport clamps) remain a regression floor **only so they don't break existing desktop behavior at unusual DPI/zoom**; no new mobile-shaped work builds on top of them. Automated axe/keyboard/reduced-motion checks stay in the coverage matrix (they surface real desktop-keyboard defects); real-AT verification does not | Maintainer direction on 2026-08-23. The product boundary is dental / medical / contractor client sites operated by their staff on laptops or desktops; there is no target user population operating the plugin from a phone. Keeping mobile "deferred" in the plan invited scope drift back into every phase's residual-gates list; converting it to an explicit non-goal removes the invitation. Similarly, real-AT verification was carried as a residual gate from R1-E even though it needs an environment (real VoiceOver session) the maintainer has no plan to provide; retiring it as a required gate is honest about what will and will not be proven | `00-GOVERNING-DIRECTIVES.md` (desktop-only directive); `addons/visual-editor/README.md` + `ARCHITECTURE.md` (scope statement); canonical handoff (residual gates updated); [[D-036]] (superseded); [[D-044]] / [[D-049]] (real-AT residuals retired) | The maintainer explicitly reopens either scope — but such a reopening would be a fresh product decision, not a resumption of prior deferred work |
| D-057 | Accepted; implemented (R2-H Slice 5) | The derived JSON mirror is written at **completion boundaries only** — after a rebuild swap, after a first-run build completion, and after a reconcile sweep that actually processed dirty rows — never per-invalidator. Only the SERVING generation is exported (rebuilds-in-flight are internal state). The importer is **guarded**: it runs only when the table is empty AND the mirror file exists AND validates, and it adopts the file's generation as the serving pointer so `entity_ref` HMACs round-trip. A populated table is never overwritten from the mirror — the runtime path stays authoritative | Writing on every invalidator would thrash a JSON file for edits the next completion sweep captures anyway; writing only at completion boundaries keeps the mirror bounded to O(builds + reconciles) writes. Preserving the exported generation on import is what makes refs round-trip — the alternative (mint a fresh generation on import) would silently invalidate every `vemx_` ref a client held. The guard prevents a stale JSON from clobbering a live-populated table (e.g. a partial deploy where the file lags the DB) | `MediaIndexJsonExporter` (`exportAll`/`importIfEmpty`); `MediaIndexBuilder` (`dbvc_visual_editor_media_index_build_completed`); `MediaIndexReconciler` (`dbvc_visual_editor_media_index_reconciled`); `Addon::register` wiring; `VisualEditorMediaIndexJsonExportTest`; E-082; [[D-053]] | A future JSON schema bump (fields added/renamed) requires the schema field, or a maintainer decides on-restore refresh (rebuild trigger) instead of the mirror-only adoption |
| D-056 | Accepted; implemented (R2-H Slice 4b-2) | Topology/exclusion rebuilds are **atomic build-into-fresh-generation-then-swap**, not in-place rotations. `MediaIndexStore` schema v2 extends the per-entity unique key to include `index_generation` so a per-entity building row can coexist with its serving counterpart mid-rebuild; `currentGeneration()` (the SERVING pointer) stays stable during a rebuild while `buildingGeneration()` (a new option) tracks the fresh generation the builder writes into. Only when the builder's final chunk completes does `completeRebuild()` swap the serving pointer and prune every other generation, in one step. To avoid losing edits that land mid-rebuild, `MediaIndexInvalidator` **dual-writes** into both generations while a rebuild is active. Triggers (ACF field-group save/delete, exclusion-option changes, and a `wp_loaded` topology-fingerprint check that catches post-type/taxonomy (de)registration) are **non-clobbering**: a call while a rebuild is already running is a no-op | Rotating the generation up-front (the earlier full-rebuild path) would have flipped reads to a half-built index the moment the rotate happened — a shrinking list until the drain caught up. Extending the unique key by generation is cheaper than a shadow table and preserves the compact one-row-per-entity model outside a rebuild window. Dual-writing during a rebuild is the smallest correctness fix that keeps mid-rebuild edits from evaporating on the swap. The fingerprint check is the smallest way to detect post-type/taxonomy topology drift without firing on every request or coupling to plugin-activation hooks | `MediaIndexStore` (schema v2, `hasActiveRebuild`/`buildingGeneration`/`activeBuildGeneration`/`beginRebuild`/`completeRebuild`); `MediaIndexBuilder` (target `activeBuildGeneration`, call `completeRebuild` on drain finish); `MediaIndexInvalidator` (dual-write); `MediaIndexRebuildController` (new); `VisualEditorMediaIndexRebuildTest`; E-081; [[D-053]] | Slice 5 (JSON export/import) changes the rebuild semantics, or a maintainer chooses a shadow-table strategy over the unique-key extension |
| D-055 | Accepted; implemented (R2-H Slice 4b-1) | The cross-user index is populated by a **structural** build, not a user's scan: a new `MediaIndexBuilder` enumerates every structurally-eligible entity (published/public/show-UI/not-excluded) with the **per-object capability check disabled** (`EligibilityPolicy` `$structural` mode) and indexes each with missing media. Per-user capability stays **read-time** authority (unchanged). Consequently the manual-scan completion hook is rewired from a generation-rotating rebuild (`onScanCompleted`/`rebuildFromSnapshot`) to a non-clobbering refresh-in-place (`onScanRefreshed`/`refreshFromSnapshot`) that upserts only the scanned entities into the current generation. The first build fills the current (empty) generation directly; the atomic build-into-fresh-generation-then-swap is deferred to Slice 4b-2 | The index is **cross-user, site-wide** (D-053), but the scan enumerates only what the *acting user* can edit (`assessPost`/`assessTerm` bake in the capability check) — so the Slice 2 completion-hook population left the shared index holding only one user's editable set, and a background build has no user context at all. A structural build fixes both; keeping capability at read time preserves the security contract. The rotating rebuild had to change or every manual scan would shrink the index back to one user's view | `MediaIndexBuilder`; `EligibilityPolicy` (`$structural`); `MediaIndexProjector::refreshFromSnapshot`; `MediaIndexScheduler` (`BUILD_HOOK`); `Addon` (hook rewire); `VisualEditorMediaIndexBuilderTest`; E-080; [[D-053]] | Slice 4b-2 introduces atomic generation-swap rebuilds, or a site needs per-user (not structural) index population |
| D-054 | Accepted; implemented (R2-H Slice 2c) | The frontend flip to the durable index is **flag-gated and fallback-safe, not a hard cutover**: a filterable bootstrap flag `mediaManager.indexList` (`dbvc_visual_editor_media_index_list_enabled`, default = Media-Manager-enabled) turns on opening from the index; when the index request **fails or returns zero rows on open**, the Manager **automatically falls back to the ephemeral scan** (`/scans/latest`), and "Start new scan" always returns to the scan source. Before flipping, the index list gains **full query parity** (search/entity/field/sort) so no filter control is lost. In index mode the list row is keyed by the opaque `vemx_` entity ref while expansion adopts the detached per-entity snapshot as a **separate per-expansion working identity** (`expansion.scan` + working `vemg_` group), leaving the assign/replace/descriptor pipeline unchanged | The index may be **empty on sites that have not built it yet** (first-run full build is deferred to Slice 4b), so an unconditional cutover would strand users on an empty list; a flag + automatic scan fallback makes the flip safe and reversible per-site. Decoupling the list-row key from the working group (rather than overloading `state.scan`) keeps the scan-mode presentation clean and the mutation pipeline untouched. Query parity first avoids a filtering regression | `releases/R2H-PERSISTENT-MEDIA-INDEX-PHASE-1.md` (Slice 2c checkpoint); `AssetLoader` (`indexList`); `media-manager-app.js`; `MediaIndexReadModel`/`MediaIndexStore`; `VisualEditorMediaIndexQueryTest`; E-078, E-079 | Slice 4b guarantees a first-run build (the empty-index fallback becomes rare), or the maintainer chooses to default the flag off / make the index the only source |
| D-053 | Accepted; R2-H Phase 1 (Slice 1 starting) | The Persistent Media Index (R2-H Phase 1) uses: (a) a **custom table** as the working store plus a **JSON export** into the DBVC sync folder for backup portability (table is source of truth; JSON is a derived, importable mirror); (b) a **cross-user, site-wide** shared index with **read-time per-user eligibility/capability filtering** (stored rows are never authority — the entity is re-resolved and re-checked for the requesting user on list/expand/mutate); (c) **Action Scheduler** for background build/reconcile when available, with a guarded/chunked **WP-Cron fallback**. Opaque refs become stable per **index generation** (not per-scan); mutation stays behind the R2-A bridge + audited pipeline (the index is a read accelerator, never a write authority) | Maintainer steer 2026-08-18 (deferring to the recommended approach): Action Scheduler is the robust queue with a fallback for sites without it; a table is the right store for a queryable, incrementally-updatable index while a JSON export satisfies the backup-portability requirement; cross-user site-wide persistence gives the best UX/consistency/load, at the cost of a read-time capability-filtering redesign which is accepted and built in from the start | `releases/R2H-PERSISTENT-MEDIA-INDEX-PHASE-1.md`; `PROPOSAL-PERSISTENT-MEDIA-INDEX.md`; `MediaIndexStore` | The invalidation catalog or read-time security model proves infeasible at scale, or the maintainer re-scopes to per-user persistence |
| D-052 | Accepted; implemented (R2-F Slice 4) | A finding that was **missing at scan and is now populated** (present in the live assigned inventory) is projected by `expandGroup` as an **`assigned`** field (with a `valueRef` and a replace control), **not** as `resolved_or_changed` with a "refresh the scan" prompt. The genuinely gone/ineligible case (absent from the assigned inventory) keeps `resolved_or_changed`. The row's missing count is unchanged, so the row is still marked done; the row status shifts `resolved_or_changed` → `current` for a fully-resolved row | Field report (2026-08-18): after assigning the featured image on the "Agencies" CPT, the field showed "This field is no longer confirmed missing. Refresh the scan…" with no replace control — a dead end. `resolved_or_changed` is an R2-C-era terminal state that predates replace; once a field is populated and confirmed by the live rescan it should be immediately replaceable without a full rescan. Read-model projection only; the same Slice 3 expected-current-value precondition still gates the replace | `releases/R2F-...` Slice 4; `MediaScanReadModel::expandGroup`; `projectAssignedField`; R2-C tests (assert `assigned` after save); [[RK-013]] | The persistent-index phase changes how reconcile/inventory is stored, or the maintainer re-scopes post-assign UX |
| D-051 | Accepted; R2-F Slice 3 implemented | The replace flow uses **no descriptor pre-call**: unlike the R2-B/C assign path (which calls `/descriptor` before opening `wp.media`), the frontend opens the Media Library directly from the field family and the dedicated `POST .../findings/{finding_ref}/replacement` endpoint performs all revalidation at save time (`resolveReplaceable`). The expected-current-value token is an **opaque value fingerprint** (`vemv_[a-f0-9]{24}` = HMAC of the current stored value under the snapshot generation), surfaced as `valueRef`; the client never receives raw attachment ids. Every non-writable outcome of `resolveReplaceable` is a **hard `WP_Error`** (not a soft finding status), since a replace must fail closed rather than degrade | The assign pre-call exists only to confirm a still-empty field and mint an input hint; for replace the family is already known from the inventory projection, so a pre-call would add a round-trip and a second revalidation window for no safety gain. An opaque fingerprint gives the expected-current-value guarantee without exposing the current target. Hard errors keep the overwrite path unambiguous | `MediaFindingDescriptorBridge::resolveReplaceable`; `MediaAssignmentService::replace`/`applyMutation`; `MediaManagerController` `/replacement`; `media-manager-app.js` (`beginReplaceMedia`); `VisualEditorMediaManagerR2FReplaceTest`; [[RK-012]]; E-065 | A shared `wp.media` factory consolidation or a batch replace changes the frame/endpoint contract |

## Decision entry template

```text
ID:
Date:
Status: proposed / accepted / superseded / rejected
Decision:
Context:
Options considered:
Rationale:
Evidence:
Consequences:
Revisit trigger:
```
