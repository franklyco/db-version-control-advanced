# Session handoff

Prepared 2026-09-16. No code has been changed in the user's DBVC checkout by this package. No site has been enrolled, migrated or contacted by its scaffold.

Current goal: M0 local reconciliation, then first coherent M1 local observation/outbox slice.

Package architecture: two independently gated add-ons in one repository; shared contracts; existing DBVC engines retained. Prior separate-repository packaging is superseded.

Known source baseline: `franklyco/db-version-control-main`, `codex/visual-editor-r5-later-arc`, `977888535a306989e48c5a34104fb9d101298cf3`. Branch head rechecked 2026-09-16. Additional local dirty Bricks work may exist. Actual local branch and files have not been inspected here.

Preparation evidence is in `evidence/PACKAGE-VALIDATION.md`. Source evidence is not runtime verification.

## Local agent updates (2026-09-17)

- Active checkout/path/HEAD/dirty summary: work happened on branch `claude/dbvc-connected-agency-m0-ac4c5d` (worktree `.claude/worktrees/dbvc-connected-agency-m0-ac4c5d`, based on `master` @ `c88764b`). The main checkout stays on `codex/visual-editor-r6-site-manager-workspace` @ `59f686d`, dirty, untouched. This package was copied into the branch verbatim (commit `d991b32`).
- Active WP plugin path and runtime versions: the live site `dbvc-codexchanges.local` loads the main checkout (`/Users/rhettbutler/Documents/LocalWP/dbvc-codexchanges/app/public/wp-content/plugins/db-version-control-main/`), PHP 8.4.4, MySQL 8.0.16, WordPress 7.1.1, Bricks 2.3.8; it does not load this branch.
- Completed slice and source owners: M0 record and the M1 local observation slice — `includes/Dbvc/ConnectedProtocol/` (contracts), `addons/connected-environments/` (connector: gate, schema, capture, worker, observer, identity, exclusions, status), `addons/agency-control/` (inert hub gate), `commands/class-connected-cli.php`, Add-ons subtab in `admin/admin-page.php`, `dbvc_import_options_data` filter in `includes/class-sync-posts.php`. Full record: `docs/implementation/active/connected-environments-agency-control.md`.
- Exact tests and authoritative assertions: `vendor/bin/phpunit --filter "ConnectedEnvironments|ConnectedProtocol"` → OK (35 tests, 543 assertions) incl. held-migration gate, HTTP trap, export-disabled capture, coalescing (generation 3), 4 events for 3 members + order, no duplicate events on reprocess, single-member edit → 1 event, reorder → order event only, tombstone + restore with preserved identity, option deletion → 3 tombstones, generation advance during processing → marker pending then settled state wins, expired lease reclaim + stale worker fenced, duplicate keys fail closed without tombstones, non-finite float → incomplete, unavailable Bricks holds marker, variables domain independent, identity-uninitialized block, CLI inspector shapes. Disposable WP-CLI lab evidence in `docs/agents/RUNTIME_VERIFICATION.md` (2026-09-17). Full suite: 718 tests, 6 pre-existing failures identical on base `c88764b`.
- Capability/doc changes: manifest records `addon.connected_environments.observation`, `cli.connected.inspect`, `cli.connected.process`, `addon.agency_control.scaffold`, `protocol.connected.contracts`; `engine.core.import` owns `dbvc_import_options_data`; agent-docs hook IDs now occurrence-based (177 IDs remapped); `composer agent-docs:check` passes (59 records, 424 surfaces, 0 unmapped). Roadmap, agent entrypoints, active README, taxonomy, CHANGELOG updated.
- Blocked or untested: same-checkout live-site verification (site loads another branch); emergency constant and multisite only by lab/source review; Bricks trash/categories/templates not covered; no transport/enrollment/auth/hub storage.
- M2 step 1 (2026-09-18, branch `claude/dbvc-connected-agency-m2-receipt` stacked on PR #11): hub enrollment exchange with application-password principals, scoped observation receipt with per-event outcomes and durable `routing_state=pending`, sequence-guarded projections, site-URL hold, revoke/release, `wp dbvc agency`; connector schema v2, sealed credential, hub client (`rest_route` form), enrollment exchange with superseded history + epoch-restarted sequences, leased outbound delivery with holds, `wp dbvc connected enroll|deliver|resume`. PHPUnit 44/781 OK; two-server real-HTTP lab passed and was dropped. See the implementation guide's M2 sections.
- M2 step 2 (2026-09-19, branch `claude/dbvc-connected-agency-m2-routing` stacked on PR #12): hub subscriptions + pure RoutingPolicy + RoutingWorker → deliveries (frozen policy revision) and review items; `GET /inbox` + `POST /inbox/ack` with retrieval re-authorization; connector inbox poll (store-before-ack, crash recovery, cursor); `wp.service` observer through DBVC identity/masking with per-object markers and tombstones; status rows. PHPUnit 59/1060 OK; three-server real-HTTP lab ran the first scene (class → review + staging, service → staging only, offline staging caught up) and was dropped.
- M3 step 1 (2026-09-19, branch `claude/dbvc-connected-agency-m3-baselines` stacked on PR #13): hub baselines + instance links, read-only three-way `ComparisonService` (coverage from order projections, freshness, observed flags), explicit `BaselineService::confirm` (+ `--accept-absent`), `wp dbvc agency compare|baseline-confirm|baselines|link-instance|unlink-instance|links`. Bricks add-on mothership/client review recorded (ADR-027). PHPUnit 62/1151 OK; three-server lab with two real connectors passed and was dropped.
- M3 step 2 (2026-09-19, branch `claude/dbvc-connected-agency-m3-framework` stacked on PR #14): hub schema v4 — `dbvc_ac_definitions` (immutable versions, studio-supplied order unique per channel, per-channel desired flag), `dbvc_ac_overrides` (exact approved hash, frozen policy revision, rationale/approver, `approved|needs_rebase_review|detached`), `adopted_version`/`channel` on framework subscriptions; `FrameworkService` (publish from hash / environment projection / published version, desire, adopt, approve, detach) and read-only `FrameworkStatusService` over `Comparison::framework()` with `behind_version|ahead_version|channel_mismatch`; `wp dbvc agency definition-publish|definition-desire|definitions|adopt-version|override-approve|override-detach|overrides|framework-status`. ADR-028…030. PHPUnit 68/1297 OK; two-server lab with a real Bricks connector passed and was dropped.
- M3 step 3 (2026-09-19, branch `claude/dbvc-connected-agency-m3-admin` stacked on PR #15): connector `wp.service` inventory projection (`wp-service-inventory-v1`, coverage for absence verification); hub schema v5 review columns + `ReviewResolutionService` (`review-classify`/`review-resolve`, resolution through the status report only); `HubPanel` (environments, invitation form with show-once token, open invitations, framework status, review items) and connector `InboxPanel` in the Add-ons tab. ADR-031…033. PHPUnit 73/1480 OK; three-server lab + built-in-browser admin verification passed and was dropped.
- M4 step 1 (2026-09-19, branch `claude/dbvc-connected-agency-m4-prepare` stacked on PR #16): hub schema v6 (`dbvc_ac_releases`, `dbvc_ac_release_items`, `dbvc_ac_preparations`), `ReleaseService` (manifest from source projections, hash-verified payloads, seal/withdraw), `PreparationService` (requests, receipts with hub annotations), four connector-initiated routes; connector schema v4 (`dbvc_ce_preparations`), `ReleaseWorker` (payload answers, prepare dry runs via `Preparer`, store-before-report, resend), `wp dbvc connected release|preparations`, `wp dbvc agency release-create|release-withdraw|releases|prepare-request|preparations`. ADR-034…036. PHPUnit 75/1654 OK; three-server lab round trip (sealed release, `partial` then `ready` after `link-instance`, target unchanged) passed and was dropped.
- M4 step 2 (2026-09-19, branch `claude/dbvc-connected-agency-m4-ledger` stacked on PR #17): `DependencyLedger` on prepare receipts (Bricks categories + `var(--name)`, service parent/terms/media; unresolved/unsupported block), deletion items (hub schema v7 `operation`, canonical `null` payload, `remove_object`/`noop`), release + receipt tables in both admin panels. ADR-037…038. PHPUnit 76/1763 OK (no new lab; transport unchanged since the step-1 lab).
- M5 step 1 (2026-09-19, branch `claude/dbvc-connected-agency-m5-apply` stacked on PR #18): hub schema v8 `dbvc_ac_approvals` + `ApprovalService` (approve/revoke/apply-requests/apply-receipts, baseline advancement for verified applies); connector apply gate (`dbvc_addon_connected_environments_apply_enabled`), schema v5 `dbvc_ce_operations` journal, `Applier` (Bricks option containers: raw re-read, fingerprint precheck, journalled before image, single conditional SQL write, observer verification, conditional compensation, origin=apply report), `wp dbvc agency approve|revoke-approval|approvals`, `wp dbvc connected operations`. ADR-039…042. PHPUnit 79/1969 OK; three-server lab: applied + verified + synchronized, concurrent edit → stale with the edit intact; dropped.
- Next bounded task: M5 step 2 — reviewed rollback (restore journalled before image conditionally, restore conflict otherwise), `wp.service` apply, Bricks CSS regeneration, admin approval action. Live-site gate stays the operator's call.
- Earlier handoff inventory (2026-09-18): docs-only proposal `docs/implementation/proposed/agency-control-connected-environments/` (untracked in the main checkout, separate-hub-repository plan, source ZIP 2026-09-13) reviewed and preserved untouched; its M1 contract and acceptance cases are met by this slice; superseded where architectures differ.
- Decisions changed and why: ADR-013 to ADR-018 in `DECISIONS.md` (core option hooks instead of export-gated hook; separate canonicalizer; lifecycle-only table creation; order projection inside domain; reconciliation on enable; import-side exclusion filter).

Use existing repository evidence records for detailed capability verification; this is only a compact continuation aid.
