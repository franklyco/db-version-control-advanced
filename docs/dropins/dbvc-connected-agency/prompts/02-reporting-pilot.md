# M2 reporting pilot prompt

Continue DBVC Connected + Agency Control from the current session handoff and actual code. Confirm M1's exit evidence rather than assuming the package implemented it. Read only the protocol, routing, lifecycle and lab documents needed for M2.

Implement authenticated hub enrollment, scoped observation receipt, durable target delivery, outbound connector batching/polling, and a minimal freshness/error/inbox view. Add the service CPT observer through existing DBVC identity and metadata owners. Reuse verified legacy transport primitives through adapters when they meet the new contracts; keep old shared-rules behavior compatible. Choose one authentication scheme and record why.

Run the two-client disposable lab, covering subscribed/unsubscribed classes, service content, offline targets, disabled subscriptions, revoked credentials, duplicate and mismatched replay, out-of-order events across different objects, a forged apply-origin claim, crash/retry and persisted-before-ACK. Show actual source content remains unchanged by reporting.

Do not add content application or promote a received client edit into the framework catalog. Raw framework review can be pending comparison until M3. Keep hub-only activation independent of Bricks and the connector. Finish with exact evidence, measured overhead, unresolved limitations and the M3 boundary.
