# M3 comparison prompt

Continue from verified M2 source and runtime evidence. Implement per-pair/object/profile environment baselines and separately adopted framework definitions, versions and approved overrides. Use the three-way comparison fixtures; identity, completeness, freshness or profile mismatch must produce unknown/baseline-required states, not clean.

Notification ACKs never advance a baseline. Framework review does not update an approved definition automatically. An approved override has an exact value hash and adoption context; later changes create new drift, and new framework releases can require rebase review. Provide an understandable fleet/client/object view using existing UI conventions and semantic CSS tokens.

Validate the full comparison matrix in the disposable lab. Do not implement application in this slice. Record permissions, read-only versus management-write actions, before/after state and relevant capability documentation.
