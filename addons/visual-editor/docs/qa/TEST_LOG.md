# Visual Editor Test Log

## 2026-04-28

### Confirmed manually in this thread
- Visual Editor addon enabled from DBVC config and loads on singular frontend views.
- Admin-bar toggle activates Visual Editor mode and the frontend status bar appears.
- Marker discovery, descriptor session bootstrap, and authenticated descriptor lookup work.
- Shared badge runtime now uses one active badge with lightweight session bootstrap and on-demand descriptor loading.
- Visual Editor sessions now default to a longer filterable server TTL and the frontend refreshes that session periodically plus on focus/visibility return; descriptor/save endpoints now return an explicit “session expired, refresh the page” message instead of a generic missing-descriptor error when the transient has aged out.
- The frontend runtime now also has a bounded viewport-aware descriptor warmup path: an `IntersectionObserver` tracks nearby visible markers, background warmup reuses the same descriptor cache plus in-flight request map as explicit opens, and the queue pauses during save, reload-after-save, session-expired, and Media Library modal states instead of hydrating the whole page.
- Simple save flow works without reloading the page.
- The in-page editor panel works in place of the browser prompt.
- Guarded structured-field updates and recent Bricks link slices were reported working by the user.
- Live authenticated browser probing against `https://frameworkflo-live.local/vertical/dentists/` confirmed visible VE tokens on previously failing related-owner elements such as `.brxe-ozyswq` and `.brxe-zecvno`.
- Panel follow-up was intentionally paused after the recent grouped/flexible contract work so the next resume point is live save verification for nested grouped descendants, not more marker discovery.
- A targeted runtime inspection confirmed Bricks grouped ACF tags such as `acf_assortment_group_name` and `acf_assortment_group_media_image_primary` resolve as group descendants rather than flat leaf fields, and the direct grouped save path was hardened accordingly.
- A targeted runtime inspection confirmed native Bricks ACF loop roots resolve cleanly from real site data for `acf_process_section_process_steps` (repeater), `acf_related_faq_groups` (relationship), `acf_services_section_services_relationship` (relationship), and `acf_office_manager` (post_object).
- A targeted runtime inspection on page `24732` confirmed native grouped repeater roots must read from the full selector `process_section_process_steps`; the shorter duplicated parent name `process_steps` returns `null`, which explained why `guzahf` descendants were losing markers during render verification.
- A targeted runtime inspection on page `24732` confirmed Bricks duplicate repeater child tags such as `acf_process_steps_step_name` and `acf_process_steps_step_description` were carrying the wrong subfield keys (`field_658fc227...`) even though the real `process_section_process_steps` rows use `field_650b7ca2a58b1` / `field_650b7ca2a58b2`; the resolver now rebinds those duplicate child tags against the actual repeater container definition before classification and render verification.
- A targeted runtime inspection on page `24732` confirmed nested grouped repeater descendants such as `acf_step_duration_group_step_duration_value_1` carry the correct group-child key but were previously missing repeater-row context because their immediate Bricks ACF parent is `group`, not `repeater`; the resolver now inherits the native repeater container context for those grouped descendants and marks them as supported repeater-row fields.
- A resolver-level runtime probe confirmed grouped repeater descendants on page `24732` now read through raw row payloads using `group_key_path` as well as `group_path`; a synthetic descriptor for `acf_step_duration_group_step_duration_value_1` returned the real row value `"5"` when pointed at `process_section_process_steps` row 1.
- A pure in-memory verification probe confirmed the new row-rebind fallback can uniquely match rendered row text such as `Step Four`, rebind a repeater descriptor from row index `2` to `3`, rebuild the descriptor path summary as `row:4`, and regenerate a row-aware source group instead of dropping the marker.
- The next native repeater hardening slice now covers repeater-in-repeater descendants under a repeater root, such as `acf_quantities_object` inside product post `20526` (`Grow Plan`) where Bricks loops `yriblb -> dujolg -> wevhbe` render `_price_item_repeater` rows and nested `_price_item_repeater_*_quantities` rows. The contract now treats the outer repeater as the writable root and carries explicit nested repeater row segments for the inner `quantities` loop instead of flattening the inner loop to a fake top-level repeater root.
- A direct runtime resolver probe on product post `20526` confirmed the nested repeater contract can now read real inner row values from the stored `_price_item_repeater` tree: row `0 -> quantities -> 0 -> object` resolved to `10+ SEO Ready Pages`, and row `0 -> quantities -> 1 -> object` resolved to `100+ SEO Ready Services`.
- A follow-up runtime probe on the homepage pricing slice showed a second native nested-loop failure class specific to inserted-template rendering: Bricks emits the inner loop object type as `acf_price_item_repeater_quantities`, but the actual owner field family is rooted at `_price_item_repeater`, and `get_field_object()` cannot resolve `_price_item_repeater_quantities` directly. `NativeAcfQueryResolver` now falls back to owner-aware nested selector traversal, starting from the longest resolvable root field object and walking down child segments. The confirmed post-fix probe on product `20526` now resolves `acf_price_item_repeater_quantities` as `fieldKey = field_66008aae459b2`, `fieldType = repeater`, and `kind = repeater` instead of `unknown`.
- The next native owner-loop groundwork slice now carries full native-loop ancestry, not only one `parent_native_query`, through the exported loop contract and descriptor source/path/mutation layers. This is aimed at upcoming shapes like `acf_related_faq_groups -> acf_faq_items_repeater` and later `relationship/post_object -> flexible/repeater` descendants, and it also folds the ancestry into live source/sync grouping so identical nested fields under different owner-loop chains do not collide.
- A targeted runtime probe on page `88` with template `923` confirmed the native `relationship -> repeater` path is now usable as the first widened owner-loop descendant fixture: `bdxtme` (`acf_related_faq_groups`) owns related FAQ post `863`, `hudkbu` (`acf_faq_items_repeater`) resolves repeater rows on that related post, and descendants `yimqpq`, `zvywab`, `lisgki`, and `bwgvtd` classify as editable `related_entity` row-backed descriptors with `parent_native_query_kind=relationship` and full native query ancestry.
- User testing confirmed the FAQ repeater descendant save path works for the native `relationship -> repeater` fixture, so the current scalar descendant branch is closed for that source shape.
- A separate probe on page `19` with template `24551` confirmed an important non-native contrast case: the FAQ section there is a Bricks custom Query Editor post loop that reads ACF relationship data into `post__in`, so it remains a valid related-owner repeater path when Bricks exposes a concrete `WP_Post`, but it does not provide native ACF relationship ancestry and should not be used as the native relationship fixture.
- Structured synced-template scanning found 25 native `relationship -> repeater` examples, but no live `relationship -> flexible`, `post_object -> repeater`, or `post_object -> flexible` descendant fixture. Direct post-object loops such as `acf_office_manager` remain in scope for direct related-owner fields, while nested relationship-flexible and post_object descendant widening stays WIP until a real fixture is added or identified.
- A synthetic runtime probe for `post_object -> repeater` used page `19`, native post-object loop `acf_office_manager`, related team-member post `22909`, and nested cloned repeater `acf_team_member_social_repeater_social`. The probe confirmed 8/8 synthetic descendants classify as editable `related_entity` row-backed descriptors across rows `0-3`, with `parent_native_query_kind=post_object`.
- A follow-up resolver read probe confirmed the cloned repeater parent selector `team_member_social_repeater_social` is not loadable by `get_field()` as an assembled repeater, but the Visual Editor resolver fallback reads existing expanded postmeta values correctly: row `0` `url` returned `https://www.instagram.com/itsrhettbutler`, and row `3` `profile_name` returned `franklyco.bsky.social`.
- Direct inspection of the saved `/our-process/` VE session on `dbvc-codexchanges.local` confirmed that row-4 native repeater descendants were missing before the frontend badge layer; only rows `0,1,2` were being persisted for `figdnw`, `uygtdn`, `bonmng`, `jjabji`, and `wjpthy`, which isolated the issue to descriptor creation/seed collapse rather than panel rendering.
- An escalated WordPress runtime render probe on `/our-process/` showed the deeper cause for the stubborn row-4 failure: Bricks was exposing `loop_object_id = 3` on the fourth native repeater row, and VE was incorrectly treating that bare numeric index as a concrete related post because a real post with ID `3` exists in WordPress. That flipped row 4 to `scope=related`, and `render_element` stripped its markers even though `render_attributes` had added them.
- The follow-up runtime probe after the `LoopContextResolver` fix confirmed row 4 now stays `scope=current` and keeps `data-dbvc-ve` through `render_element` for `figdnw`, `uygtdn`, `jjabji`, `wjpthy`, and `bonmng`; only the genuinely empty `iuxcry` image and the unsupported mixed-string `lpfhov` stay unmarked on that row.
- A targeted runtime probe against the term-loop image tags in `flo-explainer-single` confirmed another realistic failure class for media verification: grouped term-image tags such as `acf_core_tax_group_term_image` resolve as valid ACF image fields on concrete term owners, but media URLs can differ between the resolver and the rendered `<img>` by host alias and generated size suffix. VE now normalizes media comparisons by upload path identity for `image_src` and `background_image` contexts instead of requiring exact URL-string equality.
- The follow-up frontend mismatch check for those same term-loop images also needed to be aligned: the panel was still using raw string comparison and could show a false “saving is disabled because the resolved backend value does not match the rendered page value yet” warning even after the backend verifier accepted the marker. The browser-side mismatch gate now honors descriptor `render.value_match` and falls back to the same normalized media-path comparison used by the PHP verifier.
- Stable flexible row mutation is no longer blocked solely because the owner is not the current post. Text-like, WYSIWYG, choice, link, image, and gallery flexible descendants can now save across shared post/term/user/option owners through the existing flexible row read/write path and the explicit `shared_flexible_layout` contract.
- Shared non-current post flexible descendants are currently in WIP/paused status on real pages after the readonly gate was removed; keep the article/case-study flexible-layout smoke targets available for later confirmation before treating that branch as fully closed.
- Direct/repeater/flexible gallery collection replacement is currently in WIP/paused status on real pages; the save contract is live, but final browser confirmation should still cover non-empty galleries and the reload-after-save flow when that branch is resumed.
- The connected-items collection editor now widens beyond direct current-owner query roots in code: direct current-owner repeater-row and flexible-row `relationship` / `post_object` query roots can now flow through the same `acf_collection_field` + `query_collection` contract by reusing the existing row-backed ACF read/write layer with explicit repeater/flexible source metadata.
- The connected-items collection editor now also carries explicit `container_ancestry` metadata for mixed current-owner `repeater -> flexible` and `flexible -> repeater` query roots, and the save resolver now prefers canonical nested container traversal before falling back to the older direct row-root path.
- The same connected-items resolver path now also traverses `group_path` / `group_key_path` inside canonical row-owned collection ancestry, so grouped current-owner relationship/post-object roots can resolve the final nested collection container instead of stopping at the parent repeater/flexible row.
- The collection classifier now also accepts concrete loop-owned related post owners for native ACF `relationship` / `post_object` query roots, and the mutation builder now emits collection-specific related-owner contracts such as `loop_owned_relationship_collection` and `loop_owned_post_object_collection` instead of collapsing those roots into generic loop-owned scalar field contracts.
- A focused Playwright smoke harness now exists at `tests/playwright/visual-editor-connected-items.spec.js`. In this shell it skipped cleanly because `DBVC_E2E_WP_ADMIN_URL`, `DBVC_E2E_WP_ADMIN_USER`, and `DBVC_E2E_WP_ADMIN_PASS` were not present, so live authenticated VE browser confirmation for the connected-items panel is still pending.
- An escalated live WordPress render probe on `/our-process/` isolated a native flexible image failure to duplicated Bricks layout aliases rather than missing media support: `xhcpsg` was tagged as `acf_flexible_layouts_dynamic_section_image`, but the actual row payload under `core_sections_flexible_layouts` was `standard_section` with image key `field_6701b14a44986`. `AcfFieldContextResolver` now canonicalizes flexible context from the real row `acf_fc_layout` and matching layout key before subfield resolution, and the follow-up probe confirmed the descriptor rebinds to `standard_section`, resolves attachment `22997`, and keeps `data-dbvc-ve` through final render.
- Grouped descendants now also promote their raw ACF key ancestry into `path.groupKeyPath` and keyed `group` segments, so replay, journaling, and future universal nested-path handling no longer have to recover grouped key identity only from source metadata.
- The first current-owner connected-items editor slice now exists in code for direct native Bricks ACF `relationship` and `post_object` query roots. It uses a dedicated query-root descriptor family (`acf_collection_field`), a dedicated resolver (`acf_reference_collection`), a reference-search REST endpoint, ordered add/remove/reorder controls in the panel, and reload-after-save reconciliation. This slice has passed syntax validation but has not been browser-smoked on a real page in this thread yet.
- The connected-items selected list now renders as object/CPT/term-type accordions that are collapsed by default and uses smaller selected-item move/remove controls. This is a client-side panel rendering change only; browser visual confirmation is pending.
- Row-backed ACF saves now perform a stricter path preflight before mutation: repeater/flexible descendants can still fill an empty leaf inside a proven row, but saves no longer create missing nested repeater containers, missing nested repeater rows, or missing grouped containers from descriptor metadata alone. This hardens the current native-loop branch before widening additional relationship/post-object/taxonomy descendants.

### Pending targeted smoke tests
- Bricks button and Bricks link elements backed by current-post ACF fields.
- Bricks ACF relationship/post-object query loops where loop rows render related-post `post_title`, `post_excerpt`, or safe direct ACF text-like fields.
- Related-post loop saves should require acknowledgement and mutate the related post owner, not the current page.
- Related-post loop modal should show loop ownership context clearly.
- Concrete queried term and user loop owners should surface `Related Term` / `Related User` context and save safe direct ACF fields against that loop owner.
- Current-post ACF repeater rows should mark and save text-like, WYSIWYG, link, and image descendants without cross-row sync bleed.
- Related-post ACF repeater rows nested inside Bricks relationship/post-object loops should show related-post ownership and save against the related owner.
- Native `post_object -> repeater/flexible` live-fixture work is paused; synthetic `post_object -> repeater` classification/read coverage exists, but browser save smoke is not currently being pursued.
- Exact single-tag content wrapped by one HTML node, such as `<p>{acf_faq_items_repeater_answer}</p>`, should still mark when the wrapped tag is otherwise save-capable.
- Direct flexible-content descendants should show parent flexible field, row, and layout context in the modal once surfaced.
- Current-post and related-post flexible text-like/WYSIWYG/choice/link/image descendants should save against the targeted flexible row only.
- Flexible descendants in Bricks loops where the loop object type is a fuller ACF path such as `acf_core_sections_flexible_layouts` but the rendered child tags are shortened to `acf_flexible_layouts_*` should still surface and resolve as editable when otherwise supported.
- Direct ACF fields inside Bricks custom query-editor post loops should still surface and resolve as editable when Bricks exposes a real `WP_Post` object or a CPT slug such as `benefit` instead of the literal loop type `post`.
- Nested ACF group descendants with the same leaf field names under different group roots should not cross-sync after save now that `group_path` and leaf selector identity participate in `source_group` / `sync_group` hashing.
- Row-backed grouped/nested saves should fail safely instead of creating missing group or nested repeater containers when the stored ACF payload no longer matches the descriptor path.
- Direct grouped ACF fields should save through their full selector path, not only the ambiguous leaf field name, especially for grouped leaves such as `name` and `image_primary`.
- Bricks native ACF repeater loops such as `acf_process_section_process_steps` should surface/edit supported child text, WYSIWYG, link, and image fields when rendered through the native Bricks query loop rather than only through earlier custom-loop slices.
- The fourth native ACF repeater row on page `24732` should surface the same supported VE markers as the first three rows, including row-4 fields like `process_section_process_steps_3_step_label`, `process_section_process_steps_3_step_description`, and `process_section_process_steps_3_step_duration_group_step_duration_label`.
- Native repeater rows on page `24732` should no longer collapse when Bricks reuses the same loop signature for later rows; the saved VE session should persist row `3` descendants for `figdnw`, `uygtdn`, `bonmng`, `jjabji`, and `wjpthy`.
- Bricks native ACF relationship loops such as `acf_related_faq_groups` or `acf_services_section_services_relationship` should resolve the current related owner cleanly and allow supported fields on that owner to save through the explicit loop-owned contract path.
- Bricks native ACF post-object loops such as `acf_office_manager` should resolve the selected owner cleanly and allow supported fields on that owner to save through the explicit loop-owned contract path.
- Standard Bricks `objectType: term` loops in `flo-explainer-single` should surface direct grouped ACF image fields such as `acf_core_tax_group_term_image`, `acf_core_tax_group_term_image_secondary`, and `acf_service_global_term_details_term_image` when those term fields are populated, even if the rendered image URL differs from the resolver URL by domain alias or WordPress size suffix.
- Taxonomy term repeater/flexible descendants are now active as a guarded QA target because `service_area` term field groups include grouped repeaters under `vf_sa_group_content` and `vf_sa_group_geometry`.
- Current scope is existing-row/current-term or concrete loop-owned term descendant writes only; shared term collections, taxonomy selector collection mutation, gallery collection replacement, and row/layout insert-remove-reorder are still deferred.
- Synced template search found direct `service_area` term fields in `bricks_template-archive-taxonomy-service-areas-120111.json`; a live rendered nested term repeater/flexible fixture still needs confirmation before this branch can be marked live-save closed.
- Runtime gate probe `/private/tmp/dbvc_ve_taxonomy_nested_probe.php` confirmed synthetic current taxonomy archive term repeater descendants and concrete loop-owned term repeater descendants return editable resolver state and writable `repeater_row` / `loop_owned_repeater_row` contracts.
- The same probe confirmed shared term repeater descendants remain locked with `term_nested_owner_pending`, and nested relationship-style descendants remain locked with `term_nested_field_type_pending`.
- The local DB probe did not find stored `service_area` repeater rows yet, so marker surfacing and no-op/save behavior still need a real rendered fixture before this branch is considered live-save closed.
- Post-owned linked-term collection probe `/private/tmp/dbvc_ve_post_terms_collection_probe.php` classified a Bricks `objectType: term` + `current_post_term` candidate for taxonomy `filter-tag` as editable against post `107413`, resolved the `post_terms_collection` resolver, produced badge `Filter Tag Terms`, confirmed the save contract is writable, and returned current/search term items.
- Empty post-owned linked-term loops now have backend marker support: empty Bricks element renders for `objectType: term` + `current_post_term` can register a synthetic `post_terms_collection` descriptor only when one owner post, one taxonomy, and an empty assigned-term set are proven; live marker placement and save UX still need browser QA.
- Linked-term collection live browser QA is WIP: verify marker surfacing on a real nested term query-loop container, panel load, term search, save/no-reload, optional reload, and rendered term chip updates.
- Bricks native taxonomy/terms element handling is entering a guarded first slice for `post-taxonomy` elements with one explicit `taxonomy` setting and a proven current or loop-owned post owner.
- Native `post-taxonomy` probe `/private/tmp/dbvc_ve_native_post_taxonomy_probe.php` confirmed root inspection emits `query_source: bricks_native_post_taxonomy_element`, resolver classification returns editable `post_terms_collection`, badge `Category Terms`, writable contract true, and term search results.
- Native `post-taxonomy` owner-hint probe `/private/tmp/dbvc_ve_native_post_taxonomy_owner_hint_probe.php` confirmed a rendered card post hint resolves as `related_entity`, targets the hinted card post ID instead of the current page ID, and remains writable.
- Repeated `post_terms_collection` query-collection badges are now grouped by source owner/taxonomy before Bricks query/element ID, so repeated post cards can expose separate term-management badges instead of collapsing to the first rendered card token.
- Native `post-taxonomy` markers now include the rendered post ID as an owner hint and can show the standard hover/focus badge fallback, addressing repeated cards where terms were outlined but no actionable term editor badge appeared beyond the first card.
- Missing/conditional Bricks image/media markers now have a first guarded slice in code: proven direct `image_src` / `background_image` ACF image and featured-image descriptors can survive empty Bricks image output, but only surface a parent-anchored `data-dbvc-ve-missing-media="1"` marker when the resolved backend media value is empty. Save defaults to reload because the absent Bricks image element cannot be patched reliably in place. Missing-media targets now keep a minimum `2rem` hover/click hit area so empty wrappers such as image-card containers can reveal the edit badge.
- Rendered single-media no-reload clear patching is implemented for `image_src` and `background_image`: clearing a saved image removes or clears the visible media child while preserving the marker target, and background clears remove the inline background image. Browser QA still needs to verify direct image, featured image, and background clear/add-back flows. User QA on 2026-06-13 confirmed the `xxrpfg` Bricks image-gallery add/replace/remove/reorder/no-reload save path still behaves as before after this single-media patch.
- A temporary live render probe on `https://dbvc-codexchanges.local/vertical/websites-for-contractors/` confirmed the Benefits section related benefit card with empty `.brxe-ktiaxo` image wrapper now receives one related-owner `image_src` marker on the parent wrapper, with `data-dbvc-ve-missing-media="1"`, label `Add Image (primary)`, input `media_reference`, and five normal marked `.brxe-zecvno` image markers for the populated sibling cards. Browser panel/media-library save remains pending user QA.
- The follow-up missing-media probe confirmed deeper fallback behavior: when a configured image parent is absent, the marker injects onto the nearest known rendered ancestor; when the immediate parent already has a Visual Editor marker, the parent is skipped and the nearest unclaimed ancestor receives the missing-media marker. The live Benefits target still anchors to `.brxe-ktiaxo` with `data-dbvc-ve-missing-media-anchor="ktiaxo"`.
- Follow-up read-only media-context probes on 2026-06-13 confirmed rendered image/background marker coverage after the single-media clear patch: `/vertical/websites-for-contractors/` rendered 9 `image_src` markers and 2 missing-media anchors; `/our-process/` rendered 35 `image_src` markers and 20 missing-media anchors; `/service-areas/ohio/akron/` rendered one `background_image` marker from template `120111`, element `117a72`, term `437`, and ACF field `vf_service_area_hero_image`. The in-app browser session was not authenticated (`DBVCVisualEditorBootstrap` absent, no admin bar, 0 markers), so direct image/background panel clear/add-back save QA remains manual/authenticated-browser work.
- Flexible-content descendants owned by shared term, user, and option entities should now be retested for text-like, WYSIWYG, choice, link, and image subfields; the expected behavior is editable markers plus the existing shared-scope acknowledgement flow instead of inspect-only state.
- `/our-process/` flexible-content image descendants rendered in `ivqecf` should now stay editable even when Bricks emits the wrong flexible layout alias in the child tag, especially `xhcpsg` with `{acf_flexible_layouts_dynamic_section_image}` inside a real `standard_section` row.
- Direct Bricks gallery collections, including stable repeater and flexible row gallery descendants, should now allow ordered Media Library replacement and then reload the page after save so Bricks can rebuild the gallery markup.
- Empty direct Bricks gallery collections now follow the same guarded parent-anchor fallback as missing images when the descriptor proves a direct ACF gallery source and the backend gallery value is empty. The marker uses `data-dbvc-ve-missing-media-kind="gallery"` with `Add Gallery`/field-label badge copy, writable galleries route to the ordered Media Library selector, and readonly galleries remain preview-only.
- Direct current-owner native Bricks ACF `relationship` query roots should now surface a container-level `Edit Connected` badge, list the current connected posts in stored order, allow search/add/remove/reorder, and reload the page after save.
- Direct current-owner native Bricks ACF multi-`post_object` query roots should now use that same connected-items panel mode and reload-after-save reconciliation.
- Native `Edit Connected` query-collection badges now use the container-level overlay badge path instead of the shared per-marker hover badge, so repeated query-loop items should no longer each surface their own `Edit Connected` button. Browser visual confirmation is pending.
- Derived custom Bricks Query Editor post loops backed by one provable current-owner relationship/post_object field now have an inspect-only first slice: final Bricks `posts/query_vars` are captured, `post__in` plus a single target `post_type` must exactly match one current-owner ACF field subset, and the loop root can surface a locked `Modify Linked Posts` badge. Writable subset replacement remains untested/pending; first browser smoke should use a mixed relationship field such as `page_related_items` filtered to one CPT and confirm the panel lists the source field, target post type, queried subset, preserved non-target IDs, and save-disabled warning.
- Runtime probe `php /private/tmp/dbvc_ve_derived_collection_probe.php 19 feature` classified page `19` / `page_related_items` / `feature` subset as `readonly` current-owner `acf_collection_field`, returned badge label `Modify Linked Posts`, and preserved the non-feature IDs in source metadata. This validates the classifier branch only; browser marker surfacing and panel loading still need live Visual Editor smoke.
- Runtime render probe `php /private/tmp/dbvc_ve_render_probe.php 19` initially found zero `query_collection` markers because Bricks strips `hasLoop` from the visible loop root and the generic rendered-post guard rejected current-owner collection markers while rendering related loop rows. After narrowing those guards for `acf_collection_field` + `query_collection`, the same probe found 5 query-collection markers on page `19`. The frontend now mounts linked-posts bridge buttons in the overlay layer, anchored to the resolved common Bricks query-loop items container rather than the fixed viewport rail; multiple badges for the same container are laid out horizontally with viewport-safe wrapping. On `FLO-Verticals-Single`, the intended target is the cards container `brxe-swnffk`. This still needs browser visual confirmation.
- The temporary linked-posts section badge now prefers the proven query target post type for labels such as `Review Posts`, and keeps a `Manage {Bricks label} Posts` fallback for future mixed-post-type query markers. Runtime probe `php /private/tmp/dbvc_ve_derived_collection_probe.php 19 review` returned `badgeLabel = Review Posts`.
- The first writable filtered-subset contract is now enabled for current-owner derived Bricks Query Editor linked-post loops. Runtime probe `php /private/tmp/dbvc_ve_derived_collection_probe.php 19 review` returned `status = editable`; a no-op resolver save for page `19` / `page_related_items` / `review` returned `saveOk = true` and kept the review subset `[1560, 19890, 22938]`; a stale-subset probe returned the expected conflict message before writing.
- User testing confirmed the derived Bricks Query Editor linked-post filtered-subset editor works after enabling the writable contract; the panel now also scopes selected/search labels to the proven target CPT and shows preserved non-target linked-item context. Browser visual confirmation for the copy refinement is still pending after syntax validation.
- Filtered-subset save results now provide supplemental durable-journal context for full source IDs before/after, target subset IDs before/after, preserved IDs before/after, descriptor target IDs, query element ID, and stale-conflict state. Syntax validation passed; a journal-row readback smoke is still pending because it requires a real save through the REST/UI path.
- Derived Bricks post-query collection detection now captures native dynamic include/post__in source evidence from saved Bricks controls such as `{acf_page_related_items}`. Static/manual native ID lists and opaque final-ID lists without saved ACF dynamic-tag evidence stay unsupported because the marker cannot prove an editable ACF source; custom Query Editor fallback branches are handled separately through current-owner exact-match, shared-option exact-match, or locked fallback evidence.
- Runtime guard probe `php /private/tmp/dbvc_ve_native_id_guard_probe.php 19 review` confirmed an opaque native-style final `post__in` list that exactly matches the current page's `review` subset still returns `status = unsupported` when no saved ACF dynamic-tag evidence or Query Editor hint is present. The same current-owner subset with Query Editor source hints remains editable via `php /tmp/dbvc_ve_derived_collection_probe.php 19 review`.
- Simple Query Editor loops that use direct `get_field('page_related_items')`-style current-owner reads now add source hints for exact current-owner filtered-subset matching. Calls with an explicit object argument, including `get_field(..., 'option')`, are intentionally excluded from writable hints until the shared-option fallback phase.
- Mixed/`any` derived post queries can now classify as full current-owner collection editors when final ordered query IDs exactly equal one relationship/post_object field's full stored value; these use the existing full collection contract instead of the filtered-subset contract. A read-only runtime probe on page `19` classified the full 22-item `page_related_items` value across seven post types as editable with `query_collection_write_mode = replace_full_collection`; browser confirmation is still pending.
- Mixed post-type derived query roots now preserve ACF source evidence and can reach the full-collection descriptor path. Read-only probe `php /private/tmp/dbvc_ve_current_mixed_summary_probe.php` on vertical post `23690` / template `26763` / element `zsfmel` returned `post_type = ""`, `post_types = [post, case-study]`, `field_name = page_related_items`, `status = editable`, `query_collection_write_mode = replace_full_collection`, and badge fallback `Manage posts-card Posts`.
- The same probe confirmed native dynamic mixed include/post__in controls preserve `{acf_page_related_items}` evidence even when the saved setting cannot be rendered during summary capture, while a static mixed `post__in` list still returns an empty summary and remains unsupported.
- Render probe `php /private/tmp/dbvc_ve_current_vertical_render_probe.php` confirmed the actual Bricks render for `/vertical/dentists/` emits 10 marked `brxe-zsfmel` repeated tags and 14 total `query_collection` markers; the first `zsfmel` tag includes `data-dbvc-ve-status="editable"` and `data-dbvc-ve-context="query_collection"`.
- Runtime probe `php /private/tmp/dbvc_ve_group_collection_probe.php` on vertical post `23690` / template `26763` / element `igjxbs` confirmed a Bricks Query Editor loop backed by nested ACF group relationship field `benefits_section_benefitsContent_related_items` now emits 4 editable `query_collection` markers. The probe also confirmed ACF reads this field by the exact flattened selector but not by the lowercased selector or leaf key, so the descriptor now carries `field_selector_raw` for the collection resolver.
- Shared-option fallback and seed-current-field collection matching now use the same nested-group candidate model as current-owner derived Query Editor collections. Existing fallback probe `php /private/tmp/dbvc_ve_shared_option_fallback_probe.php 19 post settings_globals_default_posts` still returns `status = editable`, `scope = shared_entity`, `sourceContext = shared_option_fallback`, `queryCollectionWriteMode = replace_target_post_type_subset`, and contract `shared_relationship_collection_filtered_subset`. A read-only reflection probe against page `23690` confirmed the nested grouped current-owner seed context for `benefits_section_benefitsContent_related_items` can preserve `field_selector_raw = benefits_section_benefitsContent_related_items` and `groupPath = [benefits_section, benefitscontent]` when targeting an empty `assortment-cpt` subset. A live nested-group option fallback fixture has not been identified yet.
- Source-summary metadata now includes `selector:benefits_section_benefitsContent_related_items` when a grouped collection descriptor carries a raw selector that differs from normalized `field_name`, giving panel/status QA a direct way to confirm the visible source details match the resolver's trusted selector.
- Empty current-owner derived query loops now have a first supported code path: query summaries can survive empty `post__in`/`[0]` results when explicit ACF source evidence and a concrete target post type are present, classification requires the stored target-CPT subset to be empty, and the captured query-vars hook can register a synthetic descriptor when Bricks never renders the loop element. Final render can then inject a hidden marker immediately after `<!--brx-loop-start-{element_id}-->` or mark a Bricks `.brx-query-trail` placeholder so the container-level badge still appears. Probe `php /private/tmp/dbvc_ve_mismatched_query_ids_probe.php` confirmed a non-empty raw ID list outside the target post type is filtered to an empty target subset while preserving the raw ID as non-target data and supporting query-trail marker anchoring. Authorized framework render probe for vertical post `28148` confirmed `zsfmel` now has a `query_collection` marker with `data-dbvc-ve-badge-label="Posts"` and `data-dbvc-ve-input="reference_collection"` inside `#brxe-yjxtqp`.
- Frontend query-trail badge fallback was added after live testing showed only the other query badges mounted. The fallback creates an entry for connected `.brx-query-trail[data-dbvc-ve-context="query_collection"]` markers against their parent container and exposes `data-target-id` on the mounted badge for console QA.
- Query-collection badge mounting now performs bounded delayed remounts and observes added/updated marker attributes so Bricks-managed query-trail markers that appear after the initial overlay pass still mount a container badge.
- The public descriptor map now exposes `index.queryElementId`, and the overlay can recover missing query-collection marker attributes onto `[data-query-element-id]` placeholders from the session map before mounting badges. This covers browser sessions where the descriptor exists but marker attributes are absent from the final DOM.
- For loops that render only `<!--brx-loop-start-{element_id}-->`, the overlay now scans comments and attaches the recovered query-collection marker to the comment parent container, e.g. `#brxe-yjxtqp` for empty `zsfmel`.
- User testing confirmed the empty comment-only Bricks loop recovery on `frameworkflo-live.local/vertical/websites-for-contractors/`: the `zsfmel` query loop rendered no item markup, the marker recovered onto parent `#brxe-yjxtqp`, and the `Posts` collection badge mounted for adding the first matching connected item.
- Custom Query Editor `post__in` branches that fail current-owner proof now classify as locked evidence instead of disappearing. A read-only runtime probe on page `19` with recent post IDs returned `status = readonly`, `source.type = derived_query_collection`, and `query_branch_state = query_editor_post_in_unmatched`.
- Exact options fallback evidence is now editable for the narrow target-CPT subset branch and exact full-field branch. A read-only runtime probe using `settings_globals_default_posts` filtered to `post` IDs returned `status = editable`, `scope = shared_entity`, `entity.type = option`, `source_context = shared_option_fallback`, `query_branch_state = shared_option_fallback_exact_match`, `resolver = acf_reference_collection`, `input = reference_collection`, `query_subset_write_mode = replace_target_post_type_subset`, and contract `shared_relationship_collection_filtered_subset` requiring shared acknowledgement. A full-field probe returned `query_collection_write_mode = replace_full_collection` and contract `shared_relationship_collection`. Browser save smoke is still pending.
- Exact shared-options fallback branches can now expose a separate `Add to current page field` action when Query Editor source hints prove exactly one current-owner relationship/post_object field and that current field has no existing target items. The route builds a current-owner seed descriptor, requires explicit seed acknowledgement, mutates through the existing collection resolver/mutation service, and reloads after save. Browser seed smoke is pending.
- Locked Query Editor fallback branches now use the `reference_collection_preview` panel input. Read-only runtime probes confirmed the unmatched branch returns 3 queried items with notice title `Inspecting Query Editor result`; exact target-CPT options fallback branches now promote to `reference_collection` and shared acknowledgement instead of staying in preview mode. Browser visual confirmation is still pending.
- The editable shared-option fallback panel now shows branch context for exact fallback matches before selected items, with separate copy for full shared fallback replacement versus target-post-type subset replacement. Syntax validation passed; browser visual confirmation is pending.
- User testing confirmed exact shared-option fallback saves and the explicit `Add to current page field` seed action work correctly on real fallback loops.
- Query-collection editors now expose a no-reload `Save` path plus `Save and Reload`; no-reload saves update the transient descriptor state to avoid immediate stale-subset conflicts on repeat saves. Seed actions now expose undo/reload controls instead of forcing an immediate reload. Syntax validation passed; browser confirmation for no-reload save, save-and-reload, seed undo, and seed reload controls is pending.
- Follow-up fix: seed and seed-undo actions now re-enable the primary collection `Save` / `Save and Reload` buttons after the secondary action completes, so manual item changes in the still-open panel remain savable after undo.
- Locked Query Editor fallback previews now include a source-evidence block that lists the active branch, queried count, target type, current/options field hints, dynamic tags, and saved Bricks include/post__in setting evidence where present. Syntax validation passed; browser visual confirmation is pending.
- Query Editor source-hint parsing now treats `get_field('field', get_the_ID())` and `get_field('field', get_queried_object_id())` as current-owner hints, while literal IDs and variable object arguments remain explicit-owner evidence. Runtime probe `php /private/tmp/dbvc_ve_get_the_id_query_hint_probe.php` against `dbvc-codexchanges.local` vertical post `23690` confirmed `page_related_items` classified as editable current-entity `acf_reference_collection` with `query_collection_write_mode = replace_target_post_type_subset`; literal ID, option, and variable object-argument controls stayed out of writable current-owner hints. Browser confirmation with a temporary Bricks template fixture was not needed for this parser/classifier validation.
- Query Editor source-hint parsing now also treats variables directly assigned from `get_the_ID()` / `get_queried_object_id()` as current-owner hints, so patterns such as `$current_post_id = get_the_ID(); get_field('relationship_gallery_cpt', $current_post_id)` can enter the same exact-match collection editor path. Runtime probe `php /private/tmp/dbvc_ve_current_alias_query_hint_probe.php` against `dbvc-codexchanges.local` vertical post `23690` confirmed direct aliases and queried-object aliases classify as editable current-entity `acf_reference_collection` with `replace_target_post_type_subset`; reassigned aliases, arbitrary variables, and literal IDs classify as readonly inspect-only even when final IDs match the current field; exact shared-option fallback still classifies as editable shared-entity `acf_reference_collection`.
- Hover badge presentation was corrected so `query_collection` descriptors only show `Inspect only` when their descriptor status is `readonly`; editable derived Bricks query collections should now display their real badge labels such as `Review Posts`.
- Direct current-owner repeater-row and flexible-row native `relationship` / `post_object` query roots should now reuse that same connected-items panel mode when the row path is stable.
- Mixed current-owner `repeater -> flexible` and `flexible -> repeater` native `relationship` / `post_object` query roots should now surface through the same `Edit Connected` flow when their nested row chain can be resolved canonically.
- Grouped current-owner row-owned native `relationship` / `post_object` query roots should now surface through that same `Edit Connected` flow when the native query path can prove the intermediate group ancestry canonically.
- Loop-owned related-post native `relationship` / `post_object` query roots should now surface through that same `Edit Connected` flow when Bricks exposes a concrete related post owner, and the panel should require related-owner acknowledgement before save.
- A WP runtime probe on page `86` using a fallback direct descriptor for `gallery_section_gallery` confirmed the new gallery save contract accepts an ordered attachment-ID array, sanitizes it, and round-trips a save successfully even when the local field is currently empty. A non-empty live gallery save still needs browser smoke on a page/template where the gallery marker is surfaced.
- Follow-up runtime validation confirmed gallery UI routing now maps editable gallery descriptors to `media_gallery_reference` and readonly gallery descriptors to `media_gallery_preview`. A synthetic empty Bricks `image-gallery` element for page `86` / `gallery_section_gallery` injected a parent-anchored marker with `data-dbvc-ve-context="gallery_collection"`, `data-dbvc-ve-missing-media="1"`, `data-dbvc-ve-missing-media-kind="gallery"`, and `data-dbvc-ve-input="media_gallery_reference"`.
- Live gallery fixture validation: `https://dbvc-codexchanges.local/vertical/websites-for-contractors/` / post `28148` uses `FLO-Verticals-Single` template `26763`, element `xxrpfg`, and ACF field `gallery_section_gallery` with stored IDs `[29113,29112,29110,29109,29335]`. Runtime probe `/private/tmp/dbvc_ve_xxrpfg_gallery_probe.php 28148` confirmed the gallery now renders exactly one editable wrapper marker on `<ul id="brxe-xxrpfg">` with `data-dbvc-ve-context="gallery_collection"` and `data-dbvc-ve-input="media_gallery_reference"`. The marker originally stripped because text comparison saw gallery markup as empty text vs resolved `5 images`; gallery verification now compares rendered attachment IDs instead.
- Empty same-template gallery control: post `23690` (`/vertical/dentists/`) has the same field empty. Runtime probe confirmed Bricks emits no `xxrpfg` markup and no marker, so fully condition-skipped gallery discovery remains a follow-up distinct from empty-output fallback markers that still reach the render hook.
- Gallery panel collection-management UI is now additive instead of replace-only by default: `Add images` appends unique selected Media Library images to the current ordered gallery, `Replace gallery` remains available for intentional full replacement, and each thumbnail exposes remove plus move earlier/later controls. Desktop drag-and-drop sorting now calls the same ordered gallery render path as the move buttons, preserving the existing save payload. Rendered image, background-image, and gallery markers now offer no-reload `Save` plus `Save and Reload`; no-reload saves patch visible DOM, while missing/conditional media markers remain reload-only. User QA on 2026-06-13 confirmed `xxrpfg` gallery add/replace/remove/reorder/no-reload save remains correct; direct rendered image and background-image no-reload clear/add-back browser QA remains pending.
- Panel compact warning UX: warning blocks now move to a footer notice beside the save controls as typed icons for shared, related, query collection, and current-post contexts, with the original warning text available through hover/focus tooltip and title/ARIA text. Shared/related acknowledgement checkboxes now render in that footer notice area. Connected-items selected/results lists are capped at `30svh` with internal scrolling to avoid large collections expanding the panel on load. Syntax checks passed for touched JS/PHP and CSS parsed cleanly; browser hover/focus QA is pending.
- Toolbar 2.0 token styling tranche: added root design tokens for primary/secondary/accent/light/dark colors, toolbar/icon dimensions, type scale, radius, border, shadow, blur, and repeated success/shared/info/warning/error surfaces; expanded those tokens across toolbar, upward popovers, status/review, panel chrome, collection rows, inputs, notices, WYSIWYG shells, and action buttons. Follow-up button contrast pass keeps primary panel buttons, active filters, shared actions, toolbar icon buttons, and disabled hover states readable. CSS parse and `git diff --check` passed. Editable element outline and badge source colors were left on the existing values. Browser visual QA is pending while manual browser testing is underway.
- Toolbar Shared Globals configured-field slice: added settings-backed field allowlist defaulting to `settings_globals_default_posts`, a REST inventory endpoint that creates toolbar-scoped option-owned relationship/post_object descriptors, and frontend popover wiring that opens configured fields through the existing panel. The popover is scoped to configured sitewide option fields only; current-page fallback query-loop descriptors remain in the regular status/review flow. Syntax checks passed for touched PHP/JS and `git diff --check` passed for the touched tracked files. Runtime probe `/private/tmp/dbvc_ve_shared_globals_probe.php` confirmed the default field resolves as ACF key `field_66bbd9ab78ecb`, type `relationship`, with 9 stored IDs; the controller returned one editable descriptor with contract `shared_relationship_collection`, `source_context = toolbar_shared_global_option`, shared acknowledgement required, and no warnings. Focused Playwright connected-items smoke skipped because local admin env credentials are not set. Live authenticated browser save smoke for the configured option field is still pending.
- Visual Editor settings/exclusions slice: added a dedicated DBVC -> Visual Editor submenu page while preserving Configure -> Add-ons as the same settings source. New post type and taxonomy exclusion settings default to `bricks_template`, `template_tag`, and `template_bundle`; exclusions apply to supported page context, descriptor/session surfacing, Go To Object, connected-item reference searches, linked-term searches, and Shared Globals target fields. Full ACF reference collection saves preserve hidden excluded stored IDs so omitted internal items are not silently removed. PHP lint passed for touched PHP files, targeted Visual Editor `git diff --check` passed, and targeted PHPUnit passed for `BricksAddonPhase1Test` plus `ConfigurationPortabilityRegistryTest`; browser settings-page and frontend search confirmation is pending.
- Review Fields index parent-section slice: the frontend now preserves `.dbvc-ve-field-index` scroll position across passive descriptor/status refreshes and adds a section layer above field item accordions using safe public index metadata such as immediate ACF group paths, row containers, field groups, option pages, and native loop labels. The redundant `dbvc-ve-field-index__subgroup-summary` toggle layer is removed, and each `dbvc-ve-field-index__item-title` keeps the field label and marked-field count in one row. `node --check addons/visual-editor/assets/js/overlay-app.js`, `php -l addons/visual-editor/src/Assets/AssetLoader.php`, and `git diff --check` passed. A focused jsdom smoke confirmed section labels such as `Hero`, `Intro`, `Benefits`, and `Page Settings`, preserved `scrollTop = 180` after descriptor-driven passive refresh, reset scroll to `0` after filtering, found no subgroup summary nodes, confirmed two item-title spans, and kept the filtered shared row count at `1`. In-app browser navigation to `dbvc-codexchanges.local/our-process/` did not complete in this environment, so live browser visual confirmation is still pending.
- Performance profiler baseline slice: added a disabled-by-default server profiler gated by `dbvc_visual_editor_performance_profile_enabled`, with shutdown summaries available through PHP error log and `dbvc_visual_editor_performance_profile_summary`. Syntax checks passed for the touched PHP files. Live profiling with the filter enabled, frontend `performance.mark()` coverage, and browser timing comparison on normal plus marker-heavy pages remain pending.
- Performance profiler live pass: temporary read-only probe `/private/tmp/dbvc_ve_perf_probe.php` enabled the profiler before WordPress bootstrap, rendered Bricks content, hydrated descriptor payloads in memory, persisted the session once for measurement, and suppressed error-log output. Page `86` (`/gallery/`) produced 8 markers / 8 descriptors and render-data repair under 1 ms. Page `24732` (`/our-process/`) produced 333 markers / 366 descriptors and a 2.5 MB session payload; final render-data repair remains about 1.1 seconds, but the new anchor prefilter reduced partial render-data chunks from about 95 ms to 10 ms and 443 ms to 101 ms while preserving marker counts. Post `23690` (`/vertical/dentists/`) produced 264 markers / 261 descriptors / 15 query-collection markers, with final render-data repair about 646 ms and repeated query-collection/native-readonly classification still a major hotspot. A token-present fast path was tested and reverted because it dropped `/our-process/` marker count from 333 to 329.
- Render-data cached opening-tag repair pass: `ElementInstrumentationService::finalizeRenderedData()` now reuses cached Bricks opening-tag matches for normal marker injection and invalidates the cache when fallback injection or duplicate-gallery cleanup changes the current HTML. `php /private/tmp/dbvc_ve_perf_probe.php 86` kept 8 markers / 8 descriptors with final repair about 0.55 ms. `php /private/tmp/dbvc_ve_perf_probe.php 24732` kept 333 markers / 366 descriptors / 7 query-collection markers and reduced the full final repair to about 762 ms, with descriptor chunk `362` at about 32 ms. `php /private/tmp/dbvc_ve_perf_probe.php 23690` kept 264 markers / 261 descriptors / 15 query-collection markers and reduced full final repair to about 107 ms. Browser panel/save QA was not part of this server-render optimization pass.
- Request-local memoization pass: `LoopContextResolver` now caches decorated active loop contexts by current Bricks loop state, `NativeAcfQueryResolver` caches native ACF query field definitions, and `ResolverRegistry` caches derived Query Editor ACF field objects/candidate/value/post-type lookups. `php /private/tmp/dbvc_ve_perf_probe.php 86` kept 8 markers / 8 descriptors. `php /private/tmp/dbvc_ve_perf_probe.php 24732` kept 333 markers / 366 descriptors / 7 query-collection markers, with active loop-context time about 99 ms and derived query-collection inspection about 2-3 ms. `php /private/tmp/dbvc_ve_perf_probe.php 23690` kept 264 markers / 261 descriptors / 15 query-collection markers, with active loop-context time about 103 ms, derived query-collection inspection about 3 ms, and readonly derived collection classification about 163 ms after cache warmup. Session persistence for marker-heavy pages remains a major follow-up hotspot at roughly 220 ms for 2.4-2.5 MB transient payloads.
- Compressed session descriptor-store pass: `EditableRegistry` now stores descriptor maps as gzip JSON/base64 blobs when available, decodes descriptors only for descriptor/save/search/hydrated paths, keeps `public_map` plain for initial frontend boot, and throttles TTL refresh writes through `dbvc_visual_editor_session_refresh_interval`. `php /private/tmp/dbvc_ve_perf_probe.php 86` kept 8 markers / 8 descriptors and reduced session payload from about 35 KB to 12.5 KB. `php /private/tmp/dbvc_ve_perf_probe.php 24732` kept 333 markers / 366 descriptors / 7 query-collection markers and reduced session payload from about 2.5 MB to about 534 KB, with persistence around 100 ms. `php /private/tmp/dbvc_ve_perf_probe.php 23690` kept 264 markers / 261 descriptors / 15 query-collection markers and reduced session payload from about 2.45 MB to about 450 KB, with persistence around 85 ms. Browser REST session, descriptor panel, save, and shared-global endpoint smoke remain pending.
- Derived-query profiler detail and direct option-hint fast-path pass: added server profiler spans/counters for ACF field-object cache hits/misses, derived field-candidate cache hits/misses, hinted option candidates, stored-value cache hits/misses, current-owner match loops, option-fallback match loops, fallback classification builds, and seed-context matching. Added frontend `dbvc.ve.*` Performance API measures for overlay boot, marker scans, public-map request/sync, query-marker recovery, marker/badge mounting, viewport prefetch setup/pump, descriptor requests, panel open/render, and save requests. `php /private/tmp/dbvc_ve_perf_probe.php 86` kept 8 markers / 8 descriptors. `php /private/tmp/dbvc_ve_perf_probe.php 24732` kept 333 markers / 366 descriptors / 7 query-collection markers. `php /private/tmp/dbvc_ve_perf_probe.php 23690` kept 264 markers / 261 descriptors / 15 query-collection markers and reduced the readonly derived-query branch from about 176 ms to about 2.3 ms by avoiding the cold all-option-field scan for direct hinted option fields. In-app browser timing smoke loaded `/vertical/dentists/`, but Visual Editor was inactive in that browser session (`DBVCVisualEditorBootstrap` absent, 0 markers), so authenticated browser timing confirmation remains pending.
- Render-data exact-tag fast-path pass: added profiler detail for final repair substeps, then changed normal marker repair to skip only when the exact current Bricks opening-tag occurrence already contains the descriptor token. A broad token-present skip was rejected because it dropped `/our-process/` from 333 to 327 markers; the exact-tag path restored 333 markers. Missing-media parent anchors now use the same fast opening-tag window lookup without the full-page regex fallback. `php /private/tmp/dbvc_ve_perf_probe.php 24732` kept 333 markers / 366 descriptors / 7 query-collection markers and reduced the full final repair from about 770 ms to about 214 ms, with missing-media fallback down to about 9 ms. `php /private/tmp/dbvc_ve_perf_probe.php 86` kept 8 markers / 8 descriptors with final repair under 1 ms. `php /private/tmp/dbvc_ve_perf_probe.php 23690` kept 264 markers / 261 descriptors / 15 query-collection markers with final repair about 55 ms. Browser panel/save QA was not part of this server-render pass.
- Session public-map compaction pass: `EditableRegistry` now recursively omits empty strings, `null`, `false`, and empty arrays from the public marker map, exports zero entity/owner IDs as omitted public fields, records session persistence substep timings, records public-map and descriptor-blob byte counts, and keeps descriptor compression configurable through `dbvc_visual_editor_session_descriptor_compression_level` with gzip level 4 as the default. `php /private/tmp/dbvc_ve_perf_probe.php 86` kept 8 markers / 8 descriptors with a 9.3 KB session payload, 4.5 KB public map, 4.3 KB descriptor blob, and `registry.persist_session` about 9 ms. `php /private/tmp/dbvc_ve_perf_probe.php 24732` kept 333 markers / 366 descriptors / 7 query-collection markers with a 399.7 KB session payload, 241.7 KB public map, 154.9 KB descriptor blob, `registry.persist_session` about 89 ms, and `registry.persist.set_transient` about 46 ms. `php /private/tmp/dbvc_ve_perf_probe.php 23690` kept 264 markers / 261 descriptors / 15 query-collection markers with a 374.1 KB session payload, 233.9 KB public map, 137.7 KB descriptor blob, `registry.persist_session` about 81 ms, and `registry.persist.set_transient` about 46 ms. A pre-compaction comparison on `/our-process/` showed the public map dropping from about 375.8 KB to 241.7 KB; browser panel/save QA was not part of this server-session payload pass.
- REST round-trip reduction slice: `SessionController` now exposes a minimal `touch` endpoint for session TTL/status refresh, `DescriptorController` now exposes a capped batch descriptor hydration endpoint, and the frontend uses touch-only keepalive/pre-action refreshes plus small viewport-prefetch descriptor batches. Cached descriptor hydrations are normalized with `ok: true` so batch/session hydration payloads are reusable by the same panel-open cache path as single descriptor responses. Syntax checks passed for touched PHP and JS files. Follow-up render probes kept page `86` at 8 markers / 8 descriptors and page `24732` at 333 markers / 366 descriptors / 7 query-collection markers. Authenticated local REST timing using the browser session bootstrap showed the full public-map session response at 390.9 KB, the new `touch` response at 92 bytes, a 4-token descriptor batch at 30.2 KB, and a single descriptor at 8.1 KB. True browser network-panel timing for viewport-prefetch request counts and explicit open reuse of an in-flight batch token is still pending because the browser automation context does not expose fetch/XHR or network capture.
- Shared edit-badge hover grace tweak: the passive hover-leave hide delay is now 500ms instead of the previous short delay, giving users more time to move from rounded media/card elements to the shared badge before it disappears. Syntax validation passed. In-app browser navigation reached `https://dbvc-codexchanges.local/our-process/`, but that browser session had no `DBVCVisualEditorBootstrap` and 0 markers, so authenticated live hover/click confirmation is still pending.
- Server-cost follow-up slice: `ElementInstrumentationService::finalizeRenderedData()` now reuses per-pass opening-tag/token checks and uses bounded opening-tag window scans instead of the expensive full-content opening-tag regex fallback; render verification now indexes descriptors by Bricks element UID before resolving loop signatures; `LoopContextResolver` now reuses one sanitized Bricks loop getter snapshot for cache-key and context-build work. `php /private/tmp/dbvc_ve_perf_probe.php 86` kept 8 markers / 8 descriptors, final repair about 0.54 ms, and reduced active-no loop-context calls to 16. `php /private/tmp/dbvc_ve_perf_probe.php 24732` kept 333 markers / 366 descriptors / 7 query-collection markers, final repair about 60.5 ms, `bricks.finalize.find_opening_tags` about 5.2 ms, and active loop-context resolve calls at 884. `php /private/tmp/dbvc_ve_perf_probe.php 23690` kept 264 markers / 261 descriptors / 15 query-collection markers, final repair about 24.2 ms, and active loop-context resolve calls at 529. Authenticated in-app browser DOM QA on `/our-process/` confirmed Visual Editor active, bootstrap/session present, and 592 rendered DOM marker attributes; true Resource Timing/network capture remains unavailable in this browser automation sandbox, so viewport-prefetch request-count QA remains a manual DevTools follow-up.
- ACF field-context request-local cache pass: `AcfFieldContextResolver` now caches Bricks ACF provider lookups/tags, parsed dynamic expressions, exact `get_field_object()` lookups, ACF field/group definitions, ACF options-page maps, owner entity mapping, and nested container field/raw-row reads within the current request. The field-object and container-row lookup paths preserve raw selector casing because grouped selectors such as `benefits_section_benefitsContent_related_items` are not safe to lowercase. `php -l addons/visual-editor/src/Bricks/AcfFieldContextResolver.php` passed. `php /private/tmp/dbvc_ve_perf_probe.php 86` kept 8 markers / 8 descriptors with final repair about 0.3 ms. `php /private/tmp/dbvc_ve_perf_probe.php 24732` kept 333 markers / 366 descriptors / 7 query-collection markers with final repair about 65.4 ms. `php /private/tmp/dbvc_ve_perf_probe.php 23690` kept 264 markers / 261 descriptors / 15 query-collection markers with final repair about 31.1 ms, including the vertical grouped-query/gallery fixture. The heavy timing totals included local single-call outlier noise, but marker and descriptor parity held. Browser panel/save QA was not part of this server-cache pass; manual DevTools Network QA instructions were added to the performance guide for authenticated touch and viewport-prefetch confirmation.
- Manual Chrome DevTools Network QA on authenticated `https://dbvc-codexchanges.local/our-process/`: Visual Editor loaded with the desktop Chrome session and completed Visual Editor REST rows returned `200`. Initial filtered Visual Editor traffic included `overlay.css` at 8.3 KB transferred / 98 ms, `api-client.js` at 1.2 KB / 85 ms, `overlay-app.js` at 51.7 KB / 13 ms, and `GET /wp-json/dbvc/v1/visual-editor/session/{id}` at 22.4 KB transferred / 391 KB resource / 6.41 s. The touch endpoint stayed small at about 0.8 KB transferred / 0.1 KB resource, but local response time ranged from 3.38 s to 6.75 s. Scrolling one viewport grew the filtered request count from `4 / 217` to about `18 / 223`, showing bounded lazy descriptor activity rather than eager full-page descriptor hydration. Completed descriptor batch rows were about 3.2-4.5 KB transferred / 26.9-37.9 KB resource each, with local response times around 3.3-6.7 s; one direct descriptor fetch was 2.4 KB transferred / 8.0 KB resource / 3.86 s. No saves were performed. The practical finding is that payload sizes now look sane for keepalive and bounded descriptor hydration, while LocalWP/server response latency remains the main user-visible cost to keep optimizing.
- Single embedded Bricks text tag slice: investigated `https://frameworkflo-live.local/listings/sample-apartment-for-rent/` post `107582` and template export `bricks_template-single-listing-urban-loft-inspired-draft-107593.json`. The export uses one pure ACF tag (`{acf_listing_price_notes}`), several single embedded text/html tags such as `Bathrooms: {acf_listing_property_details_bathrooms}` and `<strong>Property type</strong><br>{acf_listing_property_details_property_type}`, and one multi-tag card meta string. A read-only direct PHP bootstrap against the `frameworkflo-live` path was not a true site-content validation because `home_url()` resolved to `dbvc-codexchanges.local` and post `107582` was not loaded, but the Bricks ACF provider did expose the three ACF tags and the ACF context resolver mapped them to textarea/select/number current-entity descriptors. The isolated inspector probe `/private/tmp/dbvc_ve_embedded_inspector_probe.php` confirmed pure and single-embedded tags are supported while the multi-tag card meta remains unsupported. `php -l` passed for `DynamicDataInspector.php` and `ElementInstrumentationService.php`, `node --check addons/visual-editor/assets/js/overlay-app.js` passed, `php /private/tmp/dbvc_ve_perf_probe.php 86` kept 8 markers / 8 descriptors, `php /private/tmp/dbvc_ve_perf_probe.php 24732` produced 355 markers / 390 descriptors after surfacing additional embedded text markers, and `php /private/tmp/dbvc_ve_perf_probe.php 23690` produced 252 markers / 250 descriptors with current query/gallery/text descriptors still present. Authenticated frameworkflo browser marker/panel/save QA remains pending after the patched plugin files are deployed to that LocalWP site.
- Composite mixed Bricks text inspect-only slice: the isolated inspector probe `/private/tmp/dbvc_ve_embedded_inspector_probe.php` confirmed pure tags stay on `acf_field`, one embedded tag stays on `text_projection=single_embedded`, and multi-tag text now returns `source_type=composite_text` / `render_context=composite_text` with ordered literal/dynamic segments and child candidates. Syntax checks passed for `DynamicDataInspector.php`, `ResolverRegistry.php`, `DescriptorPayloadBuilder.php`, `NativeReadonlyResolver.php`, `DescriptorSummaryBuilder.php`, and `overlay-app.js`. Runtime probes kept page `86` at 8 markers / 8 descriptors and page `23690` at 252 markers / 250 descriptors. Page `24732` (`/our-process/`) produced 360 markers / 395 descriptors, including 5 readonly related-entity composite descriptors with `input=composite_text`, and REST descriptor payload building covered those composite descriptors. Authenticated in-app browser QA on `/our-process/` confirmed frontend Visual Editor mode, 633 DOM marker attributes, 6 composite marker attributes, and 0 markers after accidental Builder-mode navigation to `?bricks=run`, preserving the Builder guard. Console output showed only an existing NextBricks `gsap is not defined` error. The visible composite marker was in a top header banner and the in-app browser did not expose the shared badge reliably, so panel-open visual confirmation remains pending. Batch save, rollback, owner-group acknowledgement, and no-reload composite patching remain intentionally deferred.
- Follow-up user QA confirmed the composite text `Inspect Text` badge opens the composite panel successfully. Partial-lock parser coverage was added to `/private/tmp/dbvc_ve_embedded_inspector_probe.php`: `{acf_listing_property_details_bedrooms} Bedroom | {echo:unknown_provider}` now returns readonly composite inspection with `dynamic_count = 2`, `supported_child_count = 1`, and `unsupported_child_count = 1`; the unsupported tag remains a locked child row/value expression. Syntax checks passed for `DynamicDataInspector.php`, `ResolverRegistry.php`, `DescriptorPayloadBuilder.php`, `DescriptorSummaryBuilder.php`, and `overlay-app.js`. Runtime probes kept page `86` at 8 markers / 8 descriptors, page `24732` at 360 markers / 395 descriptors with 5 readonly composite descriptors, and page `23690` at 252 markers / 250 descriptors, so the widened inspect-only threshold did not add unexpected markers on those fixtures.
- Composite text batch-save readiness slice: descriptor hydration now includes `compositeText.saveReadiness` and child `saveReady` / `blockReason` fields for panel preflight display only; actual composite saves remain disabled. `php /private/tmp/dbvc_ve_composite_readiness_probe.php 24732` confirmed the first `/our-process/` composite descriptor reports `status = ready_pending_endpoint`, `canBatchSave = false`, `childContractsReady = true`, `readyChildCount = 3`, `blockedChildCount = 0`, `unsupportedChildCount = 0`, one related post owner group (`Go Plan`, post `23071`), and required acknowledgement type `related`. Child contracts were `loop_owned_repeater_row` for text, select, and number fields. `php /private/tmp/dbvc_ve_perf_probe.php 24732` held at 360 markers / 395 descriptors / 5 readonly composite descriptors after adding the readiness payload.
- In-app browser reload smoke on `https://dbvc-codexchanges.local/our-process/` confirmed frontend Visual Editor mode remained active and composite marker attributes were present after the readiness UI change. The readiness block is only inserted after a composite panel opens, so this smoke did not visually confirm the block. Browser console filtering found no Visual Editor overlay errors; the only errors visible in the broader browser log were existing NextBricks `gsap is not defined` errors.
- Composite text backend batch-save foundation: added `CompositeSaveController`, frontend `api.saveComposite()`, `MutationService::mutateBatch()`, and batch journal helpers without enabling user-facing `Save All`. Syntax checks passed for `ChangeJournalRecorder.php`, `MutationService.php`, `CompositeSaveController.php`, `Routes.php`, `api-client.js`, and the temporary route probe. `php /private/tmp/dbvc_ve_composite_route_probe.php 24732` rendered `/our-process/`, persisted a Visual Editor session, submitted all three current child values for an acknowledgement-gated composite descriptor, and confirmed the new route returns `400` with the related-owner acknowledgement message before writes. The descriptor readiness remained `status = ready_pending_endpoint`, `canBatchSave = false`, `childContractsReady = true`, `readyChildCount = 3`, `blockedChildCount = 0`, and `requiresAcknowledgementTypes = [related]`. Live changed-value save, rollback failure simulation, panel `Save All`, and no-reload composite DOM patch QA remain pending.
- Composite scalar `Save All` enablement slice: `DescriptorPayloadBuilder` now sets `compositeText.saveReadiness.canBatchSave = true` only when every child has an embedded writable scalar descriptor (`text`, `textarea`, `number`, `url`, `email`, or single-select). `overlay-app.js` now renders scalar child controls, reuses the related/shared acknowledgement checkbox, submits child index/value pairs through `api.saveComposite()`, updates the cached composite payload, and patches the active marker from the original Bricks template plus returned child display values without a reload. `php /private/tmp/dbvc_ve_composite_readiness_probe.php 24732` confirmed the first `/our-process/` composite descriptor reports `status = ready`, `canBatchSave = true`, `childContractsReady = true`, `readyChildCount = 3`, `blockedChildCount = 0`, `unsupportedChildCount = 0`, related owner `Go Plan` post `23071`, and `requiresAcknowledgementTypes = [related]`. `php /private/tmp/dbvc_ve_composite_save_restore_probe.php 24732` ran same-value save, changed child index `0` (`Cost Subtitle`) from empty to `DBVC VE QA restore`, verified `readAfterChange = DBVC VE QA restore`, restored the original empty value, verified `readAfterRestore = ""`, and returned 200 for change sets `37`, `38`, and `39` with `childCount = 3`. `php /private/tmp/dbvc_ve_perf_probe.php 24732` held at 360 markers / 395 hydrated descriptors / 5 readonly composite descriptors. Syntax checks passed for `DescriptorPayloadBuilder.php`, `CompositeSaveController.php`, `overlay-app.js`, and `api-client.js`. In-app browser smoke confirmed Visual Editor scripts, toolbar, hundreds of markers, and composite marker attributes on `/our-process/`, but panel-click QA for `Save All` remains pending because the only readily visible composite marker is a top banner whose shared badge overlaps the admin bar in the automation surface; direct marker click navigated to Builder mode, where marker count dropped to 0. No Visual Editor console error was confirmed; the visible console issue was the pre-existing NextBricks `gsap is not defined` error.
- Composite stale/rollback hardening slice: `api.saveComposite()` now submits `baseValues`, composite child controllers refresh their base values after successful saves, and `MutationService::mutateBatch()` rejects a submitted child when its base value no longer matches the backend value before any writes. `CompositeSaveController` returns `409` for `stage = stale`. `php /private/tmp/dbvc_ve_composite_stale_probe.php 24732` temporarily changed child index `0` (`Cost Subtitle`) from empty to `DBVC VE stale external`, submitted a composite save with the old empty base and attempted value `DBVC VE stale attempted`, received `409 / stage = stale / failedIndex = 0`, verified the field still read `DBVC VE stale external`, and restored it to empty. `php /private/tmp/dbvc_ve_composite_rollback_probe.php` used in-memory fake descriptors/resolvers to force the second child write to fail after the first child write; `mutateBatch()` returned `stage = write`, `failedIndex = 1`, recorded rollback evidence for child index `0`, and restored both fake values to their originals. A follow-up `php /private/tmp/dbvc_ve_composite_save_restore_probe.php 24732` confirmed normal same-value, changed-value, and restore saves still returned 200 for change sets `40`, `41`, and `42` with `childCount = 3`. Browser QA loaded the patched `overlay-app.js` asset and showed 634 markers / 7 composite markers plus Review Fields composite entries, but the in-app browser still did not fire the Field Index `Open` action into a panel even after adding token fallback and toolbar statusbar-action delegation. Console logs showed only the existing NextBricks `gsap is not defined` error, not a Visual Editor error. Live browser panel `Save All`, acknowledgement click, and no-reload DOM patch confirmation remain pending.
- Composite preflight block coverage: `php /private/tmp/dbvc_ve_composite_preflight_probe.php 24732` temporarily rewrote only the persisted Visual Editor session descriptor for the first `/our-process/` composite child (`Cost Subtitle`) and confirmed missing child descriptor, readonly child descriptor, structured child input, and unsupported mutation contract each return `400` before writes. The probe verified all child values were unchanged before and after every rejected request and restored the original session descriptor metadata at the end.
- Composite batch journal coverage: `php /private/tmp/dbvc_ve_composite_journal_probe.php 24732` changed and restored the first `/our-process/` composite child (`Cost Subtitle`) through the composite-save route. Change sets `43` and `44` each finished `completed`, used parent descriptor token `ve_4efdbeb870f8`, stored batch type `composite_text`, recorded `childCount = 3`, and created one completed item row for child indices `0`, `1`, and `2`. The same probe forced a controlled second-child failure with a fake resolver inside the WP runtime, confirmed a failed composite batch change set with one `failed` item and one `rolled_back` item, verified fake values returned to originals, then deleted the fake journal rows it created.
- Composite live-browser hardening follow-up: Review Fields `Open` now closes the Toolbar 2.0 popover before opening a field panel, panel outside-click handling ignores Visual Editor toolbar clicks, and non-openable marker nodes route directly to token-based descriptor panel loading instead of previewing hidden/zero-size nodes. Composite stale `409` responses now clear the active descriptor cache, preserve the attempted child input values, show stale panel state/copy with conflict styling, and keep `Save All` disabled until the field is reopened/refreshed. `node --check addons/visual-editor/assets/js/overlay-app.js` and `git diff --check -- addons/visual-editor/assets/js/overlay-app.js addons/visual-editor/assets/css/overlay.css addons/visual-editor/CHANGELOG.md addons/visual-editor/docs/qa/TEST_LOG.md addons/visual-editor/docs/enhancements/DBVC_VISUAL_EDITOR_ADVANCED_IMPLEMENTATION_GUIDE.md` passed. An in-app browser navigation attempt to `https://dbvc-codexchanges.local/our-process/` timed out before Visual Editor state could be inspected, so live browser confirmation for composite panel open, acknowledgement click, `Save All`, stale-state UI, and no-reload DOM patch remains pending.
- `universal_cta_options` / Site Settings global-link fields must remain read-only in Visual Editor and must not save through the overlay.
- Unsupported generic loop/query shapes should stay unmarked or honestly locked.
